# Plan: Swift port of XBolo — a faithful, distributable Mac Bolo

> **Convention:** every question gets a permanent ID, never reused or renumbered. Answered
> questions move to the Decisions log keeping their ID. Questions live only in these two
> sections — never in prose elsewhere.
>
> This document is the durable home for the plan (see D17) — it lives in the repo, not just in
> assistant-side session state, so it survives a dropped session. This doc + `docs/AGENT_NOTES.md`
> are the two common references for all three roles — status and decisions here, chronological
> log there. Role-specific instructions live separately, one file per role, and deliberately don't
> restate anything from either of these two: `CLAUDE.md` (IMPLEMENTER), `docs/PARITY.md`
> (PARITY), `docs/PLANNER.md` (PLANNER).

## Open questions

| ID | Question | Blocks | Priority |
| --- | --- | --- | --- |
| **Q14** | `explosions`-list attribution semantics: C has two different, irreconcilable rules for which player's `explosions` list a given entry lands in (shell-list owner for the range-expiry phase; hardcoded `client.player` — the local process's own identity, meaningless in a merged simulation — for collision/tank-hit phases). IMPLEMENTER's uniform `state.players[shell.owner].explosions` choice is internally consistent but matches neither C rule exactly. PARITY found no mechanical gameplay consumer of this list today (only `explosionlogic`'s cosmetic counter-decay-and-remove) — if that holds, this is a Wave-6 UI-layer decision, not a Phase-3 parity gate. Needs a ruling (or an explicit defer-to-Wave-6 note) before Wave 6 starts; does not block 5.3b. | Wave 6 | Medium |
| **Q23** | Wave 5.9 pre-brief — two one-line follow-ups found in off-limits files (`RunTick.swift`, `RecvSR.swift`), not fixed by the Wave 5.9 agent since those files are outside its scope | `RunTick.swift`'s `tankMoveTick(...)` call site needs 3 more forwarded arguments (`onMineExplosion`/`onSuperboomTerrain`/`onDropPills`) once Wave 5.9 adds them as parameters — until then, a tank dying via the dead-tumble timeout won't scatter its onboard pills (unchanged from today's behavior, not a new regression). `RecvSR.swift`'s 5 existing `smallboom`/`superboom` call sites each omit one of the three closures — once those params exist, a rare second-level recursive splash cascade *originating from a broadcast receive* would silently no-op at the second level; both files already have all three closures in scope at each site, this is purely a threading gap. Not code-blocking for Wave 5.9 or 6.4; tracked here so it isn't lost — whoever next touches either file should apply both one-line fixes. | Whoever next owns `RunTick.swift`/`RecvSR.swift` (likely Wave 6.4, which works with `RunTick.swift`'s territory) | Low — no current regression, just an unfinished thread |
| **Q24** | Cost-effective use of the agent paradigm: when do our agents (Implementer/Planner/Parity/Admin/deep-dive) actually need to log memories (durable notes outside the wave-status docs), versus relying on `docs/AGENT_NOTES.md`/`docs/PLAN.md`/project memory as-is? **Ruled 2026-09-03: deferred, to be resolved as part of the final after-action review** (`docs/notes/AFTERACTION.md`), not ruled now. A draft recommendation is parked there pending that review, not adopted. | None code-blocking; deferred to final after-action review | Low |
| **Q29** | Milestone D's signing/notarization sub-wave needs a real Apple Developer Team ID — `project.pbxproj` currently has no `DEVELOPMENT_TEAM`/`CODE_SIGN_IDENTITY` keys at all (confirmed 2026-09-05). This is an account/credential dependency, not an engineering decision — Jerod needs to supply or provision one before that sub-wave can proceed. | Milestone D's signing sub-wave only | Low now, rises when Milestone D is GO'd |

## Decisions log

Closed questions, newest last. IDs are permanent.

| ID | Question | Decision |
| --- | --- | --- |
| **D1** | Engine / platform | Native macOS, not Godot |
| **D2** | Build from scratch or fork? | ~~Fork and drive to fidelity~~ → **superseded by D13.** Now: Swift port using xbolo as reference implementation |
| **D3** | Fidelity target version | Mac Bolo **0.99.7bv** — the replay-log tooling decodes this version |
| **D4** | Network interop | Self-contained, you and friends. No protocol archaeology, no WinBolo interop |
| **D5** | Copyrighted original assets | Recreate clean-room replacements before any distribution |
| **D6** | Who draws the art | ~~You draw~~ → **superseded by D10** (generated glyphs, nobody draws) |
| **D7** | Sprite basis | Unicode / ASCII glyphs |
| **D8** | Upstream relationship | ~~Contribute fixes back~~ → **revised by D13.** Swift patches can't go to a C project. Contribute *bug reports and fidelity findings* instead |
| **D9** | Source of fidelity data | **No existing logs.** Emulation is the sole source; the measurement rig is its own gating phase |
| **D10** | Art style | **ASCII / unicode glyphs for all graphics.** Not a placeholder — this *is* the art |
| **D11** | Upstream courtesy | Open an issue flagging intent, framed on the learning goals (Xcode, AI / Apple Intelligence, networking). **Draft reviewed before posting** |
| **D12** | Project home | Take over the blank Xcode project slot; delete the SwiftUI stub (verified boilerplate) |
| **D13** | Language | **Full port to Swift, simulation included.** Legally clean: xbolo is MIT, which permits derivatives provided the original copyright notice is retained |
| **D14** | Location | Move out of Xcode's `UntitledProjects` scratch area to `~/Developer/XBolo` |
| **D15** | Name | Keep `XBolo` for now; revisit if/when a hard fork is declared |
| **D16** | Minimum macOS target (was Q9) | **macOS 26+** — required for FoundationModels (Apple Intelligence learning goal), and the dev machine is already on 27. Implemented: `Package.swift:7` declares `.macOS(.v26)` |
| **D17** | Where the plan document lives | **`docs/PLAN.md`, version-controlled in the repo** — supersedes the Xcode assistant's session-local plans directory, which is not durable across dropped sessions |
| **D18** | Numeric representation | **Use 32-bit floats (`Float` in Swift) for position, physics, and trig.** The reference C code directly uses float values (e.g., `Vec2f tank`, `Vec2f builder`) and trig/sqrt operations. Custom integer/fixed-point math would break bit-identical differential testing. |
| **D19** | Package manifest tools version | **Align `Package.swift` tools version with platform targets.** Tools version `6.0` does not support `.macOS(.v26)` (introduced in PackageDescription 6.2). Update `Package.swift` to declare tools-version `6.4` to support current system SDK features correctly. |
| **D20** | Test Harness & Socket Coupling | **Isolate and mock simulation code for unit test bridging.** The C code in `client.c` and `server.c` is tightly coupled with low-level POSIX sockets and pipe multiplexing. We must isolate pure state logic or wrap C entry points to avoid launching network services in differential tests. |
| **D21** | Project Renaming (was Q11) | **Adopt the engine-first architecture.** Rename the core simulation framework to `BoloKit` (supporting future expansion such as mods, plugins, and AI agents), and name the primary application `Bolo 2026`. |
| **D22** | Q12 resolution — mine-chain/flood + pill-scatter subsystem | **Split Wave 5.5 into two sub-waves rather than folding silently.** 5.5a = the producers (`explosionAt`, `superboomAt`, `chain`, `flood`, `droppills`) — the genuinely new, undesigned subsystem Wave 5.2b surfaced. 5.5b = the consumer (`explosionTick`, the per-tick pass that drains the **explosion particle lists** — `client.explosions`/`client.players[i].explosions` — *not* the chain-reaction ring buffers, which 5.5a's `chain()`/`flood()` drain themselves) plus `forestvis`, since it cannot be meaningfully tested until 5.5a's state exists. Both wire into the `onMineExplosion`/`onSuperboomTerrain`/`onDropPills` injection points Wave 5.2b already added. **Correction (2026-09-02, PARITY finding on Wave 5.5b audit):** the original text here said 5.5b "drains the chain list," which is wrong — that's 5.5a's job. Also, `forestvis` was subsequently moved out of 5.5b into Wave 5.3c per D23, before 5.5b was ever implemented — 5.5b as actually built (see the wave table) is `explosionTick` alone. |
| **D23** | Wave 5.3 phasing (raised by IMPLEMENTER's readiness check, agreed by Jerod due to cost) | **Wave 5.3 as tabled ("shellTick, builderTick, pillTick" in one row, ~1000 lines of C across three unrelated server.c handler families) is too large a unit of work/review — split into 5.3a (shellTick: `shelllogic`, `shellcollisiontest`, `recvcldamage`, `recvcltouch`, plus `killTank` — see below), 5.3b (builderTick: `builderlogic` + the server-side build/repair/mine/grab-trees handlers merged in per the Wave 5.1 scope note — absorbs the former Wave 5.4's `buildercollision` line item, since `tankCollision`/`testAlliance`/`findPill`/`findBase` already shipped in 5.1/5.2a), and 5.3c (pillTick: `pilllogic` + `forestvis`, moved out of 5.5b — pillTick's firing condition needs `forestvis` directly, so it must exist before 5.3c, not after). **Wave 5.4 is retired as a standalone line item** — its scope is now fully absorbed into 5.1 (testAlliance/findPill/findBase), 5.2a (tankCollision), and 5.3b (buildercollision). |
| **D24** | `recvclbuildroad`'s tautological tree-sufficiency guard (`if (trees >= trees)`, always true — flagged by IMPLEMENTER, blocked 5.3b start) | **Replicate bug-for-bug, do not correct.** Same discipline already applied to the dead-tank terrain-enum mismatch (Wave 5.1/5.2a) and `growtrees`' outer guard checking `(x,y)` instead of `(growx,growy)` (Wave 5.7 pre-brief): Phase 3 is behaviour-preserving, bug-for-bug; fidelity fixes belong to Phase 5, not discovered ad hoc mid-port. This is not a Swift memory-safety concern (unlike the `writeRun` x<256 guard, which was a genuine safety deviation), so there's no countervailing reason to deviate. Requires a named regression test documenting the tautology is intentional, matching the pattern already used for the other two replicated bugs. Unblocks Wave 5.3b. |
| **D25** | Q15 resolution — WinBolo as a Wave 6 networking reference | **Read-only, clean-room policy: WinBolo's architecture may inform Wave 6, its code may not.** Confirmed via github.com/kippandrew/winbolo: classic WinBolo/LinBolo source (client & server), GPL v2, "copyright 1998-2008 John Morrison." GPL v2 is copyleft — copying or closely deriving Wave 6 networking code from it would pull GPL obligations onto BoloKit, conflicting with staying MIT (D13). Permitted: reading WinBolo's server/ for architectural understanding (session lifecycle, NAT traversal approach, protocol framing) to inform design decisions. Not permitted: copying, transliterating, or closely paraphrasing its code — any function/algorithm it inspires must be written independently from a design description, not composed while looking at the source (mirrors D5's clean-room precedent for art assets). At the Wave 6 pre-brief, IMPLEMENTER documents which specific design choices (if any) were informed by WinBolo, so PARITY can audit for accidental over-similarity rather than discovering it after the fact. |
| **D26** | C oracle build flag — `-ffp-contract` | **`CXBolo` target now builds with `cSettings: [.unsafeFlags(["-ffp-contract=off"])]` in `Package.swift`.** Wave 5.3b's broad-range fuzzing (not Wave 1's narrow 9-value grid) found `dot2f`/`mag2f` mismatching the C oracle on ~15-26% of random inputs (PARITY independently confirmed 26.5% via direct assembly A/B and traced it to Clang's default `-ffp-contract=on` fusing `v.x*v.x + v.y*v.y` into a single `fmadd` on arm64 — one rounding — versus Swift's never-contracting `+`/`*` — two roundings). This is a C-compiler code-generation gap in the **oracle**, not a Swift-side bug, and affects nearly every differential test that transitively calls `mag2f`/`dot2f` (tank physics, shell physics, collision — effectively all of Wave 1 onward). The flag is a pure fix: re-running the full existing suite (31 differential + 199 unit tests as of 5.3b) after applying it required zero changes to any prior wave's expected values — it closes a latent gap without invalidating any already-verified result. Recorded here with its own ID (not folded into the 5.3b wave-status note) because it changes what "the C oracle" computes for every future wave, not just this one — anyone reasoning about oracle behavior from here on should know this flag is in effect. |
| **D27** | Wave 5.3c FAIL/fix — shared per-tick mutable state under a per-player-call convention | **Established pattern: when N independent per-player replicas in C all mutate what becomes ONE shared field in the merged simulation, a naive "call once per connected player, in index order" loop is not equivalent — a later bystander's call can silently overwrite an earlier target's result within the same tick.** Caught by PARITY on `pillTick` (`state.pills[i].counter` reset by every non-target player's call after the real target's), not caught by the original test suite (no test exercised a same-tick multi-player sweep). Fixed by rewriting to a single per-tick election over all players at once — gather eligible candidates, compute the argmin (ties survive together and all fire, since tied replicas are independently deterministic and would all cross threshold together in the real distributed game), apply exactly one state transition per shared field per tick. Also surfaced a freeze-vs-reset distinction ("no alive connected player" leaves the field untouched, vs. "alive players exist but none eligible" explicitly resets it) that isn't obvious from the ordering bug alone. **Recorded as a decision, not just a bug-fix note, because Wave 5.5a/5.5b's mine-chain/flood/explosionTick work involves the same shape of shared per-tick state (chain lists) and should be designed as an election/single-pass model from the start, not discovered the same way twice.** |
| **D28** | Project artifact maintenance policy (raised by Jerod) | **No project artifact — docs, source, or tests — shrinks without an explicit, on-record replacement.** Formalizes the pattern already followed in practice (e.g. Wave 5.3c's 26→28 pillTick tests when the design changed): a test/doc/module CAN be rewritten or replaced when scope or design genuinely changes, but coverage or content can never just silently disappear. Concretely: (1) IMPLEMENTER's wave-completion reports already state before/after test counts — that continues, and going forward any DECREASE in a wave's test count vs. the prior wave's total must be called out explicitly with the reason (not left for PLANNER/PARITY to notice by diffing numbers); (2) PARITY's audits already check for coverage gaps case by case — this makes it a standing checklist item, not incidental; (3) the running test count is now tracked directly in this table's Notes column per wave (see rows above/below) so a regression is visible at a glance across the whole project, not just within one wave's report; (4) Wave 5.8's docs/archive pass (Q13) is extended to cover WHY superseded tests were replaced, not just that PLAN.md/AGENT_NOTES.md got compressed — archive.md entries for Wave 5+ should note test-count deltas and the reason, same granularity already used for code findings. |
| **D29** | Wave 5.6 `kPif` vs `Float.pi` for the post-spawn dir conversion | **Keep `kPif`, override the original Wave 5.6 trap note.** The trap note's `Float.pi` suggestion was a consistency preference with no behavioral basis (bit-identical either way, per D18). IMPLEMENTER observed every other shipped call site doing this exact `dir * (π/8)` conversion — `killBuilder` (5.2b), `roundDir` (5.0), `Vector.swift`'s dir-to-radians helpers — already uses `kPif`. Matching existing codebase convention is the stronger consistency argument; introducing a second spelling of the same constant for one call site would be the actual inconsistency. No change requested. |
| **D30** | Q13 resolution — Wave 5.8 docs/archive pass | **Executed as originally scoped, 2026-09-02.** `docs/AGENT_NOTES.md`'s Wave 5 chronological log (5.0–5.7, ~3700 lines) compressed into `docs/notes/archive.md` following the existing Waves 1–4 convention — commit hashes, key findings (D26 fp-contract fix, the Wave 5.3c FAIL/fix cycle behind D27, the Wave 5.7 Pill.counter/coolCounter catch), full uncompressed text preserved in git history per D28. `AGENT_NOTES.md` trimmed to its format/index scaffolding plus a fresh active log — a sanctioned exception to its own append-only convention, noted inline. `CLAUDE.md` rewritten to drop stale line-specific cross-references and reflect Wave 5's completion. Project memory (plan-status.md/roles-workflow.md/project-overview.md) refreshed to match. **One sub-item not resolvable by Claude directly:** the project-instructions config (a Cowork project setting, not a repo file) is still stale — no tool exists to edit it programmatically; replacement text was drafted and handed to Jerod to paste in manually. |
| **D31** | Q16 resolution — Wave 6 transport approach | **Port the wire format byte-exact from the C oracle; rebuild the transport mechanism on Network.framework + async/await.** D4 (no interop requirement) places no fidelity obligation on the mechanism itself, and transliterated POSIX/`select`/pthread glue isn't differentially testable, so there's no countervailing reason to port it bug-for-bug. DEEPDIVE1's protocol map (`docs/notes/DEEPDIVE1.md`) already specifies the wire format to the level of a usable 6.0 spec — 20 client→server / 34 server→client opcodes, the 113-byte `CLUpdate` header, all three encodings (raw BE float, 1/256 fixed-point, 8-bit brads), the join handshake, and eight cataloged oracle bugs for the trap list — so 6.0 is transcription plus differential tests from here, not discovery. |
| **D32** | Q17 resolution — Wave 6 sub-wave split confirmed | **6.0 wire codec (derived from the oracle per D31/D33) → 6.1 tick orchestrator (`runclient`/`runserver`) → 6.2 `recvsr*` broadcast handlers → 6.3 server session logic (join/kick/ban/alliance) → 6.4 transport + join handshake (Network.framework, per D31) → 6.5 tracker/NAT-PMP.** 6.5 carries a licensing constraint independent of D13/D25/D33: `README.md:61-64` (moved from `:42-45`, corrected 2026-09-03 per the Wave 6.5 pre-brief's citation-drift note) already commits on the record that XBolo's GPLv3 `TCMPortMapper` dependency is not used here, and NAT traversal will use a permissively-licensed alternative or manual port forwarding — that public statement governs 6.5's design, not just the general MIT posture. |
| **D33** | Q19 resolution — GPL flexibility explored, decision: stay clean-room and MIT | **D13 (BoloKit stays MIT) and D25 (WinBolo is read-only/clean-room reference, never copied or adapted) are reaffirmed, not loosened.** Jerod's big-picture call (2026-09-02): fidelity is maintained via the oracle-derived path, and the server side can be designed without derivative code using a modern architecture — so there's no longer an open question to trade GPL exposure for time savings. This also settles a practical point DEEPDIVE1 raised: even a byte-identical WinBolo match would only have saved transcription, since an adapted codec would still need the same field-by-field validation against the oracle that deriving it directly already required — and D31 shows that derivation is done. Resolves the tension DEEPDIVE1 flagged with `README.md:42-45`'s existing public GPL-avoidance commitment: no reconciliation needed, the public statement stands as written and this decision confirms it rather than superseding it. |
| **D34** | Q20 resolution — WinBolo-server substitution not pursued | **Not pursued, for the same reason as D33.** PLANNER's confirmation against `server.c:620-687` established that the oracle's server is a thin, trusting relay for tank/builder physics (client-asserted position, blind-forwarded, no recomputation) but an authority for shared arbitrated state (pill contests, tree growth, spawn, base repair, pillbox firing) — and the Wave 5 benchmarks already document a known-bad WinBolo case in exactly that layer (pillbox range). Building BoloKit's own server-side logic under D31/D32 avoids that risk entirely rather than accepting it in exchange for licensing convenience that D33 no longer needs. |
| **D35** | Wave 6.1 PARITY findings ruling | **Both findings get fixed before Wave 6.2 opens, not deferred like Wave 5.9.** Finding 1 (`server.c:1192-1197`'s `pauseonplayerexit` → `server.pause = -1` side effect, missing from `GameState`/`RunTick.swift`) is a real gap inside the exact function Wave 6.1 claims to fully port — add a `pauseOnPlayerExit` field and wire the mutation. Finding 2 (`client.c:451`'s `seq != 0` move-tank gate, undisclosed) needs only a one-line disclosure matching the existing `timelimitreached`/`basecontrolreached` note. Unlike Wave 5.9's mine-cascade gap, neither is independent of 6.2+, so both close before 6.2's coding GO rather than becoming tracked debt. IMPLEMENTER to plan and execute directly — see `docs/AGENT_NOTES.md`, 2026-09-03. |
| **D38** | Wave 6.3 PARITY secondary note — ~19 unassigned `recvcl*` server handlers, same shape as D36 | **Assigned to new Wave 6.6.** Server's TCP receive side for every `CL*` opcode except `CLSetAlliance` (that one shipped in Wave 6.3) had no assigned wave — same gap shape as D36. Not folded into Wave 6.3 (already closed/PARITY-passed) or Wave 6.4 (would bloat an already-scoped transport wave with D36's own dgramclient/tracker-echo additions). Given its shape matches 6.2/6.3 exactly (pure decision/mutation core, no transport dependency) and Wave 6.4 hasn't started coding yet, recommending Wave 6.6 run *before* Wave 6.4 — so 6.4's dispatch wiring has real handlers to call for every `CL*` opcode from day one, not just `recvClSetAlliance`. Sequencing is Jerod's call if he wants it reordered, same latitude as Wave 5.9. Pre-brief GO issued 2026-09-03 — see `docs/AGENT_NOTES.md`. |
| **D39** | Wave 6.3 PARITY secondary note — `server.pause`/`client.pause` unit mismatch (flagged by IMPLEMENTER, confirmed independent of Wave 6.3 by PARITY) | **Fix required before Wave 6.4's pre-brief GO, same precedent as D35.** The C oracle has two separate `pause` variables with different units and lifecycles: `server.pause` (ticks, decremented once per `runTick`, Wave 6.1) and `client.pause` (raw wire seconds, written verbatim by `recvsrpause()`, never decremented in `client.c`). This port's single `GameState.pause` field means `RunTick.swift`'s tick-domain countdown and `RecvSR.swift`'s `recvSrPause` decode write the same field in different units — a real hazard the moment one process holds both roles simultaneously (exactly the in-process-host shape Q22 is about). Not a C-oracle bug to replicate (D24 doesn't apply — this is a Swift-side unification choice, not an oracle behavior); fix by splitting into two fields mirroring the C's own two variables (e.g. `serverPauseTicks` / `clientPauseDisplaySeconds`), updating both write sites (`RunTick.swift`'s countdown, `RecvSR.swift`'s `recvSrPause`) and `assembleBoloPreamble`'s read (already correctly reading the server/tick-domain role — confirm it points at the renamed field). Named regression test required (D28) covering both fields independently. Both already-PARITY-passed waves (6.1, 6.2) get touched by this fix — same as D35/D37's precedent for touching prior waves' code when a cross-role hazard surfaces later. IMPLEMENTER to plan and execute directly, then report before Wave 6.4's pre-brief GO. **Closed — fixed `029c8fc` (447 tests), re-audited clean `b1efc12`. Wave 6.4's pre-brief gate is satisfied.** |
| **D40** | Wave 6.6 pre-brief finding — `recvclbuildroad`'s tautological tree-cost check (`if (clbuildroad->trees >= clbuildroad->trees)`, always true, `server.c:2416`) | **Replicate bug-for-bug, do not correct — same precedent as D24.** This is a deterministic, well-defined C comparison (not undefined behavior — contrast with `applyDamage`'s already-ruled `pills[-1]` case, which *is* UB and was correctly left non-literal for that reason), and it is not a Swift memory-safety concern, so D24's own reasoning applies without modification: Phase 3 is behaviour-preserving, bug-for-bug; fidelity fixes belong to Phase 5, not discovered ad hoc mid-port. The practical effect (road building never actually costs trees, `recvclbuildroad`'s "insufficient trees" `else` branch is dead code) is a real, user-visible gameplay quirk, not just an internal bookkeeping oddity like Q21 — but that makes it more important to preserve faithfully, not less, since a silent "fix" here would make this port's road-building economy diverge from the oracle's actual behavior. Port it literally as `clbuildroad.trees >= clbuildroad.trees` (or equivalent), with a named regression test documenting the tautology is intentional and asserting road construction always succeeds regardless of `trees`, matching D24's own required-test pattern. IMPLEMENTER cleared to code Wave 6.6 per this ruling. **Applied `ebb8fe4`** — replicated literally, plus a disclosed second-order effect: since the tautology makes the success branch the only reachable one, the leftover-trees ack can go negative (`trees - roadTrees` when `trees < roadTrees`), passed through as a plain `Int` un-truncated here (matches the C's own `uint8_t` wraparound happening at the wire-struct boundary, a Wave 6.4 concern, not this handler's) — named regression test covers this explicitly, not just the "always succeeds" half. |
| **D41** | Wave 5.9 pre-brief finding — `smallboom`/`superboom`'s self-splash-damage exclusion depends on C client/server network round-trip latency that this single-process, synchronous port doesn't have | **Approved: defer the `explosionAt`/`superboomAt` call until after the causer's own `dead = true` is set, not where the literal C statement order would suggest.** The C oracle's self-exclusion (`recvsrsmallboom`/`recvsrsuperboom`'s tank-damage check guarded only by `!dead`) relies on real network latency: by the time a causer's own broadcast round-trips back, its local `dead` flag already landed synchronously well before any socket I/O completed. `BoloKit` has no such latency, so porting the call in its literal source position would let the causer take a second, spurious splash-damage hit from its own detonation — a real, distinct-from-D24 hazard, since this isn't a C bug to replicate but a timing assumption the C's distributed architecture made that the single-process merge invalidates (same general class as Q14's already-noted "local process identity is meaningless in a merged simulation" observation, and D27's per-tick-election principle for collapsed multi-role state — naming this its own decision because it's a distinct pattern: **timing-dependent self-exclusion**, not shared-state ordering). General principle for future waves: when a C behavior's correctness depends on network round-trip timing rather than pure logic, identify what invariant that timing was actually protecting (here: a causer never takes splash damage from its own detonation) and preserve *that invariant* directly in the single-process port, even where it means reordering code relative to the C's literal statement sequence. Verified independently for both entry paths (first-death and already-dead) and against recursive-escalation re-entry — see the pre-brief's full trace. Named regression test required per D28 (no double-damage assertion), same pattern as every other project ruling. |
| **D42** | Wave 6.4 pre-brief Finding 1 — real tension between `Buf.swift` (Wave 1, transliterated POSIX sockets) and D31 (Network.framework ruling) | **Confirmed: `Buf.swift`'s pure byte-queue half (`initbuf`/`writebuf`/`readbuf`/`freebuf`/`resizebuf`) stays as the accumulation buffer BoloNet's decode functions consume; its POSIX socket-layer half (`sendbuf`/`recvbuf`/`selectreadwrite`/`selectreadread`/`cntlsend`/`cntlrecv`) is NOT used — exactly what D31 already ruled out ("transliterated POSIX/select/pthread glue... isn't differentially testable, so there's no countervailing reason to port it bug-for-bug"). New async/await code against `NWConnection`/`NWListener` handles actual socket I/O, feeding bytes into/out of the same byte queue. `docs/PLAN.md`'s Wave 6.4 row text ("builds on `Buf.swift`'s existing `sendbuf`/`recvbuf`/`cntlsend`/`cntlrecv`") was imprecise shorthand and is corrected by this entry — it should always have meant the byte-queue half only; good catch by IMPLEMENTER reading it literally rather than assuming intent. |
| **D43** | Wave 6.4 pre-brief Findings 2 + the host-vs-client architecture question tied to Q22 | **`dgramserver()`'s pure dedup/relay/apply logic is in scope, same as `dgramclient()`'s under D36 — D36's text just didn't name it explicitly.** Real, substantial, differentially-testable logic (once a new oracle export exists for it), not a thin wrapper. On the host-vs-client question: **Wave 6.4 needs both roles regardless of Q22's still-open product-packaging outcome** — even a single in-process-hosting "Bolo 2026" app needs a working host accept loop and a working joining-client handshake in the same binary, so this isn't gated on whether a separate dedicated headless server binary ever ships. Not deciding Q22 itself, which stays open for Jerod. **Splitting Wave 6.4 into 6.4a (client: join handshake + `dgramclient` application) and 6.4b (host: accept loop + `dgramserver` relay + the `dgramserver`-occurrence tracker echo), same precedent as D23's Wave 5.3 split** — this is genuinely two units of work bundled under one name, and 6.4a's pure logic is more directly oracle-testable (Wave 6.0's `clupdate_decode_oracle`) than 6.4b's (needs a new oracle export), so sequencing 6.4a first reduces risk before the harder half. `registerserver()`'s separate tracker-echo occurrence stays deferred to Wave 6.5 as the pre-brief proposed — confirmed, not a new call. |
| **D44** | Wave 6.4 pre-brief Finding 3 — `dgramclient()`'s dead-reckoning extrapolation loop is unbounded in the C oracle, a real griefing/DoS vector once real transport exists | **Approved: bound at `Int(ticksPerSec) * 3` (3 seconds of ticks), as proposed.** Same class of deviation D36 already framed this as (a `writeRun`-class Swift-side safety bound, not a fidelity fix) — this entry just confirms the specific value. 3 seconds smooths an ordinary UDP burst-loss gap while bounding worst-case per-packet CPU; named regression test required per D28 covering both the normal (unbounded-equivalent, gap smaller than the bound) and bounded (gap exceeds it, extrapolation clamps) cases. |
| **D45** | PARITY's Wave 6.4a audit finding — `joinclient()`'s back half (`client.c:690-750`, applying a received `BoloPreamble` to `GameState`) has no Swift home anywhere in the codebase; the pre-brief mis-cited server-side `evaluateJoinRequest`/`applyJoin`/`assembleBoloPreamble` as covering it, and neither PLANNER's `ab101da` ruling nor PARITY's own earlier stand-in assessment caught the mix-up before coding | **Confirmed as a real completeness gap, not a fidelity bug — Wave 6.4a is not closed.** This is scope the sub-wave already claimed ("join handshake") going unfinished, same shape as every other "found a gap in what a wave already owns, fix it now rather than tracking it separately" ruling in this project (Wave 5.9's corpse-explosion gap, Wave 6.3/6.6 completing full functions). **Wave 6.4a's scope extends to cover:** (1) the missing client-side preamble-to-`GameState` application function (player index assignment, roster init, pause/gametype — mirroring D39's server/client pause-domain split for the client's own `clientPauseDisplaySeconds` field, per-player init, one `spawn()`-equivalent call reusing whatever this port's existing tank-spawn logic already is — confirm before writing new spawn code, per PARITY's own suggestion); (2) the persistent `NWConnection` receive loops (UDP → `applyRemotePlayerUpdate` per packet; TCP → dispatch decoded `SR*` opcodes to the already-shipped `recvSr*` handlers, Wave 6.2) that D43's split named the pure functions for but never explicitly assigned the actual always-running loop around them to either sub-wave. **Wave 6.4b's own pre-brief must explicitly verify** whether it has the mirror-image gap on the server side (does Wave 6.3 fully cover `joinplayerserver()`'s "new player" state init, or was that also assumed rather than confirmed?) before it's treated as safe, plus explicitly own its own TCP `CL*`-dispatch receive loop — not inherit 6.4a's oversight by repeating the same kind of assumption. IMPLEMENTER to plan and execute the extension directly, same two-stage process. |
| **D46** | PARITY's Wave 6.4a extension audit finding — `applyBoloPreamble` never writes `state.baseControlThreshold` from `preamble.baseControl` (`client.c:690-713`'s `client.game.domination.basecontrol = ...` assignment has no Swift counterpart); not caught by the extension's own tests despite one constructing a non-default `baseControl: 60` value | **Confirmed real, load-bearing, and required before Wave 6.4a closes — same precedent as D45.** `GameState.baseControlThreshold`'s own doc comment already states `0` means an instant domination win the moment all bases are held, and `RunTick.swift`'s win-condition timer (`Int(ticksPerSec) * state.baseControlThreshold`) depends on it directly; `assembleBoloPreamble` (server side, Wave 6.3) already reads this same field correctly to build the outgoing preamble — the write path exists on the send side and was simply never mirrored on the client's receive-and-apply side. Fix: add `state.baseControlThreshold = Int(preamble.baseControl)` to `applyBoloPreamble`, extend the existing `applyBoloPreambleInitializesLocalPlayerAndSpawns` test (already constructs `baseControl: 60`, just needs the assertion) rather than writing a new test from scratch (D28). PARITY's separate minor, non-blocking observation — `decodeBMap` validates the preamble before wiping terrain, where the C wipes unconditionally first — is accepted as a defensible Swift-safety preference (no mutation on hard failure) and does not need a fix; recorded for the log only. The classic-`NWConnection`-API choice and `BoloNet`'s new `import Foundation` (for `Data`) are both approved as reasonable engineering calls — CLAUDE.md's no-Foundation rule is scoped to `BoloKit` sources specifically and was never stated to extend to `BoloNet`, and `Network.framework`'s own send/receive surface is `Data`-based regardless of API style chosen. |
| **D47** | Wave 6.4b pre-brief open question 1 — G-2's placement (the `sendsr*` broadcast fan-out, ~30 mechanical callback→opcode wirings, roughly doubles 6.4b's size) | **G-2 stays in Wave 6.4b as proposed — no 6.4c split.** Unlike D43's 6.4a/6.4b split, which separated two architecturally distinct roles (client vs. host), G-2 is the same concern as the rest of 6.4b: it's the send-side counterpart to the accept/receive loop already in scope, and D45's own principle applies directly — a host that can't broadcast anything is not a working host, and deferring scope a wave already implies is exactly what D45 exists to prevent. The size increase is volume, not complexity: all 34 `SR*` structs already have `encode()`/`wireSize` (confirmed by PARITY's independent grep), so this is mechanical wiring against an already-built encoder, not new design work — a weaker case for splitting than D43's, where 6.4a and 6.4b were each independently substantial and separately oracle-testable. If coding surfaces unexpected complexity (not just volume), IMPLEMENTER should flag it in the completion report and PLANNER will reconsider then — not preemptively split on a size estimate alone. |
| **D48** | Wave 6.4b pre-brief open question 2 — T-4's correction to D36's text (the two tracker echoes — `dgramserver()`'s verbatim-bytes echo, in 6.4b's scope, vs. `registerserver()`'s zeroed `player == 255` echo, deferred to 6.5 — are not the same mechanism) | **Confirmed.** PARITY independently re-derived both call sites directly against `server.c` (`~641` and `~1471`) and confirms they are genuinely different: `dgramserver()`'s echo sends the received buffer back unmodified, no zeroing; `registerserver()`'s does an explicit `bzero` then sets `player = 255`. D36's original text ("the tracker reachability echo (zeroed `CLUpdate`, `player == 255`) must be implemented in the server's UDP receive path as part of 6.4") described only the `registerserver()` shape and is corrected by this entry — 6.4b implements `dgramserver()`'s verbatim-echo occurrence; `registerserver()`'s zeroed occurrence stays in Wave 6.5, unchanged from D43's confirmation. No wave reassignment, text correction only. |
| **D49** | Wave 6.4b pre-brief open question 3 — T-11's single-pending-joiner serialization (`server.c`'s `listensock` only re-enters `readfds` when `joiningplayer.cntlsock == -1`) | **Replicate, as IMPLEMENTER recommended — serialize joins with an actor, don't allow concurrent accepts to race slot allocation.** This governs join-time ordering behavior, not wire format, so D31's "rebuild the mechanism, don't port it bug-for-bug" latitude would technically permit relaxing it — but the C's serialization is exactly the kind of invariant D41's general principle already flags: identify what the timing/ordering was actually protecting (here, exactly one slot-allocation decision in flight at a time, so two simultaneous joiners can never race for the same free player slot) and preserve that invariant directly rather than inventing new concurrent-safe allocation logic with no oracle to validate against. An actor-serialized accept queue is the natural Swift-concurrency equivalent. Named regression test required (D28): two joins arriving concurrently resolve to distinct slots deterministically, one at a time. |
| **D50** | Q25 — Wave 6.4b's completion report disclosed three real, PARITY-confirmed open issues (`dropPills` broadcast gap, unwired live UDP listener, zeroed `dgramaddr` at join) and asked whether they become tracked follow-up scope or stay noted | **All three become tracked follow-up scope — new Wave 6.4c, sequenced before Wave 6.5's coding GO.** None of the three is a defect in what Wave 6.4b actually shipped (both IMPLEMENTER and PARITY confirm each is either pre-existing (the `dropPills` gap predates Wave 6 entirely, from Wave 5.5a's `droppills()`) or a legitimate, correctly-disclosed scope boundary D47's coding GO never named (the live UDP listener, and `dgramaddr` derivation which has no consumer until that listener exists) — so Wave 6.4b closes clean, not on probation. Bundling as one small sub-wave rather than three separate items or silent notes because they share one theme (finish wiring the host transport for actual live gameplay, not just its pure decision logic) and two of the three are directly coupled (`dgramaddr` can't be meaningfully fixed before the UDP listener exists to consume it). Scope: (1) wire `NWListener(using: .udp)` to drive `DgramServerRelay.swift`'s existing, already-tested decision core against real datagrams; (2) derive `dgramaddr` from the joining TCP connection's real address at join time (`server.c:844`, not `:817` — PARITY's cosmetic citation-drift fix folds in here since this is the next natural touch of that area) instead of a zeroed placeholder; (3) add the missing `onShouldBroadcastDropPill` callback to `MineChain.swift`'s `droppills()`/`dr()` (Wave 5.5a) and wire an `SRDropPill` send in `HostSession.swift`, mirroring the existing G-2 broadcast pattern one-for-one. Sequenced before Wave 6.5 because 6.5's own `registerserver()` tracker-echo occurrence (D48) is also a UDP receive-path addition — cleaner to land 6.4c's UDP wiring first than to have two waves independently building around an absent UDP listener. |
| **D51** | Wave 6.4c pre-brief open question 1 — whether `HostDgramListener` gets its own file or lives inside `HostSession.swift` | **Own file, `HostDgramListener.swift`, as IMPLEMENTER leaned.** Matches the existing one-listener-per-file convention already established by `HostListener.swift` (TCP accept) — `HostSession.swift` already carries `HostSessionTable` plus per-opcode dispatch, and folding a second listener's accept/drain machinery in would mix two already-distinct concerns (session bookkeeping vs. a network-facing accept loop) that this codebase has consistently kept separate since Wave 6.4a's `UDPSession.swift`/`TCPSession.swift` split. No countervailing reason raised. |
| **D52** | Wave 6.4c pre-brief open question 2 — T-15's unbounded per-remote-4-tuple `NWConnection` growth in the new UDP listener, no C-side equivalent to port | **Bounded this wave, but via connection lifecycle (cancel-and-replace per known player slot), not an arbitrary numeric cap like D44's.** Unlike D44's dead-reckoning bound (a genuinely unbounded quantity with no natural limit, so an arbitrary tick count was the only option), this one already has a natural bound disclosed back in Wave 6.4b's own pre-brief (`8a1ddcd`, §4): "one `NWConnection` per remote endpoint (T-3's port update becomes 'replace the stored connection for that slot')." That design was disclosed, not objected to, and implicitly carried into D47's coding GO — 6.4c is where it actually gets implemented, not a new call. Concretely: (1) `dgramConnection` is tracked per `HostSessionTable` slot (bounded by `maxPlayers`, an existing Wave 5.0 constant), and when T-3's port-mismatch case replaces a slot's stored connection, the superseded `NWConnection` must be explicitly canceled, not merely dropped from the var (an uncanceled Network.framework connection has no reason to tear itself down on its own); (2) a datagram that never resolves to a known player slot (`decodeDgramServerRelay` returns `.malformed`/`.dropped`) must not have its `NWConnection` retained anywhere after that one packet is processed — no separate global registry of "everything that ever connected," only the per-slot table. This keeps total live-tracked UDP connections bounded to at most `maxPlayers` (16) at any time, which is the real invariant worth having, not a magic number. Named regression test required (D28): a slot's old connection is canceled (not merely replaced) when a port-mismatch swap occurs; an unresolvable/malformed datagram's connection is not retained after processing. |
| **D53** | PARITY's Wave 6.4c post-commit audit finding — `handlePlayerDisconnect`'s `.normal` case (`SRPlayerExit`) wrongly excludes the departing player's own connection via `sendToAllExcept`; the C's `sendsrplayerexit()` (`server.c:3387-3409`) is not a single `sendtoallex` call — it does `sendtoone` to the departing player (best-effort, EPIPE-tolerant) *then* `sendtoallex` to everyone else, so the departing player receives their own `SRPlayerExit` too. `.abnormal`/`SRPlayerDisc` is confirmed correct as-is (`sendsrplayerdisc()` really is a plain `sendtoallex`, no self-send) | **Confirmed real, required fix before Wave 6.4c closes — same precedent as D35/D37/D39/D45/D46.** The file's own header comment already states the correct combined behavior; the code beneath it simply doesn't implement what the comment describes yet — a genuine implementation slip, not a considered simplification, and not disclosed as an intentional gap in the completion report. Fix: `handlePlayerDisconnect`'s `.normal` branch must reach the departing player's own still-registered connection (`table.disconnect(player)` runs after this broadcast, so the slot's connection is live at send time) — either switch to `table.sendToAll(bytes)` for that one case (simplest; already includes the departing player, and the C's `sendtoone`/`sendtoallex` split is an artifact of two separate syscalls, not an observable protocol requirement) or replicate the exact two-call shape (best-effort self-send + `sendToAllExcept`). Implementation choice between those two equivalent options is Implementer's call, not a project-wide precedent needing a Planner ruling either way. `.abnormal`/`SRPlayerDisc` must NOT change. Named regression test required (D28): the departing player's own connection receives `SRPlayerExit` on a normal exit, and does not receive `SRPlayerDisc` on an abnormal one. PARITY re-audit required before Wave 6.4c closes (standard fix→test→re-audit sequence, not folded into a future wave). |
| **D54** | Wave 6.5 pre-brief open question 1 — NAT-traversal approach, license-checked per D32/README.md's GPLv3-avoidance commitment | **Approved: `DNSServiceNATPortMappingCreate` (`import dnssd`).** A system API in `libSystem` — nothing bundled, no GPL exposure, satisfying `README.md`'s public commitment directly (not just D13's general MIT posture). Confirmed to support both NAT-PMP and UPnP IGD, the same union `TCMPortMapper` covered, and to self-renew/self-heal across sleep-wake and gateway changes, matching what a repeating callback needs. `Network.framework` itself has no port-mapping facility (`includePeerToPeer` is local-link/AWDL only), so this is a necessary addition, not a redundant one. Wrap the repeating callback as an `AsyncStream` drained by one consumer, same pattern already proven twice per D49/D52 — no new mechanism. |
| **D55** | Wave 6.5 pre-brief open question 2 — split into 6.5a (tracker protocol: codec, `registerserver()`, heartbeat, `listtracker()`) vs. 6.5b (NAT-PMP/UPnP port mapping) | **Approved — same precedent as D23/D43.** Unlike D47's ruling against splitting Wave 6.4b's G-2 (volume, not complexity, against an already-built encoder), this is a genuine two-units-of-work case: 6.5a is fully oracle-testable against `Reference/c/` the way every other wave has been; 6.5b has no C oracle to test against at all (its only real verification is a live router round-trip) and carries the licensing constraint (D54) that 6.5a doesn't. Different verification stories, same grounds D23/D43 already established for splitting. Register and browse stay together inside 6.5a despite being different roles (separate `kRegister*`/`kGetListTracker*` status enums, per the pre-brief) — they share the codec and prologue, and browse is a small fraction of the work, D47's "volume not complexity" case applying here too. No sequencing dependency between 6.5a and 6.5b — either order or concurrent work is fine, unlike D43's 6.4a-before-6.4b risk-reduction sequencing. |
| **D56** | Wave 6.5 pre-brief open question 4 — two newly-found C bugs four lines apart in `TrackerHost` field assignment: (1) `sendtrackerupdate()`'s heartbeat omits the `htonl()` on `timelimit` that `registerserver()`'s registration path applies, corrupting the advertised time limit 60 seconds after any host registers; (2) neither function `bzero`s the `TrackerHost` local before filling it, so the offset-51 pad byte and `strncpy`'s un-zero-padded final byte go out as uninitialized stack bytes | **Confirmed: these get opposite treatment, as the pre-brief flagged.** (1) is deterministic, well-defined C behavior (a real byte-swap omission, not UB) and not a Swift memory-safety concern — same class as D24/D40, replicate bug-for-bug: the heartbeat's `timelimit` field goes out un-byte-swapped, named regression test documents the corruption is intentional and matches the registration path's correctly-swapped value for contrast. (2) is genuinely UB (an indeterminate-value read), the same class D40 itself already carved out as its exception (contrasted there with the `pills[-1]` case) — Swift cannot literally reproduce "uninitialized," so this is a disclosed Swift-safety deviation, not a fidelity fix: zero-fill the `TrackerHost` local (pad byte and any other unset bytes) before filling it, for both the registration and heartbeat call sites. Named regression test required (D28) for both: the heartbeat's `timelimit` is provably un-swapped (matching (1)'s intentional corruption) while its pad byte and unset trailing bytes are provably zero (matching (2)'s safety fix) — same struct, same test, opposite claims, so the coding pass can't collapse them into one rule as the pre-brief warned. |
| **D57** | PARITY's Wave 6.5a post-commit audit finding — `trackerHost()`'s `UInt32(state.timeLimit)` traps on a negative `Int` (`GameState.timeLimit` has no non-negative invariant enforced anywhere, and `RunTick.swift`'s own tick logic already tolerates a negative value the same way the C's `if (server.timelimit > 0)` does), where the C oracle's implicit `int`→`uint32_t` conversion inside `htonl()` never crashes on the same input | **Confirmed real, required fix before Wave 6.5a closes — same precedent as D35/D37/D39/D45/D46/D53.** This is exactly the unchecked-conversion footgun this project already disciplines elsewhere (the `Int16` `truncatingIfNeeded` convention, `CLAUDE.md`) — `UInt32(state.timeLimit)` should be `UInt32(truncatingIfNeeded: state.timeLimit)`, matching the C's actual never-crashes behavior rather than introducing a new Swift-only crash mode. Latent, not currently reachable (no caller in this codebase yet constructs a `GameState` with a negative `timeLimit`) — a robustness gap, not a wire-format fidelity bug; the bytes this function produces for any valid non-negative input are already correct, confirmed independently by PARITY. Named regression test required (D28): construct a `GameState` with a negative `timeLimit` and assert `TrackerHost.timeLimit`'s resulting truncated bit pattern, not a thrown/trapped error. Implementer to plan and execute directly, then report before Wave 6.5a's close; PARITY re-audit required after (standard fix→test→re-audit sequence, same as D53). **Wave 6.5b has no findings and closes independently of this fix** — the two sub-waves gate separately, same as 6.4a/6.4b's precedent. |
| **D58** | Q22 resolution — dedicated headless server binary vs. in-process host only | **Support both, as researched and recommended in `docs/notes/HOSTMODELS.md`.** Jerod's call, confirmed directly: Bolo 2026 hosts in-process via its own "Host a Game" panel AND a separate headless Dedicated Host CLI binary ships alongside it — matching both reference implementations' own precedent (xbolo ships both; WinBolo/LinBolo ship both, dedicated predating client-hosting). No architecture change required — `BoloKit`/`BoloNet`'s existing headless design (server-side logic with no UI dependency, proven across all of Wave 6) already makes either hosting mode cheap; this decision just confirms both get built rather than picking one. Unblocks scoping the dedicated-host binary as a concrete target once the UI/app phase or a Phase-4/5-adjacent wave takes it up. |
| **D59** | Q26 resolution — does this project ship its own tracker daemon binary | **No — rely on manual IP connection for now, not a self-hosted tracker.** Jerod's call, confirmed directly. Wave 6.5a's tracker-protocol client code (`Tracker.swift`/`TrackerRegistration.swift`/`TrackerBrowser.swift`) is NOT wasted or reverted by this — it's a complete, tested, byte-exact client implementation of the wire protocol; it simply has no daemon to talk to yet, and none is being built as part of this project's own scope. Players connect by direct IP for now; Wave 6.5b's NAT-PMP/UPnP port mapping (D54, already shipped) still helps with reachability for that direct-connect path. If a third-party or community tracker daemon speaking this same protocol ever exists, this project's client code would work against it unmodified — this decision is about what this project builds and ships, not about disabling the feature in code. |
| **D60** | Wave 7 sizing check, raised by Jerod ("a big step without clear path to ship") | **Scoped down to a playable vertical slice as Wave 7's v1 cut-line, confirmed directly by Jerod after reviewing the staged alternatives.** Sizing the reference Cocoa UI first (`Reference/c/Mac OS X/`: 4,037-line `GSXBoloController.m`, ~5,700 lines total, no Xcode app target yet in this project) confirmed the original blind GO was too large a unit to review or execute safely — same discipline as D23/D43's "split when it's genuinely multiple units of work," applied here to an entire phase rather than one wave. **v1 = 7.0 (asset pipeline/Phase 2) + 7.1 (Xcode app target) + 7.2 (game rendering) + 7.3 (input/tick loop) — a window that opens, renders a real map from real assets, and lets you drive a tank via the actual physics engine. No menus, no networking UI, no HUD, no prefs.** Three further milestones are identified but explicitly NOT GO'd yet, to be sequenced as separate future waves once 7.0-7.3 land and are PARITY-passed: **Milestone B** (Host/Join panels wired to Wave 6's already-built networking, per D58), **Milestone C** (full HUD/stat views, key remap, alliance/messages panels, sound synthesis, preferences), **Milestone D** (zoom/scroll polish, signing/notarization, and — no later than this point — Q18's git-history rewrite, later found not to be a real operation, see D92). Rejected the full-feature-parity option as this wave's scope outright (too large, same reasoning that motivated splitting in the first place) and the slice-plus-multiplayer-UI middle option in favor of the smaller, faster-to-validate slice alone, per Jerod's own choice. |
| **D61** | Q10 resolution — keep `Reference/c` in-tree permanently, or point docs back to the original source instead? | **Confirmed by Jerod: hold, don't remove yet.** `Reference/c` stays in-tree as an actively-consulted oracle for now — documentation should eventually point back to the original source (https://github.com/bananazon/xbolo, MIT, D1/D13) rather than this project maintaining a permanent copy, but that doc update, and the submodule removal Q10's original text recommended, are both held until the project is done actively referencing `Reference/c` (IMPLEMENTER/PARITY no longer hand-tracing or differential-testing against it) — not tied to the old "Phase 5" language, which Q18 already flagged as stale. Practically this lands no earlier than Milestone D (ship prep), alongside Q18's git-history rewrite of original asset bytes — one 'done with the C reference' cleanup pass rather than two separate ones. README's top-of-file link to the original repo already points the right direction; still open at that future point: (a) actually removing the `Reference/c` submodule (recoverable in git history per Q10's original text) and (b) updating the README Approach section, which currently says Reference/ is "kept as oracle," to instead say it's been removed and link to the original source as canonical. No action now — logged as a Milestone D scoping item. **Correction confirmed 2026-09-05 (D92):** "Q18's git-history rewrite of original asset bytes," referenced above, turned out not to be a real operation — `Reference/c` was always a plain git submodule, never vendored content, and no copyrighted asset bytes exist anywhere in this project's own object history. Removing `Reference/c` at Milestone D is a plain `git submodule deinit`, not a destructive rewrite. (a) above stands as written; the "recoverable in git history" framing was already correct, just for a reason (a trivial submodule pointer removal) smaller than Q18's original text assumed. |
| **D62** | PARITY's ad hoc pre-code Wave 7 audit, F1 (BLOCKER) — `images.h` sized wrong: is it really 297 indices fitting one 256-cell sheet? | **No — confirmed wrong, corrected.** Re-parsed `Reference/c/images.h` directly: 296 `#define ...IMAGE` directives (297 counting the `__IMAGES__` include guard), 290 unique names (6 harmless identical redefinitions). The index space resets to `0x00` twice — `WALL46IMAGE` and `PTKB00IMAGE` — because these are **two independent index spaces for two separate sheets** (`GSBoloView.m:40-41` loads `"Tiles"` and `"Sprites"` as distinct `NSImage`s). Tiles: 177 names, dense `0x00`-`0xb0`. Sprites: 113 names, sparse `0x00`-`0x91`. 177+113=290 > 256, so "one sheet" was arithmetically impossible; the per-sheet "fits 256 cells" conclusion is still true, only the sheet count was wrong. Independently re-verified (define count, both reset points, both `imageNamed:` call sites) before ruling. `CLAUDE.md` and this wave's row below corrected to two sheets, not one. **Attribution correction confirmed 2026-09-04:** this audit was written by the Implementer session, self-identifying as PARITY in error (it read `git config user.name` as a role signal — that identity string is role-agnostic across all four bootstraps, see the 2026-09-04 role-alignment audit note below); corrected in commit `8170a18`. No substantive change — every fact above was independently re-verified by PLANNER before D62-D65 ruled, only the byline was wrong. |
| **D63** | PARITY's audit, F2 — is 7.0's "parse `images.h` → manifest → render" scope still accurate? | **No — narrower than briefed, corrected.** The parse/manifest step is already done: all 290 image constants and both `mapimage()` overloads are already in `Sources/BoloKit/Images.swift` from an earlier wave, independently confirmed against `images.h` with zero missing/extra/mismatched values. `Sources/BoloGlyphs/main.swift` is confirmed still a one-line stub. 7.0 narrows to: consume `BoloKit`'s existing constants as the single source of truth and write the sheet renderer — do not re-parse `images.h` a second time (drift-prone duplicate, not new fidelity). `CLAUDE.md` corrected to say so directly. |
| **D64** | PARITY's audit, F3 — the reference's sheet cell math (`row = idx >> 4`) is bottom-left-origin (`GSBoloView` never sets `isFlipped`, confirmed by grep). Does 7.0's freshly-generated sheet need to match that, and who decides? | **No fidelity obligation (sheets are freshly generated, not copied bytes), but 7.0 and 7.2 must agree on one convention explicitly, not by accident.** Implementer picks either origin convention when writing 7.0's sheet generator, documents the choice in the pre-brief, and 7.2's renderer must consume that same convention — same discipline as leaving 7.2's Canvas-vs-`NSView` choice to Implementer's pre-brief rather than dictating it blind. Also confirmed: `-1` is `GSBoloView`'s "tile unseen, paint nothing" sentinel, never a valid sheet index — both sub-waves must treat it as such. |
| **D65** | PARITY's audit, F4 — 7.2 is scoped to draw "terrain + tanks from a live `GameState`," but `GSBoloView` actually reads three C-client display-side arrays (`client.images[y][x]`, `isMinedTile(client.seentiles,…)`, `client.fog[y][x]`) that `Sources/BoloKit/BMap.swift` itself documents as never modeled in this port. Is fog-of-war/seen-tiles display state in scope for the v1 slice? D60 didn't say. | **Out of scope for v1 — stub as fully visible.** Building a real fog-of-war/seen-tiles display layer is unbudgeted scope this late in a wave already trimmed once for size (D60); it adds a whole new state layer for a single-player slice where every tile is, definitionally, visible to the one player. 7.2 populates its sheet index with a straight per-tile `mapimage()` call, no `seentiles`/`fog` consulted. Real fog-of-war display is tracked as deferred scope for a future wave (alongside Milestone B/C, not yet assigned a specific one) — not a v1 blocker, and D60's v1 definition is amended to say so explicitly. |
| **D66** | Wave 7.0 pre-brief — D64 required Implementer to pick and document one sheet row-0 origin convention. Which one? | **Top-left, ratified.** Implementer's reasoning checks out: PNG and CoreGraphics are both top-down, 7.2 will sample `CGImage` sub-rects, top-left costs zero flips end to end, and D64 already established no fidelity obligation to the C original's implicit bottom-left (`GSBoloView` never sets `isFlipped`) since these are freshly-generated sheets. Binding on both 7.0 (generator) and 7.2 (renderer); `-1` stays a "no image" sentinel, never a valid index, in both. |
| **D67** | Wave 7.0 pre-brief — proposed deviation from `PLAN.md`'s named text: procedural CoreGraphics glyph drawing instead of a vendored OFL font (Noto Sans Symbols/DejaVu), flagged by Implementer as contradicting the committed row rather than assumed. | **Approved.** Nothing in the 290-cell image set is actual text; hinted font glyphs render mushy at 16×16px; dropping the font removes a vendored binary + license file from a wave whose entire point is clean asset provenance — strictly less risk, not more. Implementer keeps a `GlyphSource` seam so a font path could be added later without rework, which is enough of a hedge that this isn't a one-way door. `PLAN.md`'s Wave 7.0 row corrected below. |
| **D68** | Wave 7.0 pre-brief — proposed target split: `BoloGlyphsCore` (library, depends on `BoloKit`, holds the probe/derive/layout/pixel-buffer logic, importable by tests) + `BoloGlyphs` (thin executable: CLI args + PNG encode via ImageIO), vs. the single dependency-free `.executableTarget` `Package.swift` currently has. | **Approved.** Verified directly: `Package.swift` line 18 is currently `.executableTarget(name: "BoloGlyphs")`, no dependencies — Implementer's description is accurate. Executables can't be cleanly imported by `BoloKitTests`, and a standard SPM logic-library + thin-CLI split is exactly the fix; this adds a target, it doesn't contradict any named `PLAN.md` decision about the file layout, which was always descriptive of intent rather than a fixed target list. `CXBolo`'s `-ffp-contract=off` (D26) unaffected. |
| **D69** | Wave 7.0 completion report (`618bedf`) — activate PARITY for the first real post-commit audit of Wave 7? | **Yes, activated.** Independently spot-verified two of the report's claims before activating: `Package.swift`'s target split matches exactly (`BoloGlyphsCore`/`BoloGlyphs`/`BoloKitTests` dependencies as described); the sprite-space gap boundary (0x66-0x6F absent from sprites) cross-checked directly against `Reference/c/images.h` — those numbers belong to tile-space names (`SEAA*`/`FORE*`/`CRAT*`) and sprite-space `SHELL0-5IMAGE` sit at 0x60-0x65 as claimed, consistent with two independent spaces. The 137-variant arithmetic (47+16+10+16+31+8+9) sums correctly to the D62-established 177 tile count. Full behavioral hand-trace (autotiling correctness per family, sprite closed-form math, pixel-buffer drawing correctness) is PARITY's job, not re-derived here. |
| **D70** | PARITY's Wave 7.0 post-commit audit — new finding, not a 7.0 defect: `GlyphSource.swift`'s `drawTank` draws heading 0 pointing screen-**north**, sweeping **clockwise** as heading increases; `Sources/BoloKit/Vector.swift`'s `dir2vec(dir:)` (the port's own established heading convention, used throughout the simulation) has heading 0 = screen-**east**, sweeping **counterclockwise**. Full mismatch (reference direction AND rotational sense both disagree), not just an offset. Which convention should the shipped sheet use before 7.2 wires a real heading to a sprite column? | **Fix the sheet generator to match `dir2vec` exactly (heading 0 = east-pointing tip, increasing heading sweeps counterclockwise) — don't carry a permanent translation layer in 7.2.** The sheet is a regenerable build product (not a committed binary, per the 7.0 completion report) and no fidelity obligation applies (D64) — so there's no cost to fixing the generator itself versus a real, ongoing cost to maintaining a separate angle-conversion function in 7.2 forever, plus the risk of that conversion itself having a sign/offset bug that isn't caught until a tank visibly spins the wrong way. Small follow-up fix for Implementer to land before 7.2 draws its first tank — does not block Wave 7.0's close (PARITY explicitly scoped this as a 7.2 prerequisite, not a 7.0 defect). Required: a named regression test locking in the corrected convention (e.g. heading 0 → tip at screen-east, heading 4 of 16 → tip at screen-north) so this can't silently drift back. PARITY re-checks whenever Implementer lands the fix.  **Filing correction 2026-09-04:** the verification note below was mistakenly appended to D71's row instead of this one in an earlier edit (wrong anchor match) — moved here, not duplicated. **Fix landed and independently verified 2026-09-04 (`dd064dc`):** `GlyphSource.swift`'s `drawTank` now calls `dir2vec` directly (via `Canvas.swift`'s `fillRotatedTriangle(dx:dy:...)`, which takes the vector itself rather than an angle), using the same `kPif/8.0` 16-heading step `PhysicsOps.swift`'s `roundDir` already uses. PLANNER independently re-read `Vector.swift:138-142`, `PhysicsOps.swift:15`, and the new rotation math in `Canvas.swift`/`GlyphSource.swift` line by line before accepting the fix. Test count re-verified independently by direct file-by-file grep: `BoloKitTests` 452 + `DifferentialTests` 156 = **608**, exactly matching the claimed 605→608 (+3). The three new named regression tests (`tankHeadingZeroPointsEast`, `tankHeadingsSweepCounterclockwise`, `allHeadingsMatchDir2Vec`) exist as described and correctly encode the convention. **PARITY PASS confirmed 2026-09-04 (`c4da9e0`)** — independently re-derived every claim above rather than restating the completion report, found zero citation drift. **D70 is fully closed; Wave 7.2 has no outstanding tank-heading prerequisite.** |
| **D71** | Disposition of the uncommitted `docs/PARITY.md` working-tree diff (adds an `[ADMINISTRATIVE SECTION]` and self-modify language letting PARITY edit its own directives) — flagged by PARITY (`79840b1`) as a governance concern: an adversarial auditor able to rewrite its own governing rules undermines the independence the role exists for. Commit as-is, discard, or adopt a narrower PLANNER-gated version? | **Leave uncommitted, as-is, for now — confirmed directly by Jerod.** Jerod's stated intent: the edit lets PARITY keep track of critical decisions, not a blanket self-governance grant. Disposition stays provisional ("for now"), not formally adopted or ruled into `docs/PARITY.md`'s committed text — no D-numbered change to PARITY's actual bootstrap responsibilities is made by this ruling. **Important technical nuance for future sessions:** because this is a local working-tree edit rather than a commit, it does not appear in `git log`/GitHub, but it IS already live to any role reading `docs/PARITY.md` directly from this same Mac's working copy (not a separate clone) — so "uncommitted" here means "not yet in project history," not "inert." Revisit if PARITY actually invokes the self-modify language, or if Jerod wants it formalized later. **Superseded same day:** Jerod asked for `docs/PARITY.md` to be brought in line with PLANNER's narrower proposal 3 (PARITY drafts proposed rule/decision changes in `docs/AGENT_NOTES.md` tagged `[TO: PLANNER]`; PLANNER rules on adopting them) instead of leaving the self-modify grant in place. `docs/PARITY.md` edited accordingly — the self-modify permission language is replaced with that propose-then-adopt convention; the file's other Director-authored changes (heading style, `[ADMINISTRATIVE SECTION]` label, "How to report" section placement) are left as-is, since those were never the concern. |
| **D72** | Wave 7.1 pre-brief — sheet delivery: a Run Script build phase on `BoloApp` invokes the built `BoloGlyphs` executable to regenerate `Tiles.png`/`Sprites.png` into the app target's resources at build time, instead of committing generated PNGs to git. Flagged since `docs/PLAN.md`'s one-line Wave 7.1 row doesn't settle build-process choices either way. | **Approved.** Consistent with D64 (these sheets are a regenerable build product, no fidelity obligation) — build-time generation avoids a second, hand-synced copy of build output sitting in version control, the same reasoning D64 already established. **Required for this to work correctly, not optional polish:** `BoloGlyphs` must be an explicit target dependency of `BoloApp` (not just assumed build order) so the script phase runs after the executable exists, and the script phase should declare its input/output file lists so incremental builds behave — verify both directly once the target is wired, not just that the app happens to build once. |
| **D73** | Wave 7.1 pre-brief — proposed Xcode app target name `BoloApp`, depending on `BoloKit` now and `BoloNet` "once 7.3 needs it." | **Corrected, not approved as proposed — two separate issues.** (1) **Naming:** the project's application name was already ruled at D21 — "name the primary application `Bolo 2026`" — confirmed again in README.md's own text ("the playable Mac app target built on top named Bolo 2026"). `BoloApp` isn't an extension of that, it's a different name; the target/product name must be `Bolo 2026` (a valid Xcode target name — spaces are fine), not invented fresh at this pre-brief. (2) **`BoloNet` timing:** 7.3 does NOT need `BoloNet` — Wave 7.3's own `docs/PLAN.md` row is explicit that v1 is "single-process, no networking wired in," and D60 excludes all networking UI from the v1 slice entirely; Host/Join wiring is Milestone B's concern (not GO'd), not 7.3's. `BoloApp`/`Bolo 2026` should depend on `BoloKit` only for the whole v1 slice (7.1 through 7.3) — revisit `BoloNet` only if/when Milestone B is GO'd, not on a 7.3 timeline. Everything else in the pre-brief (native Cocoa App template via `XcodeNewTarget` — **tool name corrected to `XcodeNewProject` 2026-09-04, see D74**: no `.xcodeproj` existed for a target to be added to, and the substance of this approval was the template/target identity, not the MCP tool that creates it — minimal window, generated-not-copied placeholder icon, v1-sized empty entitlements) is approved as briefed — **coding GO issued on Wave 7.1's pre-brief with these two corrections.** |
| **D36** | Wave 6.4 scope gap — `dgramclient()`'s post-decode application logic (list mutation, sound hooks, vis updates, dead-reckoning) had no assigned sub-wave | **Assigned to Wave 6.4.** Wave 6.0's pre-brief deferred this to "6.1/6.2," but neither landed wave actually claims it — 6.1 shipped as `runclient()`/`runserver()`'s own tick state machines only, 6.2 is TCP `recvsr*` handlers only (a different wire channel from UDP `CLUpdate`). 6.4's pre-brief should split `dgramclient()`'s pure application logic (differentially testable against 6.0's `clupdate_decode_oracle`) from the UDP-receive mechanism, mirroring 6.3's `joinplayerserver()` split. Not folded into 6.2 despite the matching "apply a given value" shape — 6.2 already has a coding GO in flight, and changing its scope mid-wave risks the drift D28/D35 exist to prevent. The dead-reckoning bound (`client.c:1446-1454`) is a `writeRun`-class Swift-side safety deviation, not a fidelity fix — 6.4 should bound it, not port it unbounded or omit it. Also: the tracker reachability echo (zeroed `CLUpdate`, `player == 255`) must be implemented in the **server's** UDP receive path as part of 6.4, even though it's only exercised once 6.5's tracker client exists — see `docs/AGENT_NOTES.md`, 2026-09-03. **Correction confirmed 2026-09-03 (D48):** this text describes only `registerserver()`'s zeroed, `player == 255` echo — `dgramserver()` has its own, separate, verbatim-bytes echo occurrence, which is Wave 6.4b's scope; see D48. |
| **D37** | Wave 6.2 PARITY findings ruling (2 new findings + Q21 confirmed) | **All three fixed before Wave 6.3's coding GO, same precedent as D35.** Finding 1 (real bug): `recvSrSuperBoom` (`RecvSR.swift`) applies local-tank splash damage unconditionally; `recvsrsuperboom()` (`client.c:2737-2851`) nests that entire check inside `player != client.player` — gate `RecvSR.swift`'s damage-check block the same way. `recvSrSmallBoom`'s lack of that gate is correct (its C twin's damage check is a genuine sibling `if`, brace-depth-verified) and must NOT change. Finding 2 (missing UI hook): add `onTankStatusChanged` firing to `recvSrSmallBoom`/`recvSrSuperBoom` when local-tank damage is applied, matching `client.c`'s unconditional `settankstatus()` calls and the existing pattern in `recvSrHitTank`/`recvSrMineAck`. **`MineChain.swift`'s `applySplashDamage` (Wave 5.5a) does NOT get the same fix** — ruled as correctly mirroring `server.c`'s `explosionat()`/`superboomat()`, which have no `settankstatus` analog at all (confirmed by grep), so its omission is a considered match to the authoritative role, not a gap. Q21: confirmed bug, one-line fix — `ShellTick.swift:73`'s `.counter = 0` → `.coolCounter = 0`. Named regression tests required for all three (D28), including the previously-uncovered `recvSrSuperBoom(player: state.localPlayer, ...)` case PARITY flagged as silently untested. IMPLEMENTER to plan and execute directly — see `docs/AGENT_NOTES.md`, 2026-09-03. |
| **D74** | Wave 7.1 completion report (`426c6a4`) — four deviations from the GO'd pre-brief text, each flagged by Implementer rather than smoothed over: (1) `XcodeNewProject` used instead of D73's named `XcodeNewTarget`; (2) `Package.swift` had no `products:` block at all, so nothing outside the package could depend on `BoloKit` — two products added; (3) no `.entitlements` file created, since Xcode 26's Cocoa App template synthesizes entitlements from build settings instead; (4) two `.pbxproj` sections hand-edited, against the MCP tools' "do NOT modify `project.pbxproj` directly" warning. Plus unruled choice 2: project directory location. | **All accepted as delivered — no rework, and D73's text is corrected rather than the work.** (1) **Mechanical, not a scope change.** Same template identifier (`com.apple.dt.unit.cocoaApplication`), same product name, same target shape as approved; `XcodeNewTarget` was simply the wrong tool for a repo whose only workspace is the SPM-synthesized `package.xcworkspace` — you cannot add an app target to a Swift package, and the pre-brief's own "no `.xcodeproj` exists yet" already implied one had to be created. D73's row is corrected above. Flagging it rather than proceeding silently was correct even though the answer is "proceed." (2) **In-scope hard prerequisite, not scope creep.** Exactly two products (`.library(BoloKit)`, `.executable(BoloGlyphs)`) — the library the v1 slice consumes and the executable D72's Run Script phase invokes. `BoloNet` deliberately **not** exported, which is D73's letter and its intent. **Recorded so a future session doesn't "fix" it:** adding a third `.library(BoloNet)` product is Milestone B's mechanical first step; its absence today is a decision, not an omission. (3) **Accepted; do not create the literal file.** The pre-brief asked for a *state* — sandboxed, no network entitlement — not for a file, and that state is verified on the signed product rather than assumed (`codesign -d --entitlements -` → `app-sandbox`, `files.user-selected.read-only`, `get-task-allow`, and no network entitlement of any kind). An empty `.entitlements` file alongside template-synthesized settings would be an inert decoy that later reads as authoritative. **Milestone B dependency recorded here so it isn't rediscovered:** Host/Join wiring flips `ENABLE_OUTGOING_NETWORK_CONNECTIONS` (and `ENABLE_INCOMING_NETWORK_CONNECTIONS` for hosting) to `YES` — build settings, still no new file. (4) **Accepted as the only available path.** The MCP warning is scoped to what those tools can do; there is no tool for a Swift package reference or a shell-script build phase, so the alternative to a hand-edit was not doing D72/D73's approved work at all. The verification standard offered — `plutil -lint` clean plus a structural dump showing exactly one `PBXNativeTarget`, one `PBXShellScriptBuildPhase`, one `PBXTargetDependency`, one `XCLocalSwiftPackageReference`, two `XCSwiftPackageProductDependency`, 21 objects total, no duplicates or orphans — is adequate for this GO; a second opinion on the *encoding* (structure vs. a reference project) is PARITY's, and is explicitly requested. **Project knowledge worth keeping from this sub-wave:** after an on-disk `.pbxproj` edit, `XcodeListSchemes` can return zero schemes and `BuildProject` fail with "No active scheme is selected" purely from stale in-memory workspace state — close and reopen the workspace before concluding the file is corrupt (Implementer validated the file first, then reopened; correct order). See D76, which makes this less fragile. **Unruled choice 2 (project at `Bolo 2026/` off the repo root, local package `relativePath = ".."`) — ratified as-is.** SPM ignores the directory, `swift build`/`swift test` unaffected, an `App/` subdirectory would have been equivalent; moving it now would cost churn for zero benefit. |
| **D75** | Wave 7.1 completion report, unruled choice 3 — `Bolo 2026`'s `MACOSX_DEPLOYMENT_TARGET = 27.0` (Xcode template default) while `Package.swift` declares `.macOS(.v26)`. Compatible as built (app floor higher than the package's), and Implementer deliberately left it rather than align a project-level floor unilaterally. | **Align the app target down to `26.0`, matching D16.** D16 ruled the project's minimum as macOS 26+ (required for FoundationModels, the Apple Intelligence learning goal) — that floor is a project decision, and a template default silently overriding it is exactly the kind of drift this log exists to catch. The practical consequence of leaving 27.0 is not a build error, it's that the shipped product is 27-only while every package target still advertises 26, and nobody discovers the mismatch until someone on 26 can't launch it. Nothing in the v1 slice plausibly needs 27: the target is a SwiftUI `WindowGroup` over `BoloKit`, and `BoloKit` itself compiles at 26. Required: set `MACOSX_DEPLOYMENT_TARGET = 26.0` (via `UpdateTargetBuildSetting`, not a hand-edit — this one *is* a tool-supported change), then a clean build to confirm neither the app nor D72's script phase depended on 27. **If something does turn out to require 27, stop and report — do not revert silently:** that would be a real amendment to D16, which is Jerod's call, not a build-setting detail. Correct call to flag it; the direction just happens to be opposite the template's default. |
| **D77** | PARITY's Wave 7.1 final-state audit (`a03aa07`), F1 — confirmed defect: `Sources/BoloGlyphs/main.swift`'s `writePNG` declares its `CGContext` `premultipliedLast` but is handed straight-alpha buffers from `Canvas.swift`, so CoreGraphics un-premultiplies on PNG export. Measured on the shipped `Tiles.png`: 2,548 pixels at RGB(64,64,64)/a=200 where the source authors RGB(50,50,50)/a=200. Pre-existing from Wave 7.0 (`426c6a4` didn't touch this path), missed by PARITY's own Wave 7.0 audit — not a 7.1 regression. Does it block Wave 7.1's close, and when does it get fixed? | **Does not block Wave 7.1's close — PARITY has no standing to call it a 7.1 defect, and I agree.** But required before Wave 7.2's coding GO, not tracked indefinitely: same precedent as D45/D53/D57 — a real, measured defect that a later audit surfaces in an already-closed wave gets fixed promptly, just gated on the next wave's coding GO rather than reopening the closed one. Directly relevant to 7.2 specifically, which PARITY correctly flagged: 7.2 is the wave that starts drawing these tiles for real, and the defect's blast radius is every partial-alpha glyph, not just today's one pill backing. Fix (Implementer's call which): declare `CGImageAlphaInfo.last` to match the straight-alpha buffers, or premultiply at write time. **Regression test is the load-bearing part, per PARITY's own recommendation** — decode the emitted PNG and compare against the source buffer; the existing suite's blindness to this whole defect class (asserts only on in-memory buffers, never decodes an emitted PNG) is the real gap, not just the one wrong colour. |
| **D78** | PARITY's Wave 7.1 audit, F3 — record correction: D74's text says Milestone B "flips `ENABLE_OUTGOING_NETWORK_CONNECTIONS` (and `ENABLE_INCOMING_NETWORK_CONNECTIONS`) to `YES`," but PARITY confirmed neither key exists anywhere in `.pbxproj` at HEAD — `ENABLE_APP_SANDBOX = YES` is the only sandbox-related key present. | **Correction accepted, wording only, no code implication.** At Milestone B these two keys must be **added**, not flipped from an existing `NO` — D74's phrasing implied the latter. No re-verification needed on anything already shipped: absent-and-defaulting already produces the correct signed-product result (confirmed independently by both Implementer and PARITY), this is purely about not sending a future session hunting for a key that was never there. |
| **D79** | PARITY's Wave 7.1 audit, F4 — `Bolo 2026` target compiles in Swift 5 language mode (both configurations) while `BoloKit` builds under the package's `swift-tools-version: 6.4`/Swift 6 semantics. Inert today (17-line `WindowGroup` + 42-line `ContentView`), but 7.2/7.3 put the draw loop, tick loop, and input handling in exactly this target — where actor-isolation/`Sendable` diagnostics matter most under D41's tick-timing discipline. Fix now or later? | **Fix as part of Wave 7.2's pre-brief/coding GO, not retroactively into 7.1.** Cheap now (59 lines), expensive once a render/tick loop exists to silently misbehave under relaxed concurrency checking — and D41 already established that this project takes tick-timing/concurrency discipline seriously enough to warrant a named decision, not a shrug. Implementer: raise `SWIFT_VERSION`/language mode on the `Bolo 2026` target to match the package (Swift 6 mode) as part of 7.2's work, confirm a clean build, and report before 7.2's coding GO is asked to stand. |
| **D80** | PARITY's proposed `docs/PARITY.md` amendment (drafted via the propose-then-adopt convention, D71) — the "Standing limitation" paragraph asserted "this environment has no Swift toolchain," which the Wave 7.1 audit found false (Swift 6.4, `xcodebuild`, `plutil`, `codesign`, `vtool`, `xmllint` all present and used, and F1 — the PNG premultiply defect — was only found *because* PARITY decoded a built artifact rather than reading source). Adopt? | **Adopted, `docs/PARITY.md` edited accordingly.** Replaces the false blanket claim with an instruction to check tooling at the start of each session and state which checks were execution-verified versus hand-traced, preferring execution where a toolchain exists. Directly evidenced by this same audit's own F1 finding, which a hand-trace-only session would very plausibly have missed — the old text wasn't just inaccurate, it would have actively suppressed a real finding on future hosts where a toolchain *is* present. |
| **D81** | Wave 7.2 pre-brief — rendering mechanism. D60/the Wave 7.2 row both required PLANNER to review the proposed mechanism against D41's tick-timing discipline before the coding GO stands, not to dictate it up front. Implementer built two disposable prototypes directly in the `Bolo 2026` target (SwiftUI `Canvas`+`TimelineView` vs. AppKit `NSView` via `NSViewRepresentable`), benchmarked both live at two viewport sizes against real sheets/`mapimage()`/tank sprites, deleted the scaffolding after (confirmed clean `git status`). Measured: AppKit ~15-20% faster with tighter tail latency at v1's actual window sizes (1280×800, ~4,400 tiles: 6.7ms/7.3ms p95 vs. Canvas's 7.8ms/9.3ms); Canvas wins at near-full-map sizes (3840×2160, ~32,000 tiles) but that's out of v1's scope entirely (no zoom/scroll, D60). Also found and disclosed: `Canvas`+`TimelineView` silently stops compositing when its draw closure's inputs are byte-identical frame-to-frame (confirmed by instrumentation — the closure fired twice at startup then never again on a static scene, no error, no log), which won't bite 7.2's real `GameState`-driven redraws but is a real hazard if a future edit ever hands it unchanging content (e.g. during `server.pause`). Separately (not gating this decision, but load-bearing for either mechanism): `Vector.swift`'s `dir2vec`/`vec2dir` doc comments already establish BoloKit's own `Vec2f`/grid convention as +y-down, matching D66's top-left sheet origin — so **no C-style `255-y` flip is needed anywhere in the renderer**, for either mechanism. Approve? | **Approved: AppKit `NSView` via `NSViewRepresentable`, redraw invoked externally (`setNeedsDisplay`) by 7.3's tick loop rather than SwiftUI-diffed.** Faster and tighter-tailed at every viewport v1 actually needs; the Canvas-wins case is Milestone D's problem (zoom/scroll), not this wave's — recorded there as a recommended future hard cap on rendered-tile-count, not a v1 requirement. More directly analogous to `GSBoloView`'s own externally-invalidated `drawRect:` model, which under D41's discipline is the safer choice specifically *because* of the disclosed Canvas gotcha: an explicit, auditable `setNeedsDisplay` call after each `runTick()` carries no risk of silently stopping compositing the way SwiftUI's implicit change-detection does. `isFlipped = true` maps directly onto D66's already-decided top-left convention with zero translation math. **Coding GO issued for Wave 7.2 on this mechanism.** The no-y-flip finding is accepted as sound (it follows directly from `Vector.swift`'s own documented convention, not a new architectural call) but is exactly the kind of foundational, easy-to-invert claim D70 already caught once for tank headings — PARITY should specifically verify it against `Vector.swift` when it next audits 7.2's actual output, not treated as unfalsifiable just because the reasoning here checks out on paper. |
| **D82** | Wave 7.2 pre-brief open questions — (1) does the `NSViewRepresentable` live in `ContentView.swift` or its own file; (2) does the tick-driven redraw trigger get wired in 7.2 as a placeholder, or deferred entirely to 7.3 (Implementer's stated preference: 7.2 accepts a `GameState` snapshot and redraws on demand, 7.3 owns *when* "on demand" fires). | **Both as Implementer proposed.** (1) **Own file**, same precedent as D51 (one-concern-per-file convention already established by `HostListener.swift`/`HostDgramListener.swift`) — `ContentView.swift` is the app-entry-point concern, rendering is a distinct one. (2) **Defer entirely to 7.3.** Keeps 7.2's responsibility identical in shape to the reference's client/view split (a view that draws whatever state it's handed, with no opinion on when that happens) and mirrors D43's precedent for cutting sub-wave boundaries along real architectural seams rather than convenience. 7.2 should not wire even a placeholder tick. |
| **D83** | Wave 7.2 completion report — scope addition not named in D65/D81/D82's text: `displayTileGrid`/`tileFor` overlay pills and bases onto terrain *before* `mapimage()` autotiles, ported faithfully from `client.c:6106-6141`'s `tilefor()` (pills beat bases beat terrain, first-match-wins). D65's text ("populate the sheet index straight from `mapimage()` per tile") only said what to skip (fog/`seentiles`), not that pills/bases must overlay first — an omission, not a contradiction, but real scope the pre-brief never named. Also flagged: the literal port is `O(256×256×(pillCount+baseCount))` — measured ~122ms/call in debug, 6× a single tick's entire 20ms budget — so `displayTileGrid` precomputes position→`Tile` overlay dictionaries instead of scanning per cell, with an exhaustive equivalence test against the literal `tileFor`. Ratify? | **Ratified — this is what D65 should have said, and the performance rewrite is exactly the discipline this project expects.** A pillbox rendering as bare grass because only terrain fed `mapimage()` would have been a real, visible defect in what 7.2 explicitly promises ("terrain + tanks from a live `GameState`," Wave 7.2's own row text) — this isn't scope creep, it's completing scope the row already implied but the pre-brief's D65 citation didn't spell out. The perf rewrite follows the exact pattern this project already trusts: keep the literal port (`tileFor`) as the fidelity reference, add a fast equivalent, lock them together with an exhaustive test — same shape as every prior perf-vs-fidelity tradeoff in this project. `docs/PLAN.md`'s D65 text is amended by this entry to note the overlay requirement; no change to D65's actual fog/`seentiles` exclusion. |
| **D84** | Wave 7.2 completion report — a real Wave 7.0 defect discovered while building this wave: `GlyphSource.swift`'s `drawConnective` never fills diagonal corners for the five families that don't track diagonal connectivity (`sea`/`river`/`forest`/`crater`/`boat` — only `.wall` has real `diag` bits per `Autotile.swift`'s `needsDiag`), so any run of 2+ adjacent same-family tiles renders with a permanent checkerboard of transparent corner gaps. Invisible to Wave 7.0's own tests (they check "does this glyph have *some* opaque pixel" in isolation; nothing tiled two glyphs adjacently before 7.2 existed to do it). Fixed: corners inferred from adjacent orthogonal bits for the five affected families; `.wall`'s real diagonal tracking is explicitly untouched. Two new tests, one parameterized across all five families, one locking `.wall`'s unchanged behavior. Does this reopen Wave 7.0, and does the fix stand? | **Fix ratified, does not reopen Wave 7.0 — same precedent as D77 exactly.** A found-late-but-real defect in already-closed, already-PARITY-PASSed work gets fixed promptly, gated on the wave that actually depends on the affected output (7.2 is the first wave to ever composite two glyphs edge-to-edge; 7.0's own scope never required it, which is why 7.0's tests couldn't have caught it). `.wall`'s exclusion from the inference is the right call — it has real diagonal data and 47 already-tested variants; inferring over real data would be a regression, not a fix. PARITY re-audit requested, same fix→test→re-audit sequence as D77. |
| **D85** | Multi-agent orchestration workflow — raised after PLANNER demonstrated it can spawn IMPLEMENTER/PARITY directly as subagents via its own tooling, which `docs/PLANNER.md`'s original caveat text hadn't anticipated ("software limitations require Director to trigger the pass between any of our agents"). Should PLANNER auto-trigger the pipeline end-to-end now that it technically can, or keep a human checkpoint between passes? Complicated in practice by Director independently running PARITY/Implementer as separate manual sessions in parallel — PLANNER had to stop a PARITY subagent it spawned because a Director-run one was already in flight on the same audit. | **Jerod's call: keep the human checkpoint, but change its mechanism.** PLANNER asks Director one yes/no question naming the specific pass about to run, before spawning any subagent or handing off any work — no multi-option menu, a plain yes/no. On yes, PLANNER acts directly (spawns the subagent itself, or assigns the work), rather than Director manually starting a session for each pass as before. Director confirmed he will stop spawning role-agents outside PLANNER's own instance going forward, so PLANNER can assume — absent an explicit statement otherwise — that no duplicate/parallel session is already running the same pass. `docs/PLANNER.md`'s caveat text is corrected to match; the "software limitations" framing is superseded, not merely restated — the constraint was a deliberate process choice, not a tooling ceiling, so it's recorded as a decision here rather than a bug fix to the doc. |
| **D86** | PARITY's Wave 7.2 audit (`3dfabff`) — confirmed regression introduced *by* D84's own corner-fill fix, not inherited debt: `GlyphSource.swift:91-94`'s `family == .wall` diagonal-data exemption should also cover `.road`, and doesn't. `Autotile.swift`'s own `deriveRoadConnectivity()` sweeps real diagonal data for road (`mapimage()`'s road branch reads diagonal neighbors via `isRoadLikeTile` to disambiguate several cases, `Images.swift:506` etc.) — the same shape as wall's `needsDiag`, not the "always `diag==0`" shape the five-family inference rule was actually written for. Measured directly against real rendered output: 26 of road's 31 autotile image indices now collapse into just 9 distinct glyphs. Cosmetic under D64 (no fidelity obligation on generated art), not a v1 gameplay blocker — does it gate Wave 7.2's close? | **Required before Wave 7.2 closes — same precedent as D53/D57, not tracked as deferred debt.** This is a fresh regression in code that landed this same wave (D84's fix), not an old defect surfacing late (contrast D77/D84 themselves, which got the "gate on the next dependent wave" treatment specifically because they predated the wave that found them) — there's no reason to let a wave ship with a bug its own immediately-prior fix introduced when the fix is a one-line scope correction (`family == .wall` → `family == .wall \|\| family == .road`) plus a regression test mirroring the existing wall one. Assigned to IMPLEMENTER; PARITY re-audit required after, standard fix→test→re-audit sequence. **Fixed and landed `3e03137`+`e5fccee`** — verified against real rendered output (direct PNG decode of a fresh `swift run BoloGlyphs`, not just the new test in isolation), 626→627 tests (+1, D28-compliant). Also disclosed, not hidden: the fix's own regression test found the audit's illustrative image-81/143↔diag-0/15 pairing was swapped from measured reality (an artifact of `deriveRoadConnectivity()`'s dictionary construction, doesn't affect the pixel-difference assertion either way), and sweeping all 31 road images shows 21 distinct rendered groups (up from the regression's 9), with the remaining 10 same-pixel pairs hand-verified as sharing identical `(ortho,diag)` values — a pre-existing, in-scope characteristic under D64, not a residual defect. **PARITY re-audit PASS, `07974bd`** — independently re-derived from a fresh harness (not checked against Implementer's report): the widened `tracksOwnDiag` exemption confirmed correct with no overreach into other families; the test's image-81/143 anchors match PARITY's own `deriveConnectivity(family: .road)` call exactly; the diag-value swap confirmed real and correctly fixed; the full 31-image sweep reproduces exactly (21 distinct groups, same 10 residual pairs by exact index, each pre-dating this fix). Test count 627 confirmed independently. D86 closed. |
| **D87** | Scope of D85's yes/no gate for Wave 7.3 — Jerod authorized PLANNER to run the entire Wave 7.3 workflow (pre-brief review, coding GO, PARITY activation, any fix→re-audit cycles) on auto mode, without a yes/no checkpoint before each handoff, until Wave 7.3 reaches a clean PARITY PASS. | **Granted, scoped to Wave 7.3 only — not a repeal of D85.** D85's yes/no-then-act gate stays the standing default for every other pass on this project; this is a bounded, explicitly-requested exception for one wave's workflow, not a reversion to the pre-D85 "no checkpoint at all" state. PLANNER still performs every review/ruling step itself (pre-brief review against decisions and non-negotiable rules, PARITY-finding rulings, fix→test→re-audit sequencing) exactly as it always does — the only thing removed is asking Jerod's permission *between* those steps while Wave 7.3 is in flight. The gate resumes automatically the moment Wave 7.3 reaches a clean PARITY PASS; no separate ruling is needed to turn it back on. If a genuinely ambiguous or high-stakes call comes up mid-wave (an architectural fork, a scope question only Jerod can answer, anything resembling the Q-numbered open-questions list) — that still gets raised, same as always; auto mode covers routing handoffs between the three roles, not overriding Jerod's own decision authority. |
| **D88** | Wave 7.3 pre-brief — two real gaps found by tracing the actual C source rather than assuming the wave's shape: **§3**, `keyevent()`'s `LMINE` branch (`client.c:6456-6516`) plants a mine immediately on the key-down edge even while standing still, a different mechanism from the already-ported `TankLocalTick.swift`'s tile-change-only `.lmine` handling — proposing a new `public` BoloKit function `layMineOnKeyDown`. **§4**, `onSpawn` is a documented unwired pass-through (confirmed by grep — `spawn(state:)` is never called from anywhere), so a dead local player never revives, soft-locking the v1 demo on first death — fix must live inside `TankTick.swift`'s `tankMoveTick` itself (calling `spawn(state:)` directly) to avoid a nested `inout`-exclusivity trap, plus a corollary that 7.3's initial `GameState` needs a nonempty `starts` array. Both recommended in-scope for 7.3 rather than deferred/tracked separately. Rule? | **Both in-scope, approved as proposed — same precedent as D45/D77/D84/D86.** Neither is a hypothetical: §4 specifically would falsify this wave's own stated charter ("closes the loop... driven by the actual physics engine") the instant the local player dies once, and §3 would ship a Lay-Mine key that visibly does nothing under the single most obvious use (standing still) — deferring either would ship a wave that's visibly broken in its own core scope rather than one with an inherited gap. Both are small, both sit squarely in 7.3's own input/tick territory (not a reopening of Wave 5/6's design), and §4's chosen fix location (inside `tankMoveTick`, not the app-level callback) is exactly the exclusivity-safe pattern this project already established for Wave 5.9's mine-cascade wiring — reuse of a known-good pattern, not a new one. **Coding GO issued for Wave 7.3** (D82 tick-driver ownership, D41 measured jitter reporting, D79 Swift 6 concurrency discipline, keyboard-capture architecture mirroring `GSBoloView.m` exactly, both §3/§4 fixes, literal default keymap incl. the hardcoded `autoSlowdownBool == true` behavior) — issued directly under D87's auto-mode grant, no separate Director check needed. Flagging for PARITY's eventual audit (not a blocker on coding): confirm §3's new guard logic doesn't duplicate `TankLocalTick.swift`'s existing tile-change guard without a shared helper, and confirm §4's fix genuinely can't trigger the exclusivity trap it's designed to avoid. |
| **D89** | PARITY's Wave 7.3 audit (`4eb483f`) — confirmed real defect, introduced by this wave's own new code: `layMineOnKeyDown` (`TankLocalTick.swift:390-405`, D88 §3) unconditionally decrements `state.local.mines` *before* checking terrain minability, calls `plantMine` after. `keyevent()` (`client.c:6456-6516`) only spends a mine *inside* its terrain switch's 15 matched minable cases — unminable terrain (sea/wall/river/boat/damaged-wall) falls to `default: break` with nothing spent. So Lay-Mine on unminable terrain silently wastes a mine in this port where the C oracle spends nothing — a real resource-accounting divergence, not cosmetic. The new test `layMineOnKeyDownNoopsOnUnminableTerrain` actually asserts the mine *is* consumed, enshrining the bug rather than catching it. The correct pattern already exists three lines away in the same file (`enterTile`'s `.lmine` handling, `TankLocalTick.swift:494-503`, which gates the decrement inside its own terrain switch, matching `keyevent()`). Required before Wave 7.3 closes? | **Required before Wave 7.3 closes — same shape and same precedent as D86 for Wave 7.2.** A real regression this wave's own new code introduced, not inherited debt, discovered by the same audit reviewing that code — no reason to let it ride, especially since this wave closes the entire v1 slice (D60) and the divergence sits on the exact input path that slice exists to demonstrate. Fix direction (Implementer's choice which): give `plantMine` a `Bool` return and only decrement on `true`, or share an `isMinable(terrain:)` predicate between `plantMine` and `enterTile`'s switch, checked before the decrement in `layMineOnKeyDown`. The existing test's assertion must flip to `mines == 5` (unchanged) once fixed — it currently locks in the bug. Assigned to IMPLEMENTER directly under D87's auto-mode grant; PARITY re-audit required after, standard fix→test→re-audit sequence. **Fixed and landed `2a53299`+`28b00ee`** — `plantMine` now returns `Bool` (whether terrain actually changed, `@discardableResult` since `enterTile`'s own call site doesn't need it), `layMineOnKeyDown` only decrements `mines` on a `true` return, matching `keyevent()`'s literal structure exactly. Regression test corrected and expanded from 1 case to `@Test(arguments:)` over all five unminable terrains PARITY's finding named. 639→639 tests (0 removed — existing test corrected+expanded, not new coverage; D28 satisfied). **PARITY re-audit PASS, `a2f1779`** — went beyond the three named check-points, exhaustively swept every `Terrain.allCases` value (not just the 5 sampled) confirming mines are spent iff terrain matches one of `keyevent()`'s 15 minable cases. D89 closed. |
| **D90** | Q27 — bundle identifier prefix for `Bolo 2026`. `com.cosmicceo.Bolo-2026` (taken from the GitHub org name) had stood provisionally since Wave 7.1 pending Jerod's call, since it's a product-identity/distribution decision, not an engineering one. | **Confirmed by Jerod: keep `com.cosmicceo` — aligns with the GitHub org (`github.com/CosmicCEO/BoloKit`).** Same shape as D58/D59: a direct ruling on a raised (not Planner-recommended) question, adopted as final rather than provisional. `com.cosmicceo.Bolo-2026` is now the settled bundle identifier, not a placeholder — no further action needed before Milestone D's signing/notarization work, and no orphaned on-disk container risk since it was never changed from what's already shipped. Q27 removed from the open-questions table. |
| **D91** | D85's standing yes/no gate — PLANNER asking Jerod one yes/no question before spawning any PARITY/Implementer/Admin subagent — vs. this environment's own built-in Auto Mode (session-level bias toward proceeding without stopping for routine confirmations, redirect-if-needed rather than ask-first). Now that Auto Mode is a first-class environment feature rather than something PLANNER had to invent a manual protocol for, is the manual gate still needed? | **Removed by Jerod's direct ruling — this environment's inherent Auto Mode supersedes D85's manual mechanism.** Same shape as D87 but permanent rather than scoped to one wave: PLANNER no longer asks a yes/no question before spawning or handing off a role subagent — it acts directly, using judgment, same as Auto Mode's own standing guidance ("bias toward working without stopping for clarifying questions... they'll redirect you if needed... still fine to stop when genuinely blocked"). **D85's text stands as the historical record of why the gate existed (the real subagent-collision incident that motivated it) — not deleted, but its mechanism is superseded, not merely narrowed like D87 was.** D87 itself becomes moot as a distinct decision (there's no longer a standing gate to grant a scoped exception from) but is left on record unchanged for the same reason. This does not touch Jerod's own decision authority over genuinely ambiguous or high-stakes product/scope calls (architectural forks, anything Q-numbered) — those still get raised, exactly as D87's own carve-out already said; only the *routing* checkpoint between role handoffs is removed. |
| **D92** | Milestone B/C/D pre-plan (2026-09-05) — Q18/D61 assume a real, costly, destructive git-history rewrite is eventually needed to strip Stuart Cheshire's original copyrighted art/sound bytes out of this project's history, growing more expensive with every commit. Is that actually true? | **No — resolved, closing Q18.** Independently verified twice (PLANNER's own read, then a PARITY-lens re-derivation with fresh commands): `Reference/c` is a genuine git submodule (`.gitmodules` → `github.com/bananazon/xbolo.git`, pinned at `51c3cbc1`), and has been since the original scaffold commit (`git rev-list --all -- Reference/c` returns exactly one commit, already a `160000` gitlink). Submodules store only a commit pointer in the superproject, not the linked repo's blob history — confirmed the submodule's object store (`.git/modules/Reference/c`) is fully isolated from the superproject's own commit graph/packs/reflog (no `alternates` file, `git cat-file` against the superproject's own object db fails for the submodule's commit hash). `git log --all --diff-filter=A --name-only` across this project's entire own history, filtered for image/audio extensions, turns up only the 7 `Bolo 2026/.../AppIcon.appiconset/*.png` files — already confirmed generated-not-copied and md5-verified against every `Reference/c` file at D74. **There are no copyrighted asset bytes anywhere in this project's own git history to rewrite.** Removing `Reference/c` at Milestone D (per D61/Q10, whenever the port is done referencing it) is `git submodule deinit -f Reference/c && git rm Reference/c && rm -rf .git/modules/Reference/c` — a clean, non-destructive operation, not a `git filter-repo`-style history rewrite. This meaningfully de-risks Milestone D's sizing (see the new Milestone D wave-table row). D61's text is corrected in place with a dated pointer to this decision, not rewritten. Q18 is removed from the open-questions table — both its concerns (stale phase-order language, and the assumed rewrite cost) are resolved: Phase 2 is done (folded into Wave 7.0), and the rewrite premise doesn't hold. |
| **D93** | Jerod's ruling on the Milestone B/C/D pre-plan (D92) — which milestone to GO first? | **Execute in alphabetical order: Milestone B, then Milestone C, then Milestone D.** Coding GO issued for Milestone B now; C and D remain queued (not yet GO'd) until B closes with a clean PARITY PASS, same sequencing discipline as Wave 7's own sub-waves (7.0→7.1→7.2→7.3, each gated on the previous closing clean). IMPLEMENTER assigned B's first sub-wave (B.0, package/entitlement plumbing) directly under D91 — pre-brief first, same cadence as every prior wave; this ruling authorizes sequencing, not a skip-the-pre-brief shortcut. |
| **D94** | B.2 pre-brief scope-split finding — Milestone B's pre-plan (D92) called B.2 "host panel, local-only path... wired to `HostSession`," assuming that was small UI-wiring like B.0/B.1 since Wave 6's networking was "essentially nothing left to design at the protocol level." Tracing the actual code, IMPLEMENTER found `HostListener`/`HostDgramListener`/`HostSessionTable`/the join-handshake functions/per-message dispatch are all real, tested, but **unwired standalone primitives** — no orchestrator anywhere drains the accept loops, runs the 50Hz tick concurrently with inbound dispatch, and broadcasts `CLUpdate`s back out under real concurrent async I/O. Is the narrow "local-only, zero `HostListener`/`HostSessionTable` calls" reading of B.2 correct, with the real host-network engine split into its own not-yet-named sub-wave? | **Yes — narrow reading confirmed, correcting D92's sizing.** Same shape and same precedent as D22 (Wave 5.5a split from 5.2b) and D60 (Wave 7 split into a v1 slice plus deferred milestones): a genuine, previously-undesigned unit of engineering discovered mid-pre-brief gets its own sub-wave rather than silently ballooning the one it was found in. **B.2 = host settings form + map picker + a real single-process `GameSession`** (reusing Wave 7.3's own machinery — the same mechanism B.1's "Play Demo" button already exercises, just map-seeded instead of hardcoded-demo-seeded), zero `HostListener`/`HostDgramListener`/`HostSessionTable` calls; no other player can join in B.2's shipped state. **New sub-wave `B.5`** (numbered after the existing B.0-B.4 list rather than inserted mid-sequence, since it isn't a hard prerequisite for B.2/B.3) owns the real host-network engine — accept-loop draining, join-handshake wiring, concurrent tick+relay orchestration under real async I/O (a materially harder version of the single-writer exclusivity problem D88 §4 already solved once for the synchronous single-player case). `B.5` needs its own pre-brief and coding GO when its turn comes; not designed here. `docs/PLAN.md`'s Milestone B row corrected to reflect the split. This is exactly the kind of thing D92's pre-plan pass, however thorough, couldn't have caught without reading `HostSession.swift`/`HostListener.swift` line-by-line the way a real pre-brief does — recorded as a sizing lesson, not a process failure. |
| **D95** | B.5 pre-brief — two questions before coding starts. (1) Concurrency architecture for the host engine: `receiveAndDispatchOneHostMessage`'s read-phase must run concurrently across N connections but its apply-phase must never run concurrently with any other mutator of `state: inout GameState`. Two designs: **(a)** merge every event source (new connections, dgram packets, per-connection "message ready" signals, tick fires) into one `AsyncStream`, exactly one `Task` drains it serially; **(b)** wrap `state`/`table` in a new `actor HostGameEngine`, relying on every actor-isolated method staying fully synchronous between its read and write of `state` (true today, a standing invariant to maintain, not structurally enforced). (2) Should B.5 split further — accept/join wiring (low-risk, `JoinAcceptSerializer` already serializes it) vs. the tick+relay+dispatch engine (the genuinely hard, novel concurrency-safety part)? | **(1) Approved: design (a), same footing as D81.** Same reasoning D81 already used to prefer `GameRenderView`'s explicit externally-invoked `setNeedsDisplay` over `Canvas`/`TimelineView`'s implicit change-detection specifically *because* the implicit mechanism could silently misbehave in a way that's expensive to re-verify: actor reentrancy (option (b)) is safe only as long as every actor-isolated method stays fully synchronous between reading and writing `state`, an invariant the type system doesn't enforce and that has to be re-checked by inspection every time this code changes. A single serialized consumer over a merged event stream makes the "only one thing ever touches `state` at a time" guarantee structural and auditable in one place, and it directly reuses a pattern (`JoinAcceptSerializer`, T-11) this codebase already trusts — not a novel mechanism. **(2) Approved: split into B.5a (accept/join wiring — a host can be joined and see players register, no gameplay relay yet) and B.5b (the tick+relay+dispatch engine, actual playable multiplayer, where (1)'s merged-consumer design gets built).** Same precedent as D22/D43/D60/D94: a low-risk half and a genuinely hard, novel-concurrency half are different risk profiles and different audit units, same reasoning applied once already earlier in this very milestone. Exact scope boundary between B.5a/B.5b (e.g. whether B.5a needs any message dispatch beyond the join handshake) is IMPLEMENTER's call at B.5a's own pre-brief, not dictated here. `docs/PLAN.md`'s Milestone B row updated to reflect B.5 → B.5a/B.5b. |
| **D96** | B.5b pre-brief — further split proposed, same shape as D94/D95: tracing what the merged-consumer engine needs to drive found two risk profiles inside "B.5b" — (i) tick timer + dgram relay + the host's own outbound `CLUpdate` assembly, all single-linear-consumer with no concurrency design needed (`HostDgramListener.packets` is one `AsyncStream`, `decodeDgramServerRelay` is pure/synchronous); (ii) TCP `CL*` message dispatch for N concurrent players, which needs `receiveAndDispatchOneHostMessage` split into a concurrent read-phase and a serialized apply-phase — real design risk, reopens Wave 6.6's tested dispatch code. Confirm splitting into B.5b (i only) + new B.5c (ii)? | **Yes — approved, same precedent as D94/D95.** A comparatively simple half and a genuinely hard, novel-risk half are different audit units, not one sub-wave sized for the easier of the two — this milestone's third time catching this same shape of hidden scope, which says more about how load-bearing this discipline is than about the pre-plan's original sizing. **One condition attached before coding GO stands, not yet satisfied by the pre-brief as written:** §2 describes B.5b's engine as running "three `for await` consumers plus the timer... no two ever concurrently, since each is a single linear stream/timer" — true of each source *individually*, but doesn't establish that the accept loop, the dgram-relay loop, and the tick timer are serialized *against each other*. If each runs as its own concurrent `Task` against the same `state`, that's exactly the cross-source race D95 ruled out for B.5's engine generally, just not yet named for this specific set of sources. Before a coding GO stands, IMPLEMENTER must confirm explicitly: are these three sources merged into one `AsyncStream`/unified event enum drained by exactly one `Task` (D95's own architecture, generalized to this sub-wave's three sources), or is there some other mechanism ensuring only one of them ever touches `state` at a time? "No design needed" is true for each source's own internal shape, not yet demonstrated true for the engine as a whole. |
| **D97** | PARITY's B.3 audit — the empirical claim underlying the reference's 8-network-error-code → 3-case collapse only half reproduces. Bad-hostname/non-routable → hangs/times out: confirmed. Closed/never-bound port → instant `ECONNREFUSED`: **not** confirmed across 19 independent trials against the real `joinClient` (always `.timedOut` instead), root-caused to a real mechanism — `NWConnection` enters the retryable `.waiting(ECONNREFUSED)` state and never transitions to `.failed` on its own on this OS/SDK, so only `joinClient`'s own explicit timeout ever fires. The disclosed "standalone-binary-vs-`swift test`" sandboxing split also didn't reproduce (`.timedOut` in both). No functional defect — `.connectionRefused`'s mapping is correct by inspection, and `.timedOut` is a true, reasonable, disclosed user-facing outcome either way. But `JoinClient.swift`'s header comment asserts a specific causal story (a sandboxing difference between process types) PARITY couldn't confirm, and `joinClientMapsConnectionRefusedToNamedError`'s tolerant assertion currently provides no live coverage of the `.connectionRefused` branch at all. Required before B.3 closes, or accept as a disclosed gap? | **Required before close, narrowly scoped — a documentation/test-honesty correction, not a functional redesign, same shape as D86/D89's "real, well-scoped fix" precedent but for accuracy rather than behavior.** No architecture change: `.connectionRefused` stays in `JoinClientError`, correct-by-construction for if/when it's ever reached, matching the reference's own taxonomy. IMPLEMENTER should: (1) re-probe once, cheaply, in case this is genuine environment/OS-point-release flakiness rather than a fixed fact (PARITY's finding doesn't rule that out, but doesn't require an extensive re-investigation either); (2) correct `JoinClient.swift`'s header to describe PARITY's actual root-caused mechanism (`.waiting` never transitioning to `.failed` for a refused port on this OS/SDK) rather than the unconfirmed standalone-vs-`swift-test` sandboxing story; (3) if `.connectionRefused` still can't be made to fire live, say so plainly in the header ("empirically unreachable on this environment as of this date, correct-by-construction for other platforms/OS versions") rather than leaving a specific causal claim standing that a real audit couldn't confirm. Citation drift (pre-brief's "19" vs. the actual 21-entry `kJoin*` enum) gets fixed in the same pass, trivial. |
| **D98** | PARITY's B.5b audit — `client.c:430-434`'s `runclient()` skips its whole body (incl. `seq++`/`seq%5==0`-gated `sendclupdate()`) during pause/time-limit/base-control-reached. `HostGameEngine.tick()` checks none of `state.serverPauseTicks`/`clientPauseDisplaySeconds` before its `localSeq`/broadcast section — would keep broadcasting `CLUpdate` every 5th tick during a pause the C reference goes silent for. Dormant today (nothing sets those fields until B.5c), not gameplay-corrupting (state's genuinely frozen). Required before B.5b closes, or defer to B.5c? | **Required before B.5b closes — small, one-line guard, same "real regression this wave's own code introduced, fix now not later" precedent as D86/D89/D94's own findings.** Deferring to B.5c would ship B.5b with a known-wrong broadcast condition sitting live in the tree, waiting for B.5c to make it observable rather than externally caught later — cheaper to gate now while the fix is a single early-return, same shape PARITY already recommended. IMPLEMENTER adds the guard before the `localSeq` section, matching `runclient()`'s early-return condition. |
| **D99** | PARITY's D98 re-audit — proved a real off-by-one: `RunTick.swift`'s time-limit freeze is two-phase (the tick where `ticks` first reaches `limitTicks` still runs full simulation; freezing starts the *following* tick), but D98's guard used `ticks >= limitTicks`, already true at the end of that last simulated tick — dropping its `CLUpdate` broadcast one tick early. Proved with a boundary-seeded test; neither existing D98 regression test catches it (both start past the threshold). Fix: `>=` → `>`, matching `RunTick.swift:104`. | **Required before B.5b closes — same shape as D86/D89/D94/D98 itself: a real regression this wave's own code introduced, small and well-scoped.** IMPLEMENTER: flip the comparison, add PARITY's boundary-seeded regression test (or an equivalent), re-verify against `RunTick.swift:104` directly rather than re-deriving independently a second time. |
| **D100** | B.5c pre-brief — two questions. (1) D95/D96 specified exactly three fixed producer Tasks (accept, dgram, tick); the TCP `CL*` dispatch loop needs a **dynamically-spawned** producer per successfully-joined connection, feeding the same merged stream. Extension of D96's architecture to "three fixed + N dynamic," approve? (2) Milestone B's earlier ruling folded all 9 unwired `runTick` callbacks into B.5c; deeper tracing found 6 are confirmed no-ops (correct, not gaps) and 1 (`onPlayerDisconnected`) is small and in-scope, but the remaining 3 (`onMineExplosion`/`onSuperboomTerrain`/`onDropPills`) need a signature-changing refactor threading a causer parameter through `TankLocalTick`/`ShellTick`/`BuilderTick` — a real, older, pre-existing gap (disclosed since Wave 5.5a) bigger than "wire 3 more callbacks." Split out, or keep in B.5c? | **(1) Approved — a natural, safe generalization, not a new risk.** Dynamic producers are still I/O-only (read bytes, no `state` access) feeding the same single consumer; producer *count* was never load-bearing to D96's safety property, only "exactly one consumer mutates `state`" was, and that's unaffected by how many I/O sources feed it. Same footing as D95/D96, approved without reservation. **(2) Approved — split out into new sub-wave `B.5d`, not yet pre-briefed.** Correcting the earlier "fold all 9 into B.5c" ruling now that real depth shows why: a signature-changing refactor across three already-shipped, already-tested files (with real D28 stakes) is a different risk class than wiring a callback to an existing `SR*` struct, same distinction D94/D95 already drew between UI-wiring and genuine engineering. Bringing this back rather than silently narrowing scope or unilaterally attempting the refactor was exactly right — this project's standard for a ruling that turns out incomplete once more information exists is to say so and re-rule, not to guess which the ruler would have wanted. **B.5c coding GO'd for items 1-5** (dispatch-loop split, dynamic producer, wiring, `onPlayerDisconnected`, confirming the other 5 as correctly unwired) — item 6 waits for `B.5d`'s own pre-brief. **Correction confirmed 2026-09-05 (D103):** the "causer-threading" framing of item 6's fix was wrong for 2 of the 3 callbacks — see D103. |
| **D101** | B.5c completion report — a real, previously-unreachable bug found in already-closed B.5a-era code: `HostListener.swift`'s `runJoinHandshake` `.accepted` catch branch (send failure after `applyJoin`) calls `table.disconnect(player)` but never undoes `state.players[player].used/connected = true` — a permanent `GameState` slot leak, confirmed deterministically. Unreachable before B.5c's dynamic producer existed to expose it, not a B.5c-introduced regression. IMPLEMENTER proposes `removePlayer(player:state:)` in that catch branch (resets `connected`, preserves `used` for rejoin-eligibility, matching the reference's model) and no disconnect broadcast (`SRPlayerJoin` never went out, so no one's learned the slot exists). Fix it now, and where? | **Required before B.5c closes, same shape as D77's precedent (a pre-existing defect from an earlier closed wave, gated on the wave that made it reachable, not by reopening the old wave's close).** Proposed fix shape approved — `removePlayer`, no broadcast, exactly right given `SRPlayerJoin` hasn't fired yet. IMPLEMENTER may touch `HostListener.swift` despite it being outside B.5c's originally-declared file list; finding and fixing a bug your own work exposed, in the file that actually needs the fix, isn't scope creep. Land as a follow-up commit within B.5c, not a separate sub-wave. |
| **D102** | PARITY's B.5c audit — a real, currently-latent gap found by scratch-test execution, not hand-tracing: `HostGameEngine.stop()` (`HostGameEngine.swift:130-138`) cancels `listener`/`dgramListener`/`consumerTask` but never tears down already-joined players' `NWConnection`s or their dynamic per-player producer `Task`s (`HostGameEngine.swift:154-166`) — proved with a built test (a send on a still-open joined connection succeeds after `stop()`, when it should fail). `stop()` has no production caller yet (Wave 7.1-7.3's app target hasn't wired session start/stop to anything), so nothing user-visible breaks today. Own D-number now, or tracked gap to close before Wave 7.3 wires session lifecycle to the app? | **Tracked gap, not required before B.5c closes — same shape as D98's own base-control-reached deferral (confirmed genuinely necessary, not a shortcut, since nothing exercises the path yet).** B.5c's own scope is TCP dispatch + the join slot-leak fix; `stop()`'s teardown is a different lifecycle surface first exercised for real when Wave 7.3 wires "stop hosting"/quit to a live `HostGameEngine`. Revisit there, not now — IMPLEMENTER's own suggested fix shape (have `stop()` `table.disconnect` every connected slot, letting each blocked `receiveOneHostMessageBytes` throw and its producer exit via the existing `catch`→`break` path) is the right direction when it's picked up. |
| **D103** | B.5d pre-brief — D100's "thread a causer parameter through `TankLocalTick`/`ShellTick`/`BuilderTick`" framing doesn't hold: direct reads of `server.c`'s `explosionat()`/`superboomat()` show `onMineExplosion` needs **no causer at all** (`sendsrsmallboom` always fires `NEUTRAL`, unconditionally, in both detonating branches) and `onSuperboomTerrain`'s causer is already available (`superboomAt` already takes `player: UInt8`, just never calls the closure) — no signature changes needed anywhere. Real gap: `explosionAt`/`superboomAt` (`MineChain.swift`) never call `onMineExplosion`/`onSuperboomTerrain` at all. Raises 3 sub-questions: (1) approve the corrected, smaller fix shape (two missing call sites, exact placement relative to existing `player != localPlayer` gates, cited); (2) `smallboom()`/`superboom()`'s existing client-role notify-hook firing of the same two closures would now double-fire alongside the new server-role broadcast call — IMPLEMENTER recommends separate parameters (client-role notify vs. server-role broadcast) rather than collapsing them, since C's own source has no client-side `sendsr*` call at all, only the server functions; (3) `onDropPills`'s wiring shape as originally scoped would trip the same nested-`inout`-exclusivity violation D88 §4 already fixed once for `onSpawn` — proposes `killBuilder`/`killTank`/`drown` call `dropPills` directly instead and surface the already-wired `onShouldBroadcastDropPill` outward, folded into B.5d or split further? Also flags, not for this ruling: `killSquareBuilder`/`killPointBuilder` (`TankLocalTick.swift:43-78`) are hard-coded to `state.localPlayer`'s own builder even when called from `explosionAt`/`superboomAt` for any tile — a remote player's builder on an exploding tile is never found or killed, a real multiplayer-correctness gap bigger than B.5d. | **(1) Correction confirmed — D100's causer-threading premise is wrong as stated, exactly per this pre-brief's direct `server.c` reads; corrected inline at D100 with a pointer to here.** Same standard as D100 itself already applied to its predecessor: say so and re-rule, don't guess. Fix shape approved as proposed — two call sites, cited placement relative to the existing local-only gates, no signature changes. **(2) Approved — separate parameters, not collapsed.** The C source itself draws this line (client-role `smallboom()`/`superboom()` never broadcast; only server-role `explosionat()`/`superboomat()` do) — this port unifying both roles into one process doesn't erase that the two firings mean genuinely different things, matching the "preserve the invariant, not just the mechanism" precedent (D41). **(3) Approved, fold into B.5d** — this is the correct fix shape for scope D100 already intended to cover (`onDropPills` was one of the original 3 callbacks), not new scope; it's small and mirrors an already-shipped precedent (D88 §4), not a novel risk. **`killSquareBuilder`/`killPointBuilder`'s multiplayer-scoping gap: real, but out of B.5d — split into new sub-wave `B.5e`, not yet pre-briefed** (same shape as D100 splitting B.5d out of B.5c: a genuinely separable, larger unit of engineering found mid-investigation, not something to fold in or silently drop). Unlike D102's `stop()` gap, this one is reachable in live multiplayer today once B.5c's engine is running with real players — track it as a real gap for its own sub-wave, not a deferred-forever note. **B.5d coding GO'd**: items 1+2 (the two call sites) + item 3 (separate parameters) + wiring `HostGameEngine.tick()`'s `onMineExplosion`/`onSuperboomTerrain` to `SRSmallBoom(playerNeutral,...)`/`SRSuperBoom(player,...)` + the `onDropPills` direct-call refactor. |
| **D104** | B.4's disposition — proposed at D92/D94 as "tracker + UPnP wiring, optional/stretch... could fold into B.2/B.3 instead of standing alone," never explicitly folded, deferred, or dropped by any ruling since. Milestone B is otherwise fully closed (B.0-B.3, B.5a-B.5d) except for this and B.5e (not yet pre-briefed) — left open too long with nothing forcing a call. Fold, defer, drop, or stand alone? | **Don't guess the size blind — fold the sizing question into B.5e's own pre-brief, same "have IMPLEMENTER trace before Planner assumes" pattern already used at D94/D96/D100/D103, rather than ruling on scope Planner hasn't read the code for.** `TrackerBrowser`/`PortMapping` are already built and tested per the original proposal's own claim — IMPLEMENTER confirms at B.5e's pre-brief whether wiring them to `HostGameView`/`JoinGameView` is small enough to fold into B.5e's own coding pass, or genuinely deserves standing alone as its own sub-wave. Not deferring indefinitely — this closes the loop by B.5e's pre-brief, not by leaving it open again. |
| **D105** | B.5e pre-brief — two rulings needed. **Part 1:** `killSquareBuilder`/`killPointBuilder`'s scoping gap isn't separable from a real architectural refactor — `killBuilder` reads/writes 4 fields (`builderPill`/`builderTask`/`builderMines`/`builderTrees`) on `GameState.local: LocalPlayerState`, a singleton never made per-player since Wave 5.6/5.7's single-client framing (~50+ call sites, concentrated in `BuilderTick.swift`'s state machine). Bonus finding, same root cause, already-shipped/PARITY-passed, not B.5e-introduced: `builderTick` already runs per-player in a loop but every iteration clobbers the same singleton fields — with 2+ players running builder tasks, each tick stomps the previous player's in-progress state. Three options laid out: (a) full per-player migration (fixes both the kill-gap and the clobbering bug in one pass), (b) a narrower kill-gap-only fix (may not be separable from (a), per IMPLEMENTER's own tracing), (c) split the clobbering bug out from B.5e entirely. **Part 2 (D104):** B.4's tracker/UPnP UI wiring sized at ~60-100 lines across 2 view files, zero `BoloNet`/`BoloKit` gaps — small, but scope-mixing risk against Part 1's real architectural refactor, not an effort-size problem. Rule both. | **Part 1: approve (a), full migration — not (b) or (c).** IMPLEMENTER's own tracing already shows (b) isn't cleanly separable from (a) (`killBuilder` itself needs the fields generalized), and (c) would mean fixing the same 4 singleton fields twice for two "different" findings that share one root cause and one fix — there's no real second unit of work to split out. This isn't scope creep into B.5e; it's recognizing the kill-gap and the clobbering bug are the same bug wearing two names. **Given the size (~50+ sites, real D28 stakes touching already-shipped/tested `BuilderTick.swift` call sites) and that this pre-brief's Part 1 is diagnostic (shows the blast radius) rather than a fix plan (which fields become arrays vs. `PlayerState` members, ordering to keep the build green throughout, test-update strategy) — coding GO does not stand yet.** IMPLEMENTER writes a second, deeper pre-brief specifically for the migration's mechanics before coding starts; this ruling approves the *shape* of the fix, not yet the coding GO itself. **Part 2: split B.4 out into new sub-wave `B.6`, not folded into B.5e.** Same reasoning as D94/D95/D96's own precedent generalized from risk-depth to file/domain separation: a real architectural refactor in `Sources/BoloKit/BuilderTick.swift` and trivial, already-fully-sized UI wiring in `Resources/App/*.swift` are different audit units even though both are "small enough" individually — mixing them muddies B.5e's PARITY audit for no benefit. **B.6 coding GO'd directly** (no separate pre-brief round needed — Part 2's write-up already gives file-level specifics, approach shape, and confirms zero underlying-primitive gaps, which is pre-brief-equivalent detail for work this small and low-risk). |
| **D106** | B.5e's deeper migration-mechanics pre-brief — scope corrected up from D105's "4 fields" to 6 (`mines`/`trees`, the general resource counts, are also touched by `BuilderTick.swift`'s ungated per-player loop, not just the 4 builder-specific sub-counters). ~112 production sites + real test-side impact (6 test files, ~90 combined construction/assertion sites) sized. 7-step build-green ordering proposed: add fields to `PlayerState` → migrate `BuilderTick.swift` → migrate ~13 remaining sites → remove the 6 fields from `LocalPlayerState` (forcing function) → fix test fallout → the actual `killSquareBuilder`/`killPointBuilder` generalization → full negative-controlled suite. Coding GO? | **Approved as proposed, no changes.** The 6-vs-4 correction is exactly the kind of thing a deeper pre-brief exists to catch, and the reasoning (`tankMoveTick`'s own gate vs. `builderTick`'s missing one) is a real structural finding, not a guess. The 7-step ordering is sound: each of steps 1-5 keeps the build green independently, step 4's "remove and let the compiler find stragglers" is exactly the right forcing function for a migration this size, and sequencing the actual `killSquareBuilder`/`killPointBuilder` fix (step 6) after the migration lands (rather than interleaved) avoids operating on partially-migrated state. **B.5e coding GO'd.** |
| **D107** | B.6 completion report's disclosure — `HostGameView.swift` has **zero live networking calls anywhere**, confirmed by direct grep (its own header comment already says so: "No `HostSession`/`HostListener`/`HostDgramListener`/`HostSessionTable` call anywhere in this file"). B.6's two new host-side toggles are correctly built inert-but-disclosed, matching D94's own `portText` precedent — not a defect in B.6. But this means **no UI path exists anywhere in the app that actually starts a real `HostGameEngine`** — all of B.5a-B.5d's tested, PARITY-passed host-network engine work has no caller from the shipped app at all. Own D-number/tracked gap, or silently assume a later wave covers it? | **Real, previously-undiscovered integration gap — tracked, split into new sub-wave `B.7`, not yet pre-briefed, required before Milestone B can be considered functionally complete (though not blocking B.5e/B.6 from closing individually).** B.5a-B.5d's own pre-briefs and PARITY audits were scoped to the engine's internal correctness (accept loop, dispatch, tick/relay, mine-chain broadcast) and never included "wire `HostGameView`'s Start Hosting button to actually construct and run one" — a real gap in how Milestone B's sub-waves were sliced, not a regression in any of them individually. Same standard as every other real-gap finding this milestone: name it and give it a home rather than let it go unnoticed under a "some later wave must cover this" assumption. `B.7` needs its own pre-brief; not designed here. |
| **D110** | Found while live-testing D109's fallback with Jerod: `GameRenderView`'s `intrinsicContentSize` is the full 4096×4096 map inside a plain `ScrollView`, with no camera-centering logic anywhere — a session opens scrolled to the map's `(0,0)` corner, not the player's actual spawn point, with zero on-screen cue that scrolling is needed. Pre-existing from Wave 7.2/7.3, not something B.7/D109 touched. Fix it, and how big? | **Approved as IMPLEMENTER's own framing — small, self-contained: scroll to the local player's spawn point once, on session start.** Same "real, pre-existing gap surfaced by this session's own hands-on testing, fixed promptly rather than left as a footnote" precedent as D101/D102/D107 — the difference here is it's small enough to just GO directly rather than needing its own sub-wave. Coding GO'd. ✅ **Closed 2026-09-06 (`a708583`) — PARITY PASS (`ad7e8f0`), batched with `bbe039d` and D111.** Confirmed genuinely one-shot (fires once per session, view hierarchy fully replaced on navigation) and reads `state.localPlayer`, not a hardcoded index. |
| **D111** | Found live while verifying D110 with Jerod, not requested, disclosed before PARITY sees it: once the camera centered correctly, boat-mode players rendered as the destroyed-tank "X" glyph instead of a boat/tank — traced to `Sources/BoloGlyphsCore/ImageIndex.swift`'s `spriteGlyphRole(for:)` reading the `PTKB`/`PTNK` row pairing as *(dead, alive)* instead of the reference's actual *(boat, tank)* semantics (confirmed directly against `GSBoloView.m:322,325,337`'s unconditional-for-alive-players draw calls). A Wave 7.0 bug, not introduced by B.7 — first surfaced because B.7's local-play fallback was the first real playthrough to reach open water with someone watching. Fix: `destroyed` is now always `false` for the tank sprite range. **Disclosed, not fixed:** boat and tank now render identically (same triangle) — correct per the reference's own placeholder-art semantics for v1, but a real UX gap (can't visually tell boat mode from tank mode). Own ticket, or stay a known simplification? | **Fix approved as landed.** Root-caused against the actual reference draw calls, not this port's own prior (wrong) comment — exactly the standard this project wants. **Boat/tank visual distinction: stays a known, disclosed simplification, no ticket — same footing as D64's "no fidelity obligation on freshly-generated placeholder art."** The underlying simulation already tracks `player.boat` correctly regardless of how it's drawn, so nothing is functionally broken; revisit only if/when a later wave does real sprite-art polish, not as its own tracked gap now. ✅ **Closed 2026-09-06 (`c868bbe`) — PARITY PASS (`ad7e8f0`), batched with `bbe039d`/D110.** PARITY independently re-derived against the full `GSBoloView.m:295-337` draw block (not just the three cited lines) — confirmed all six tank rows drawn only for alive players, chosen strictly by boat-vs-tank mode, no death-conditional branch anywhere. Non-blocking cleanup note: `drawTank`'s wreck-drawing branch is now fully dead code (nothing can pass `destroyed: true` again) — small candidate for whenever `GlyphSource.swift` is next touched, not tracked as its own item. |
| **D112** | Fourth live-found fix (`78df7a8`): a real crash Jerod hit twice under D109's fallback — `ShellTick.swift`'s tank-hit test indexed `state.players[Int(shell.owner)]` to attribute a hit-explosion, but `Shell.owner` can be `playerNeutral` (0xff) for an unowned pillbox's return fire, trapping instantly. Reproduced deterministically (20,000-tick fuzzed drive on the real `docs/U.S.A.map`, crashed in 4 of 5 seeds within 1,500-6,000 ticks — not rare). Root-caused against `client.c:5423` (attributes to `client.players[client.player]`, never the shooter); fixed at all 3 affected sites by threading the already-in-scope `player` through instead of `shell.owner`. The file's own header comment asserting the wrong invariant was corrected in place. One existing test was coincidentally passing under the old bug (its shell owner happened to equal the shooter) — corrected to assert against the actual target. 666 tests, negative-controlled. Assign the number (Implementer's own first guess, "D111," collided with the boat-sprite fix and was self-caught/corrected before landing). | **Approved as landed — real, well-root-caused, promptly fixed and disclosed, same standard as D101/D109-batch.** The header-comment correction alongside the code fix (not leaving a wrong invariant standing for the next reader) is exactly right. Good process discipline catching and correcting the numbering collision before it left the commit history wrong in two places, rather than leaving it for Planner to discover. **The two unconfirmed live reports (turning direction, acceleration-vs-heading) are correctly held open, not part of this fix** — no guessing blind without Jerod's own repro; revisit when he's back. **PARITY activation deliberately deferred, not skipped** — matching Jerod's own "bounded effort overnight" instruction, this doesn't need an expensive audit pass started at this hour; land it in the log now, audit in the next active session. |
| **D113** | B.8 pre-brief — three findings. (1) `joinClient`'s `withNetworkConnection`-scoped handshake tears the connection down on return, and it can't be reopened (the host's `HostSessionTable` slot is tied to that specific accepted connection) — the live loop must reuse the same connection, meaning the handshake's transport needs restructuring. Two designs, no pick made: (a) move the handshake into `TCPSession` itself (IMPLEMENTER's rec — one type owns the connection's lifecycle end to end, avoiding a D102-`stop()`/`shutdown()`-style split hazard), or (b) keep `joinClient`, hand back the live `NWConnection`, give `TCPSession` an adopting initializer. (2) **Not symmetric with B.7** — `runTick` is unified server+client physics; a join client calling it would re-simulate every remote player with no real input and duplicate server-role bookkeeping the host already owns. No client-only tick function exists today. Real shape: a third `GameSession` mode that never calls `runTick`, instead applies `SR*` broadcasts via the already-built `recvSr*` functions and sends outbound `CLUpdate`s via `UDPSession`. Separately, unresolved: does the join player's own tank need local client-side prediction between host updates (matching the reference's model) to avoid feeling laggy/dead? (3) Disconnection surfacing: straightforward, `TCPSession.receiveAndDispatchOne` throwing is already the right signal, D109's `notice:` pattern reusable as-is. IMPLEMENTER recommends a narrower first GO (transport restructuring + basic broadcast-driven play, no prediction) with prediction as an explicit follow-up, rather than one GO for the whole thing — this pre-brief is meaningfully bigger than B.7's own was. Rule the design choice, the split, and issue (or withhold) coding GO. | **(1) Approved — design (a), move the handshake into `TCPSession`.** Same reasoning already applied at D95 (single-owner mechanisms over two types independently managing halves of one lifecycle) — the D102 citation is exactly the right precedent to avoid repeating. **(2) Approved — split. B.8 narrowed to transport restructuring + the third `GameSession` mode, broadcast-driven only, no local prediction; client-side prediction split into new sub-wave `B.9`, not yet pre-briefed.** Same shape as every other split this milestone has made when a pre-brief surfaces a genuinely separable, harder unit of work (D22/D94/D95/D100/D103/D107) — prediction/reconciliation is a real, independent design problem (get it wrong and you reintroduce host-authority drift or visible rubber-banding), not a small addition to bolt onto the transport fix. A join player seeing correct-but-broadcast-cadence movement (matching every other remote player's own update rate) is a real, working intermediate state, not a placeholder to be embarrassed about — ship it, then make it smooth. (3) Approved as proposed — reuse `GameView`'s existing `notice:` parameter, no new mechanism. **B.8 coding GO'd, narrowed as above.** **Correction confirmed 2026-09-06 (D114):** the "no prediction" framing here assumed the host validates/corrects a client's self-reported tank position — it doesn't. See D114. |
| **D114** | IMPLEMENTER held before writing any B.8 code: traced `HostDgramListener.swift`'s `processDgramPacket` and found the host runs **zero** physics/validation on non-host players' tanks — its `.applied` case does `state.players[player].tank = tank` from a client's own self-reported `CLUpdate` unconditionally, then relays those raw bytes to everyone else. This is the real distributed-Bolo model: each client is authoritative for its own tank's physics; the host is only authoritative for shared state (pills/bases/mines/terrain/kills) plus relay. D113's "no prediction" framing assumed the host might correct a client's position, requiring reconcile-against-correction machinery — it doesn't correct anything, so there's no drift to reconcile. Consequence: the join player's own tank still needs `tankLocalTick`/`tankMoveTick` run each tick, scoped to `state.localPlayer`, or input does nothing until B.9 lands (which could be never, since there's no correction to build). Confirm before building a `GameSession` mode that can't move its own tank. | **Correction confirmed — D113's "no prediction" scope was wrong, based on an assumption this tracing disproves.** Running `tankLocalTick`/`tankMoveTick` for the join player's own tank, scoped to `state.localPlayer`, is not deferrable optional polish — it's required for B.8 to function at all (a `GameSession` mode where local input does nothing is not a working intermediate state, it's a dead one). **Folded into B.8's own required scope, effective immediately — D113 amended above with a pointer here, not silently rewritten.** **B.9 re-scoped, not retired:** since the host never corrects anything, there is no reconcile-against-correction problem to build — B.9 becomes remote-player position **smoothing/interpolation** between the host's relay-cadence updates (a real, separate, lower-priority visual-quality item: remote tanks may look jerky at ~10Hz without it, but nothing is functionally broken without it, unlike the local tank's own physics). Same standard as every other ruling that turns out incomplete once more information exists: say so and re-rule, don't guess. Good instinct holding on this specific point rather than either guessing or blocking on the whole pre-brief. |
| **D115** | Found while wiring B.8's join-side receive loop, held before working around it: `UDPSession.receiveAndApply(previousRemoteSeq:previousRemoteLastUpdate:myOwnSeq:state:...)` requires the caller to pass in a specific player's stored `seq`/`lastUpdate` *before* receiving the datagram that reveals which player it's even for (`header.player` only resolves after decode, inside the same call) — meaning no real caller receiving relayed updates for arbitrary players over one shared socket (exactly the join client's situation) can call it as designed. `DgramClientApplyTests.swift`'s existing tests never exercise this because each test constructs its own header and already knows the player. A real, undiscovered gap in an API built ahead of its first real caller (D45's own precedent). Two designs: (a) `UDPSession` owns an internal per-player `[seq]`/`[lastUpdate]` table itself, exposing a receive call with no per-call scalar params — caller just learns which player changed; (b) split receive+decode from apply, so the caller can peek `header.player`, look up its own table, then apply — keeping `UDPSession` table-agnostic. | **Approved — design (a), `UDPSession` owns the per-player table internally.** Same single-owner-mechanism principle this session has applied repeatedly (D95/96's producer-count generalization, D102's `stop()`/`shutdown()` split hazard, D113's TCPSession-owns-the-handshake ruling minutes earlier): the type that owns the one shared socket multiple players' datagrams arrive over is the natural, single owner of the demultiplexing state those datagrams need, not something to push onto every caller to reimplement. Splitting decode from apply (b) would work but leaves the caller (and any future caller) independently rebuilding the same per-player bookkeeping `UDPSession` is already the right place to hold once. Good instinct holding rather than guessing around a real API gap — same standard as D113's transport-ownership question. |
| **D116** | Found designing the local-player tick loop, more load-bearing than D115: `TankLocalTick.swift`'s shared-object branches (`enterTile`/`grabTile`/mine-planting, touching `state.pills`/`state.bases`/terrain) directly mutate shared state — correct for this port's authoritative roles (single-process, host, which have no one else to check with), but wrong for a real join client, whose actual protocol never mutates shared state locally: `sendcl*()` → host's `recvCl*` → host applies + broadcasts `SR*` back. Calling the existing functions as-is would mutate a join client's own local copy without ever telling the host — instant desync. `tankMoveTick` (pure physics, self-authoritative, matching the host's own trust-and-relay model per D114) is fine to run locally as-is. But there is **no outbound `sendCl*`-equivalent path in `BoloNet` at all** — `RecvCL.swift`/`HostSession.swift` are host-side receive-only. This is new outbound protocol surface, not wiring up something already built and tested. Combined with D115's still-open UDP question, B.8 is turning out to be several sub-waves' worth of work discovered mid-flight. Recommend narrowing again: land movement/turning + seeing everyone else via `SR*`/relay as B.8's real scope; split "join client's own shared-object interactions" (touching pills, building, mining) into its own sub-wave. | **Approved — narrow again.** Same standard as D113→D114's own correction and every other split this milestone: a genuinely separable, harder unit of work (here, an entire missing outbound protocol direction) surfacing mid-pre-brief gets its own home, not folded into what looked like a wiring task. **B.8 narrowed to: `tankMoveTick`-only local physics for the join player's own tank (no shared-object interaction), `SR*`-driven visibility of every other player/shared-state change via relay, and the transport work already landed/GO'd (D113/D115).** A join client that can move, turn, see the map and every other player update, but can't yet grab a pill or build, is a real, honest, useful increment — not a placeholder. **New sub-wave `B.10`** (join client outbound `CL*` protocol surface — touching pills, building, mining, and whatever `HostSession.swift`'s existing `recvCl*` dispatch needs on the send side to match) split out, not yet pre-briefed. Not proceeding on B.10 until it gets its own pre-brief — this is real new protocol design, not a small addition. |
| **D117** | Building the third `GameSession` mode per D113-D116, hit a real concurrency race, self-caught from a Swift 6 compiler error before committing anything: `@MainActor` isolation only serializes synchronous code, it does not protect a read-`state`-mutate-write-back pattern spanning a long `await` (real network I/O) — a copy-mutate-write-back workaround compiles clean but reintroduces exactly the hazard `HostGameEngine`'s merged-event-stream/single-consumer architecture (D95/D96) already exists to prevent, invisible to the compiler once `state` is copied to a local var. The real fix mirrors that architecture: split `TCPSession.receiveAndDispatchOne`/`UDPSession.receiveAndApply` into an async "wait for raw bytes, no `state` access" half and a synchronous "decode + apply, no `await`" half, same shape as `HostGameEngine`'s own producers/consumer split — real `BoloNet`-level API surgery on both types, changing shape PARITY has already reviewed once. Not something to decide solo. Approve the split, or another mechanism? | **Approved — split both types the same way, mirroring `HostGameEngine`'s own producer/consumer architecture exactly.** Same single-serialized-consumer discipline this entire milestone has applied every time real concurrent I/O meets shared mutable state (D95/D96 originally, D108's tick-conflict resolution, D115's UDP table) — there is no reason for the join side to invent a different mechanism when the host side's own proven shape transfers directly: an async raw-bytes-only half feeding a merged stream, one synchronous consumer applying every decode+mutation. Good catch from the compiler's own error rather than trusting a workaround that merely stopped complaining — exactly the standard this project wants for concurrency claims. Land the split on `TCPSession`/`UDPSession`, then build the join-side `GameSession` mode as a single consumer over the merged stream, as part of B.8's own required scope (not a further split — this is the correct-concurrency version of work already approved, not new feature surface like B.9/B.10). **Landed 2026-09-06 (`106946c`+`4b309e6`+`058d23f`), pending PARITY.** Built exactly on the approved shape: a proper `AsyncStream` merged event stream, three I/O-only producers (tick timer, TCP raw-message receive, UDP raw-datagram receive), exactly one consumer touching `state` — direct replacement for the earlier self-caught, never-committed attempt. `tankMoveTick`-only local physics + ~10Hz outbound `CLUpdate`, matching `HostGameEngine.tick()`'s own cadence. `JoinGameView`→`NewGameView`→`AppRootView`→`GameView` wired end to end. 671 tests. Two disclosed, not-fixed gaps (both genuinely B.10's territory, not new scope): the local player's own death-timer pill-drop mutates `state.pills` with no way to tell the host yet; shoot/lay-mine input bits are recorded but functionally dead on this path. PARITY confirmed the single-consumer claim by tracing (neither producer touches `state`, `handleJoinEvent` is the sole mutator), the `TCPSession`/`UDPSession` splits as thin behavior-preserving wrappers verified line-by-line, zero `tankLocalTick`/`shellTick`/`builderTick` call sites by grep, and the 10Hz cadence match against `client.c:485-488`/`3509` directly. 671 tests, stable across 3 runs. **Not live-tested against a real second peer** — every component is independently tested at the `BoloKit`/`BoloNet` level, but the assembled `GameSession` mode itself has no test harness (matching this project's own established convention); PARITY's audit confirms structural soundness, not live end-to-end behavior — a real two-instance join is the one thing only Jerod's own hands-on test can confirm. |
| **D118** | Jerod's direct request, after B.8 closed: skip Milestone B's remaining loose ends (B.9 remote-tank smoothing, B.10 join-side `CL*` protocol — both non-blocking, cosmetic/feature-additive rather than correctness gaps) and start Milestone C work now, in parallel across several independent sub-waves, rather than wait for B's full close per D93's alphabetical sequencing. Which sub-waves are actually independent enough to run at once, and how should the parallel work be organized? | **Deliberate Jerod override of D93's sequencing — logged as such, not a process failure, same standing as any other explicit override** (D87's own precedent for this project's override convention). Milestone C's own pre-plan (from the D92 pass) already sizes six sub-waves; three are genuinely independent, bind-only, touch no shared files with each other or with B.9/B.10: **C.0** (HUD/status panel + kick/ban), **C.5** (preferences shell), and **C.3's own Q28 research phase** (asset-sourcing strategy for sound — a research/proposal step, not code, so it can proceed with zero file overlap while the sequencing question about actual C.3 coding stays open until Q28 resolves). **C.1** (key-remap model, touches `BoloKit`'s shipped `InputKeymap` contract) and **C.2**/**C.4** (ship a disclosed fog-of-war gap / need a new `GameState` field) carry more real risk or dependency shape and are not part of this initial parallel batch. **Authorizing three parallel pre-brief tracks now: C.0, C.5, and C.3's Q28 research** — each via an independent fresh session, checking `git status`/`git log` immediately before every commit per this project's own standing concurrency discipline (multiple named peer sessions already coexist safely under this rule). B.9/B.10 remain open, picked up whenever, not abandoned. |
| **D119** | C.0 pre-brief — model side (`PlayerState`/`testAlliance`/`hostKickPlayer`/`hostBanPlayer`) confirmed fully sufficient for the read half. Real gap found: kick/ban's *write* half has no safe entry into `HostGameEngine`'s single-consumer state machine — `hostKickPlayer`/`hostBanPlayer` take `state: inout GameState`, and calling either from a SwiftUI button would race the consumer `Task` the same way B.8's own pre-brief self-caught once already in this file family. Needs `submitKickPlayer`/`submitBanPlayer` added, mirroring the existing `submitLocalInputChange`/`submitLocalLayMineKeyDown` pattern (~15 lines: two methods + one event case). Fold into C.0's own coding GO as a small prerequisite, or split out as its own tiny pre-step? | **Approved — fold in, not a split.** ~15 lines, not a new design axis, same shape as an already-established pattern in the same file — this is exactly the kind of small, clearly-scoped prerequisite D101/D102 already set precedent for landing inside the sub-wave that needs it rather than spawning a separate one. **C.0 coding GO'd**: `submitKickPlayer`/`submitBanPlayer` on `HostGameEngine`, `PlayerStatusView.swift` (player/pill/base status rows, host-only kick/ban), the narrow `GameSession` passthrough over widening `hostEngine`'s visibility (right call — keeps `GameSession` as the one place that knows which path is active), `GameView` top-bar toggle. Check whether `submitLocalInputChange` has existing test coverage to mirror before landing the new submits without any. |
| **D120** | C.5 pre-brief — reference confirmed as one flat `NSUserDefaults` domain (~25 keys), shipped Swift app confirmed to have zero persistence today (every host/join field hardcodes the reference's own shipped defaults as local `@State`, reset every launch). Proposed v1 scope: 4 scalar fields only (player name, tracker hostname, default host port, mute-sound toggle) via `@AppStorage`, one new `PreferencesView.swift` (~60-90 lines) + a SwiftUI `Settings{}` scene, small edits to `HostGameView`/`JoinGameView` to read the same keys instead of literals. Key remap (C.1), HUD toggles (C.0), zoom/domination/message-target fields explicitly excluded. Discretionary call flagged for visibility, not blocking: bundling a mute-sound toggle ahead of C.3's actual landing. | **Approved as proposed, no changes.** `@AppStorage` over a custom persistence model is exactly this project's own established "reuse the native mechanism over inventing one" bias, and 4 scalar fields is a genuinely thin v1 slice matching the sizing. The mute-toggle-ahead-of-C.3 question: **keep it** — it's a real, already-shipped-in-the-reference preference regardless of whether C.3 exists yet, and trivially reversible if C.3 lands with different needs. **C.5 coding GO'd** exactly as proposed. |
| **D121** | Q28 (sound asset-sourcing) research — all 24 reference sound effects confirmed to be short one-shot impact/explosion/mechanical/chime effects (`GSXBoloController.m:357-490,3620-3737`), no ambient/loop/voice content, directly analogous to D67's "nothing here is actual text" classification that made procedural glyph generation viable. `BoloGlyphsCore`/`BoloGlyphs`'s existing target topology (library + thin executable, outside `BoloKit`, build-time generation, no committed binaries) confirmed directly transferable to a `BoloSoundsCore`/`BoloSounds` pair. Three options weighed: procedural synthesis (D67 precedent, zero licensing risk, quality-iteration risk since this project has zero prior audio-synthesis experience), a licensed CC0 sound library (likely better per-effect quality, but a genuinely new supply-chain shape — first vendored/downloaded asset in this project), fresh recording (infeasible for an agent session, ruled out). Recommendation: procedural synthesis for all 24. Open question: if a specific effect (most likely `explosion`/`superboom`) doesn't sound right at Jerod's own review, does the fallback stay procedural iteration, or can that one effect graduate case-by-case to the licensed-library option rather than an all-or-nothing choice? | **Q28 resolved — procedural synthesis, matching D67's precedent exactly, same reasoning: avoid vendoring (binary or license-file) entirely, keep the "generate from checked-in code" discipline this project already runs for art, accept iteration-on-quality as the cost, same trade D67 made and this project later PARITY-passed.** **Fallback question: yes, case-by-case, not all-or-nothing.** If Jerod's ear rejects a specific synthesized effect after real iteration, that one effect may graduate to a licensed source without reopening the whole set's sourcing decision — same "verify, don't assume, and re-rule narrowly when something specific turns out wrong" standard this project has applied all along, just anticipated here rather than discovered after the fact. Pre-brief GO'd for C.3's actual coding-level design (DSP primitives, 14-entry parameter table, `far*`-as-filtered-not-independent, AIFF/CAF encoding) — this research resolved the sourcing question, not the coding pre-brief itself; given this is a brand-new domain for the project (zero prior audio-synthesis art, unlike D67's glyph work which had `CoreGraphics`/pixel-buffer precedent to build on), it earns its own proper pre-brief before a coding GO, same discipline Wave 7.0 itself got. |
| **D122** | C.3's coding pre-brief landed — DSP primitives, 14-entry parameter table, `AVAudioFile`-based AIFF encoding, test strategy, sizing (~420-490 lines, matching D121's estimate). One open naming question flagged, not resolved by the pre-brief itself: the reference has `tankshot`/`pillshot` as separate near names but only one `fshot` far pool — does `fshot` pair with just one of the two, or both? | **Resolved by direct re-check of `GSXBoloController.m`'s own switch (lines 3667-3731), not guessed.** `kFarShotSound`/`farshotsounds` is a single, separate case from both `kTankShotSound`/`tankshotsounds` and `kPillShotSound`/`pillshotsounds` — the reference genuinely plays the identical `fshot.aiff` file for either a far tank shot or a far pill shot; there is no `fpillshot` because the reference never modeled one, not because of a naming coincidence. **Port this exactly: generate one shared `fshot` buffer** (lowpass-filtered from `tankshot`'s near buffer, since the two near sounds are nearly identical "pew" tones per the pre-brief's own parameter table — an arbitrary but inconsequential pick, since the reference itself doesn't distinguish which near sound `fshot` "belongs" to) **and use it for both `tankshot`'s and `pillshot`'s far variant.** Total: 24 names, 23 unique generated buffers (the 24th being `fshot` reused). **C.3 coding GO'd** on the full design as pre-briefed, with this one clarification folded in. |
| **D123** | Three parallel IMPLEMENTER-role sessions (C.0/C.5/C.3) ran fully concurrently in one shared working tree, per D118's authorization. All three independently hit and self-caught the same git index race — a `git add`/`git commit` running against an index another session had rewritten in the gap since the last `git status` check — repeatedly, three-for-three in the same short window. Every incident was self-caught and self-corrected with no data loss (independently re-verified: build clean, full test count exactly accounts for every sub-wave's additions, no duplicate/garbled doc entries), but all three sessions flagged this as luck, not a safety property, and asked for an actual process decision rather than continuing to rely on every future session noticing in time. | **Real process decision, not a footnote: future genuinely-parallel IMPLEMENTER-role sessions use separate git worktrees, not one shared working tree.** Three independent near-misses in one evening is a real signal, not noise — the `EnterWorktree`/`git worktree` mechanism already exists and is the correct tool for exactly this shape of problem (multiple agents needing simultaneous write access to overlapping git metadata, even when their actual file edits don't overlap). PLANNER's own future D118-style authorizations for parallel work should specify worktrees per track, merged back (or PR'd) once each lands, rather than trusting concurrent commits to a shared index to keep self-resolving. This session's three incidents all resolved cleanly, so no remediation needed on the landed work itself — this is a going-forward policy change, logged here so it isn't re-discovered the hard way a fourth time. |
| **D126** | PARITY's post-commit audit of B.9 (`8da3454`+`2aa96c6`+`33dca74`) and D125 sound-wiring (`a840fc3`+`3f100b4`), committed `8242443`. Three items: (1) other players' name labels (`GSBoloView.m:328-330`, unconditionally visible under D65) never drawn, undisclosed as remaining scope despite `8da3454`'s commit message claiming an "exact" match to the reference's draw order/scope; (2) `state.explosions` + each connected player's `.explosions` staging-queue double-draw, previously accepted at 7.2 for the local player alone (a network-transmission-only list per `client.c:3572`/`1427-1428`, never rendered in the reference), now silently widened to every connected player by `8da3454`'s generalization; (3) real bug — `onMineExplosion` wired to play `"mine"` (`kMineSound`), but every BoloKit call site of that hook is a detonation of an already-placed mine, never a successful lay (the reference's only two `kMineSound` call sites, `recvsrplacemine`/`recvsrdropmine`, fire exclusively on a successful lay) — should play `"explosion"` instead. | **(3) Real bug, fix now.** One-line change in `Bolo 2026/Bolo 2026/GameSession.swift`: `onMineExplosion` → `.play("explosion")`. Coding GO'd, no pre-brief needed for a one-line, already-traced fix. **(1) Accepted as a disclosed gap, not worth its own follow-up commit right now.** Cosmetic (multiplayer is playable without player-name labels); fold into B.9's own status text as "known gap" rather than spending a session on it standing alone — revisit if it becomes a real usability complaint in a live playtest. **(2) Prior acceptance still holds at the wider scope.** The staging-queue double-draw was a deliberate, reviewed scope call at 7.2 (extra render frames for an already-transmitted explosion, cosmetically harmless); generalizing it to every player multiplies occurrences but doesn't change its character — not re-litigating, but noting here so a future session doesn't mistake the multiplication for a new defect. **Fix landed 2026-09-06:** `dae5e61` (`onMineExplosion` → `.play("explosion")`), build clean, report `37659ca` — D125 sound-wiring fully closed. |
| **D127** | B.10 pre-brief — smaller than the GO's own framing assumed. Every `sendcl*()` wire shape already has a matching `CL*` Swift struct (`encode()`/`decode()` in `ClientMessages.swift`) and `HostSession.swift`'s `recvCl*` dispatch already fully applies every one host-side (built/tested since B.5c/B.5d) — zero new `BoloNet` surface needed, `TCPSession.send` already exists. Traced `client.c:5785`'s `enter()` directly: it never mutates `client.pills`/`client.bases` locally, only calls `sendclgrabtile()`/etc. and waits for the `SR*` broadcast — meaning `TankLocalTick.swift`'s existing `enterTile`/`grabTile`/`plantMine` are the wrong functions to touch (they're the correct authoritative-mutation path for host/single-process, staying as-is). The real gap: a small new read-only `BoloKit` function mirroring `enter()`'s detect-and-send half (pill/base/mined-terrain/lmine-key only — D116's concretely-named branches), called once per tick from `GameSession.swift`'s join `.tick` handler, sending whatever it returns via the already-existing `tcpSession.send`. Purely additive to B.8's single-consumer design, no concurrency rework. Explicitly out of scope: builder-task and shell-impact `CL*` sends (`builderTick`/`shellTick` families) — the join path doesn't run those ticks at all today, so nothing to intercept; D116's prose said "pills, building, mining" but its concretely-flagged branches were narrower. Approve the narrower scope, or does "building" mean builder-task sends should be in B.10 too? | **Approved exactly as scoped — pill/base/mined-terrain/lmine-key only, builder-task and shell-impact sends explicitly out.** D116's own text is the tie-breaker: its prose was loose, but its *named, concretely-flagged branches* (`enterTile`/`grabTile`/mine-planting) are exactly what's scoped here — "building" in the prose reads as "capturing/building on a tile via tile-entry" (matching the reference's own `enter()`), not builder-task machinery, which is a real, much bigger, separate gap (the join path doesn't even run `builderTick` yet) — tracked as a future follow-on if wanted, not silently folded in or silently dropped. Same standard as every other scope-narrowing this project has done when tracing reveals the concrete branches are smaller than the prose suggested. **B.10 coding GO'd** on the scope above: new `BoloKit` detect-and-send function (`detectJoinTileEntry`-shaped), a few lines in `GameSession.swift`'s join tick handler, no `TCPSession.sendCl*` convenience wrappers needed (call `.send(_:.encode())` directly, matching `CLUpdate`'s own precedent) — skip the symmetry-with-receive-side polish, not load-bearing. |
| **D128** | Jerod's own scope call: ship a **lean v1.0** rather than complete full Milestone C/D first. Path-to-v1.0 plan approved with 5 phases; Jerod then directed skipping Phase 1 (network-bug investigation) and Phase 3 (live playtest) entirely, going straight from Phase 2's cleanup to Phase 4 (signing/hygiene) then Phase 5 (ship). Phase 4 found notarization requires a paid Apple Developer Program enrollment ($99/yr, not set up) — Jerod chose to defer full notarization to 1.1 and ship with real (non-ad-hoc) Development signing only. | **All Jerod's own direct calls, logged as such — deliberate scope decisions, not process shortcuts.** v1.0 ships with: Wave 5-7 core port, Milestone B fully closed (B.0-B.10, real host+join networking), Milestone C partially closed (C.0/C.3/C.5), real code signing, 694 tests. **Explicitly deferred to a 1.1 release**: C.1 (key remap), C.2 (alliance panel), C.4 (messages panel), Milestone D (zoom/scroll polish, `Reference/c` removal, full notarization), B.10's builder-task/shell-impact `CL*` follow-on, the unresolved `NWListener` hosting bug (no further investigation beyond what's already logged), and a confirmed real two-instance host↔join playtest (never completed — the local-only fallback and solo play are the only verified-working paths as of this release). **v1.0.0 shipped** (tag pushed, GitHub release published, non-prerelease). |
| **D129** | PLANNER fanned out 3 parallel PARITY-style audit operatives (client.c/server.c/support-files) to build a complete function-level coverage matrix of `Reference/c` against the Swift port — a new completeness check, not one of PARITY's existing D18/D24/D25/D33/D26/D27/D28 items. Full table: `docs/PARITY_FUNCTION_COVERAGE.md`. `client.c` came back clean (0 unaccounted). `server.c`/support files surfaced 17 unaccounted functions in two corroborating clusters: (1) the entire host-admin command surface — manual pause/resume, allow-join toggle, server lock, unban — has no Swift equivalent anywhere, confirmed missing at both the `bolo.c` wrapper layer and the `server.c` implementation layer; (2) `sendsrflood` (server.c:3261) has no broadcast-to-client callback the way every sibling `sendsr*` op does, so `flood()`/`floodAt()` execute locally but never notify clients over the network; (3) `serverloadmap` (bmap_server.c:21) has NEUTRAL-owner-forcing/mine-clearing-at-load logic distinct from the already-ported `clientloadmap`/`decodeBMap`, with no traced counterpart. | **Filed as new 1.1-backlog items, not actioned yet.** (1) and (2) are real missing features/bugs, distinct from the already-logged D128 backlog (C.1/C.2/C.4/Milestone D/B.10 follow-on/network-bug/playtest) — add to that list rather than replacing it. (3) is a correctness gap worth investigating before any dedicated-host-focused 1.1 work, since a host started via `serverloadmap`'s real path could diverge from what's been tested via `decodeBMap`. No coding GO issued yet — this is a findings log, IMPLEMENTER pre-briefs for these come later at PLANNER's normal discretion. Also: `swift-port-director` (personal skill, `~/.claude/skills/swift-port-director/SKILL.md`) updated to require this kind of function-inventory table during pre-planning on *future* port projects, so it's built proactively rather than reconstructed after the fact like this one was. |
| **D133** | D131's completion report (`3bae6ab`): both literally-named items (`sendsrflood`, `serverloadmap`'s port) were already closed by prior D129 commits (`34f7560`, `9c55d9c`) before D131 was even written — stale backlog bookkeeping. The real gap found instead: `9c55d9c`'s own doc comment wrongly claimed no Swift call site decodes a host's raw map bytes; `HostGameView.swift`'s `handleMapPickerResult` (landed `ea089d9`, four days earlier) is exactly that call site and was skipping `serverPostProcessLoadedMap` entirely — every real map loaded through the host UI silently skipped NEUTRAL-owner-forcing/pill-speed-rescale/start-clear/mined-terrain normalization. Fixed with one call plus a doc-comment correction. IMPLEMENTER flagged two questions: (1) does this get its own decision number since it's not literally what D131 named; (2) is the `Bolo 2026` app target's total lack of a test harness worth its own backlog item. | **(1) Yes — this entry is that number, D133. D131 closes clean as "investigated, found already-done, real adjacent gap found and fixed instead" — a legitimate and valuable outcome, not a failure to execute the GO as written.** Same standard as this project's other "traced deeper than the GO's own framing assumed" entries (D113, D127) — investigate-first discipline catching a stale premise is exactly the value of requiring a pre-brief before coding. Good catch that the fix belonged squarely within D131's actual intent (host-side map load correctness) even though not its literal text. **Process note for future backlog GOs, adopted:** cross-check `git log --oneline` against a backlog item's own commit citations before re-issuing a GO — D129's own findings had already been landed one day after being filed, and neither this session's dispatch nor D131's own text caught that. **(2) Real gap, filed as new backlog: the `Bolo 2026` app target has zero automated test coverage — every UI-layer change (this fix, C.4's messages panel, MessagesView, HostGameView itself) is verified by build-success + code review only.** Not urgent enough for its own immediate coding GO (nothing here is broken, and standing up a UI test target is a real, separate unit of work), but tracked as 1.1/1.2 backlog alongside B.10's follow-on and Milestone D. PARITY: confirm the wiring fix and comment correction when picked up, per IMPLEMENTER's own handoff — low-risk, no new logic to re-derive. |
| **D132** | Jerod's own new ask: ship a small default map bundled with the app, so hosting works without requiring the user to import an external `.map` file first — currently `HostGameView.startHosting()`'s "Start Hosting" button is disabled until `mapState != nil`, reachable only via `fileImporter`, and no map is bundled (confirmed: no `.map`/`.bmap` resource anywhere under `Bolo 2026/`). This is new v1.1 scope, not part of D129's backlog. | **Approved, coding GO'd.** A small, simple, freshly-authored default map (not a copy or derivative of `docs/U.S.A.map`, which is Jerod's own untracked Director artifact — never touch it per standing project memory) — a handful of starts, a couple of pills/bases, mostly open terrain, sized to prove hosting works out of the box, not a showcase map. Ship it as a bundled `BMap`-encoded resource (same on-disk format `decodeBMap`/`encodeBMap` already round-trip, so it exercises the exact same load path as a user-imported map — no special-cased in-memory `GameState` construction that would bypass `serverPostProcessLoadedMap`'s D131 logic) loaded automatically as `HostGameView`'s initial `mapState`, with the existing "Choose a Map" `fileImporter` path left intact for overriding it. IMPLEMENTER: pre-brief first — confirm whether `BMap.swift` already exposes an `encodeBMap` (found so far: only `decodeBMap` confirmed present; verify before assuming symmetry), decide how the map's raw terrain/starts/pills/bases data is authored (a small Swift literal fed through `encodeBMap`, most likely, not hand-authored bytes), and how it's bundled/loaded (Xcode resource + `Bundle.module`/`Bundle.main`, matching `BoloGlyphs`'/`BoloSounds`' existing build-time-generated-resource precedent, D72/D124, or a simpler embedded-at-build-time approach if simpler suffices for one small file) — propose before coding. |
| **D131** | Jerod's own call, asked directly which D129 backlog item to sequence next: **1.1's `sendsrflood` broadcast gap and `serverloadmap` divergence, over the B.10 `CL*` follow-on or the real playtest.** Both are small, well-scoped correctness gaps from D129's own function-coverage audit, with no open design ambiguity — no split/design ruling needed before a coding GO. | **Coding GO'd.** Scope, per D129's own findings: (1) `sendsrflood` (`server.c:3261`) needs the missing broadcast-to-client callback every sibling `sendsr*` op already has, so `flood()`/`floodAt()` notify clients over the network instead of only mutating local state — mirror the existing `onShouldBroadcastDropPill`/`SRDropPill` pattern (D50/Wave 6.4c) one-for-one. (2) `serverloadmap` (`bmap_server.c:21`) needs its NEUTRAL-owner-forcing/mine-clearing-at-load logic ported as a distinct function from `clientloadmap`/`decodeBMap` — do not fold the two together or assume they should converge; trace `bmap_server.c:21`'s actual body directly before writing anything, same as every other port in this project. IMPLEMENTER: pre-brief first (read both C bodies, confirm call sites, propose the Swift shape), same two-stage GO pattern as always — this entry is the pre-brief GO, not a blank check to code immediately. B.10's `CL*` follow-on, Milestone D, further network-bug investigation, and the real two-instance playtest remain queued, not GO'd, per D128/D130's own backlog list. |
| **D130** | Ruling on two `[TO: PLANNER]` items IMPLEMENTER left open from the D129 backlog, plus close-out of the work that landed while unreviewed by PLANNER/PARITY across `b71d011`/`0b2492d`/`6c74efd`/`d8b0512` (C.1 key remap, C.2 alliance panel, host-admin command surface, C.4 messages panel). (1) Host-admin surface's own question: "confirm the `lockserver`/`unlockserver` = N/A ruling stands." Reviewed `c2d31b2`'s diff directly: both are a bare `pthread_mutex_lock`/`unlock` pair (`server.h:374`) bracketing every admin function purely for C-thread serialization of the global `server` struct — `HostGameEngine`'s single-consumer actor loop already guarantees this structurally, same skip class as `increasevis`/`refresh`/`printmessage`. "Locked host rejects joins" and "allowJoin=false" already collapse into one tested feature (`EvaluateJoinRequestTests.rejectsWhenJoinNotAllowed`). (2) C.4's handoff: "ready for PARITY review whenever picked up," flagging `computeMessageMask`'s `mag2f`-not-squared self-catch as the suggested starting point. Reviewed `be1b269`'s diff directly: `computeMessageMask` correctly copies `sendmessage()`'s mask switch verbatim including the un-squared `8.5` `MSGNEARBY` threshold (confirmed against `Vector.swift:107`, `mag2f` calls `sqrt`); `MessageTarget` correctly excludes `MSGGAME` (server-only synthetic target, never player-selectable, matches `client.c:6718-6742`'s own switch never handling it). Also verified ground-truth test count via fresh `swift test` run (D28 discipline) since the two completion reports' before/after deltas didn't reconcile against each other. | **(1) Ruling confirmed exactly as IMPLEMENTER proposed — `lockserver`/`unlockserver` = N/A, no Swift port needed, closed.** No further action. **(2) PARITY finding: no defects found on independent review — C.4 passes.** The `mag2f` fix is correct and correctly tested; `MessageTarget`'s `MSGGAME` exclusion is correct. C.1 (`b71d011`) and C.2 (`0b2492d`) reviewed at the commit-log/AGENT_NOTES level only (no fresh line-by-line audit performed this pass, since neither carries an open question) — both are treated as provisionally closed on the strength of their own completion reports until a future PARITY pass has bandwidth to do a full independent audit; not blocking. **Ground-truth test count: 738 total (537 BoloKitTests + 201 DifferentialTests), fresh `swift test` run, fully green** — supersedes both the 700→717 and 694→702 deltas cited in the two individual reports (those were counted from different baselines mid-stream, not conflicting bugs). C.1's flagged builder-tool digit-key (1-5) exclusion **confirmed correct as separate future scope, not folded into C.1** — no click-to-build/digit-key path exists yet for it to remap. Process note: recent 1.1 work landed via `Merge:` commits distinct from the direct-to-`main` convention used through v1.0 — acceptable going forward, no ruling needed to standardize it either way. 20 local commits pushed to `origin/main` following this ruling. |
| **D124** | C.3's two open items from its completion report: (1) `AVAudioFile` writes File type ID `AIFC`, not true `AIFF` (reference files are genuine `AIFF`, confirmed via `afinfo`) — same uncompressed PCM payload, plays identically in `NSSound`/`AVAudioPlayer`, a container-label difference only. Acceptable as-is, or worth a lower-level writer to force true `AIFF`? (2) Xcode Run Script wiring for `BoloSounds` (mirroring `BoloGlyphs`'s existing D72 pattern) was deliberately deferred to avoid touching the `.xcodeproj` while C.0/C.5 were mid-edit in the same tree — both have since landed clean. Wire it now? | **(1) Acceptable as-is, not worth chasing.** These files are generated at build time and never committed or distributed (same D72 discipline as the glyph sheets) — the internal chunk-format label has no bearing on port fidelity to the C reference (which is about gameplay/protocol behavior, not asset-file container metadata) and doesn't affect playback correctness. Not a defect, no follow-up needed. **(2) Yes — wire it now**, same shape as `BoloGlyphs`'s existing Run Script phase (D72): `BoloSounds` as an explicit target dependency/input, the 24 `.aiff` paths as generated outputs under `$(BUILT_PRODUCTS_DIR)/$(UNLOCALIZED_RESOURCES_FOLDER_PATH)`, invoked with the resources dir as `argv[1]`. The concurrency risk that motivated deferring it is gone (C.0/C.5 both closed); no reason to leave the app target unable to actually play the sounds C.3 just built. **Correction confirmed 2026-09-06 (PARITY's C.3 audit):** the "24 entries, 23 unique underlying buffers" figure in D122/C.3's completion report is a bookkeeping imprecision, not a functional defect — `buildSounds()` has exactly 24 keys (no 24th "far pillshot" key ever existed to share `fshot`'s value with), so all 24 stored buffers are naturally distinct; `fshot`'s role is simply "the only far-shot entry, serving both near shots conceptually," not "one buffer stored under two keys." Confirmed via `md5` across all 24 generated `.aiff` files (24 distinct hashes) and direct code reading. |
| **D109** | Jerod's own hands-on test surfaced a real environment bug: `NWListener` fails with `POSIXErrorCode 22` (EINVAL) on every port, reproduced independently by both PLANNER and IMPLEMENTER outside the app entirely (a bare standalone binary, no entitlements, no sandbox) — raw BSD `socket()`/`bind()`/`listen()` on the same ports succeeds fine. Root-caused to the machine's current macOS 27 "Golden Gate" Public Beta (confirmed fully up to date on that track, no OS update available) — a `Network.framework`/`NWListener`-specific regression, not a `BoloNet` defect, not fixable by entitlements (already correct, confirmed present: 5 entitlements including `network.server`). Consequence, confirmed by re-reading the app's own navigation: the B.1-era "Play Demo" no-network scaffolding was already removed once "Host" became the real way to start any game (per an earlier ruling) — so B.7 made every "Start Hosting" click require a working listener bind, even for solo play with no other players. On this machine's current OS state, **no gameplay is reachable at all** until either Apple fixes the beta regression or the app tolerates a bind failure. Jerod's ask, scope-clarified: add a fallback so a bind failure starts a purely local/offline game instead of failing outright — explicitly solo-only for now, not real hot-seat/shared-keyboard multiplayer (a separate, much bigger, unscoped feature, deferred). | **Approved, small and reuses an existing path — no new mechanism needed.** `AppScreen` already has a `.playing(GameState)` case (the exact local-simulation path `JoinGameView`'s post-handshake flow and the old B.2 local-only design both already use) alongside `.hosting(HostGameEngine)`. On `HostListener`/`HostDgramListener` construction failure, `HostGameView.startHosting()` falls back to feeding the assembled local `GameState` through `.playing(state)` instead of failing — with a disclosed, visible notice (not a silent fallback) that hosting couldn't start and the game is running local-only. This is a genuine resilience improvement independent of the beta bug (a host wanting solo-only play shouldn't need a working socket at all), not merely a workaround — same footing as D94's original "local-only path" intent that B.7 narrowed away from without meaning to. **Explicitly out of scope, per Jerod's own clarification:** real multi-human-one-computer play (shared keyboard/screen, split input) — flagged as a distinct, unscoped future item, not bundled here. Coding GO'd. ✅ **Closed 2026-09-06 (`f76191f`+`afd3f8c`) — PARITY PASS (`f48314d`).** One disclosed deviation from the ruling's literal wording, approved: instead of reusing `.playing(GameState)` bit-for-bit, a new `AppScreen.hostingFallback(GameState)` case carries a display-only `notice: String?` (default `nil`) so the fallback's UI text doesn't leak into the join path's own `.playing` usage — same underlying `GameView`/`GameSession` local mechanism either way, not a third simulation path — PARITY confirmed the two `AppScreen` cases resolve to the exact same `GameView` initializer, and that the notice's SwiftUI wiring is unconditional (only the `Text` itself is optional). 665 tests confirmed, plus a real Xcode build. One honest caveat left open by PARITY: no `#Preview` exercises the `notice` parameter, so this was confirmed by code-reading, not a rendered screenshot of the banner. The flaky test (one isolated, previously-known toolchain-timing pattern, unrelated) reproduced clean on 5/5 reruns. IMPLEMENTER's earlier entitlements detour confirmed to have never been the fix — network capability was already correctly granted via existing build settings before that detour started. |
| **D108** | B.7 pre-brief — three findings. (1) `HostGameEngine`'s contract confirmed self-driving once `start()` runs (own timer, own producer/consumer `Task`s); wiring `HostGameView.startHosting()` means constructing the two listeners + the engine, calling `start()`, then feeding `engine.state` to `GameView`/`GameSession`'s existing render path — but `GameSession`'s own tick timer would then double-drive the same `GameState` alongside the engine's own, a real conflict to resolve. (2) D102 (the `stop()` teardown gap) is confirmed real, not latent, the moment B.7 gives `stop()` a live caller — IMPLEMENTER recommends folding the fix into B.7 itself (direct consequence of a real caller existing, not separable) rather than a further split. (3) A symmetric, equally real gap on the **join** side: `GameSession`/`JoinGameView` have zero live network loop post-handshake (`GameSession.swift`'s own D73 header says so verbatim) — a second real player joining a real host gets the initial map correctly, then runs a fully disconnected local sandbox forever after, seeing none of the host's real state. Not proposed for folding into B.7. Rule on the tick-conflict resolution, D102's fold-in, and the join-side gap's disposition. | **(1) Approved — on the host path, `GameSession`'s own local tick timer/`runTick` call is bypassed entirely in favor of rendering live off the running `HostGameEngine`'s `state`; `GameSession`'s existing timer-driven path stays exactly as-is for the single-process/join-side-sandbox cases, this is host-specific.** Two independent tickers advancing one `GameState` is a real correctness hazard (same shape as every other "exactly one mutator" concern this milestone), not a style choice — same standard as D95/D96's own single-consumer discipline, applied here to timers instead of `Task`s. **(2) Approved — D102's fix folds into B.7's own coding scope**, same shape as D101 (a pre-existing gap becoming reachable/real the moment the wave that exposes it lands, fixed in that wave rather than split out again). **(3) Real, but out of B.7's own framing — split into new sub-wave `B.8`, not yet pre-briefed.** Same shape as D107 itself: `B.7` fixes the host side's missing caller, `B.8` is its exact mirror on the join side (a live receive loop + `GameSession` becoming network-aware there too) — both genuinely required before Milestone B delivers what D58 actually promised (two real players able to play together), neither blocks the other from landing independently. **B.7 coding GO'd**: the engine wiring, the host-path tick-conflict resolution, and D102's `stop()` fix, all as one coding pass. | 
| **D76** | Wave 7.1 completion report, unruled choice 4 — the `Bolo 2026` scheme is Xcode-autocreated rather than a shared `.xcscheme` committed to the repo. Implementer noted a checked-in scheme would be more reproducible for CI or for auditing on another machine, but declined to add unrequested files. | **Add the shared scheme. Land it together with D75's change, before PARITY audits 7.1.** The reproducibility argument isn't hypothetical here — this very sub-wave produced a "zero schemes / no active scheme is selected" episode after a `.pbxproj` hand-edit (D74), and an autocreated scheme is per-user derived state that every future clone, audit, or CI run has to re-materialize by luck of Xcode's autocreation rather than from anything in version control. One committed file (`Bolo 2026.xcodeproj/xcshareddata/xcschemes/Bolo 2026.xcscheme`) makes "does this build clean from a fresh clone" an answerable question. This is a deliberate override of Implementer's otherwise-correct "no unrequested files" discipline — the file is requested now, so it is no longer unrequested. Bundled with D75 specifically so Wave 7.1 is audited **once, in its final state**, rather than audited and then re-audited over two small mechanical changes. |

---

## Context

Bolo (Stuart Cheshire, 1987/1993) is a top-down networked tank game with an RTS layer:
tile-based island map, capturable refuelling bases, hostile pillbox turrets, and a Little Green
Man engineer who harvests trees to build roads, walls, boats and mines. The original is a
68k/PPC Mac app and cannot run on modern macOS.

Note that Bolo is no longer unmaintained: [WinBolo 2](https://store.steampowered.com/app/4672140/WinBolo/)
shipped June 2026 with native Mac support and modern netcode. This project is therefore
explicitly a **learning vehicle** — Xcode, Swift, Apple Intelligence, and networking
infrastructure — rather than the only way to get a playable Bolo. That framing is what makes a
full rewrite a reasonable choice instead of a wasteful one.

**Reference implementation:** [bananazon/xbolo](https://github.com/bananazon/xbolo) — MIT,
native Cocoa, ~500 KB of C + Objective-C, builds on Apple Silicon.

## What the port must preserve, and what it may fix

The inherited code has a structural flaw worth fixing: `client.c` (201 KB) and `server.c`
(112 KB) each contain their own copy of the simulation. The port unifies them into one
`BoloCore` module consumed by both roles. This is the strongest argument for rewriting rather
than forking.

**Discipline: port and fix are separate activities.** Porting changes language; fixing changes
behaviour. Doing both at once produces bugs nobody can attribute. So the port is
*behaviour-preserving* — bug-for-bug — and fidelity fixes come afterwards, in Phase 5.

## The spine: differential testing with C as the oracle

We have no replay logs (D9), so the C code is the only executable specification of Bolo
behaviour we possess. Discarding it before the port is verified would be throwing away the
only thing to check against.

So: **keep the C code compiling in the same target throughout the port.** Swift imports C
directly, but in SPM this requires defining a dedicated C target (e.g. `CXBolo`) or setting up
proper bridging headers/module maps. Note that for each module we want to call the C function 
and the Swift function in one process, on identical inputs, and assert identical results.

**Critical Test Implementation Notes:**
1. **Isolate POSIX Coupling:** Many reference files (like `client.c`, `server.c`) intertwine pure simulation state machine logic with socket initialization (`socket()`), select loops (`select()`), and Unix pipes. To prevent differential unit tests from spinning up network processes, you must mock out or isolate these network descriptors, or modularize the C simulation logic.
2. **Floats and Precision:** The C reference utilizes 32-bit floats (`float`) and trig functions for position and movement vectors (`Vec2f tank`, `Vec2f builder`). Do *not* try to enforce an integer or custom fixed-point position system in Swift; you must match C's float calculations precisely to achieve bit-identical test outputs.

```
for each module, leaf-first:
    1. write Swift implementation
    2. fuzz/enumerate inputs; run C and Swift side by side
    3. assert bit-identical outputs
    4. only then switch callers to Swift
```

This converts "risky 500 KB rewrite" into "verified incremental port," and it is the single
technique that makes D13 tractable.

**Port order — leaf modules first,** both because they're independently testable and because
they're a gentler Swift on-ramp:

| Wave | Modules | Why first |
| --- | --- | --- |
| 1 | `vector`, `rect`, `list`, `buf`, `errchk` | Pure utilities, tiny, trivially fuzzable |
| 2 | `terrain` (360 B), `tiles` (4.9 KB) | Pure enums + predicates, no state |
| 3 | `images` (`mapimage` autotiling) | Pure function of neighbours → tile index; exhaustively testable |
| 4 | `bmap` | File format; round-trip tests against real `.bmap` files |
| 5 | Simulation: shells → tank → LGM → pillbox → base | The fidelity-critical core. Unify client/server here |
| 6 | Networking, then the Cocoa UI layer | Largest and least pure; do last |

## Target architecture

```
~/Developer/XBolo/
  Package.swift            # BoloCore, BoloNet as SPM targets
  Sources/
    BoloCore/              # pure sim. NO AppKit. deterministic. one copy for client+server
    BoloNet/               # transport, session lifecycle
    BoloGlyphs/             # glyph sheet generator (CLI tool, Swift + CoreText)
  Tests/
    BoloCoreTests/         # Swift Testing framework
    DifferentialTests/     # C-vs-Swift oracle comparisons
  XBolo.xcodeproj          # app target: AppKit UI, links BoloCore/BoloNet
  Reference/c/             # inherited C/ObjC, kept as oracle until the port no longer needs it (D61)
  LICENSE                  # original MIT notice retained + our copyright
```

The original ran a **50 Hz tick** — a hard fidelity fact already extracted from the
replay-log format notes. (Note: Per D18, we will use Float rather than fixed-point to maintain
bit-fidelity with the C reference math).

---

## Division of Labor (Multi-Agent Workflow)

To maintain maximum velocity, prevent file conflicts, and enforce high architectural standards, we use a structured multi-agent collaboration model:

1. **IMPLEMENTER (Xcode Claude agent):** Full read/write on the workspace. Writes Swift, owns DifferentialTests, commits. **As of 2026-09-02: also owns detailed code-level planning** — wave-specific trap lists, C-source pre-briefs, and implementation-approach decisions — rather than receiving them pre-authored by PLANNER/PARITY.
2. **PLANNER (Claude.ai, this role):** High-level project management only, as of 2026-09-02 — wave sequencing, stage-gate GOs, the decisions/open-questions log, cross-wave policy (D26-D28 style), and routing PARITY's findings. Does not author detailed code-level trap lists or pre-briefs going forward; that's IMPLEMENTER's own responsibility now.
3. **PARITY (Claude.ai, adversarial):** Independent post-commit audit against the C source. Reports findings only, does not write fixes.

---

## Phases

Reordered from earlier drafts because D10 made the art nearly free and D13 made the test
harness a prerequisite rather than a nicety.

### Phase 0 — Take over the project slot (gate)

**Status: Step 0.1 complete, verified against the repo. Step 0.2 not started — see below.**

**Surveyed state (original):** `~/Developer` did not exist. Xcode was not running (so a move
was safe). The stub was not a git repo and contained only `Untitled Project.xcodeproj`, one
boilerplate `ContentView.swift`, and a `.DS_Store`.

**Revision to D12's implementation — replace, don't rename.** Renaming an Xcode project properly
means editing `project.pbxproj`: target `MyApp`, scheme names, product name, bundle ID. That is
fiddly and error-prone, and preserves nothing (verified: the stub is Hello World). Creating
fresh at the right path serves the same intent with less risk.

**Also: start SPM-only, add the `.xcodeproj` later.** Xcode opens a `Package.swift` directly, so
no Xcode project is needed until there's UI to show (Phase 2). This sidesteps `pbxproj` surgery
entirely *and* avoids a confusing name collision with the reference clone's own
`XBolo.xcodeproj` inside `Reference/c/`.

**Step 0.1 — Local file work — done. (PARTIALLY BROKEN — Action Required):**

| Sub-item | State |
| --- | --- |
| `~/Developer/XBolo`, `git init` | Done — repo present, `main` branch, clean tree |
| Scaffold dirs | Done — `Sources/{BoloCore,BoloNet,BoloGlyphs}/`, `Tests/{BoloCoreTests,DifferentialTests}/` present |
| `Package.swift` | **Broken:** The tools-version is declared as `6.0` but uses `.macOS(.v26)` which requires tools-version `6.2` or above. This must be corrected immediately. |
| `.gitignore` | Present, tracked |
| `LICENSE` | Present, tracked |
| `README.md` | Present, tracked |
| `Reference/c` as git submodule | Done — `.gitmodules` points at `https://github.com/bananazon/xbolo.git`, pinned at `51c3cbc`, working tree populated including nested `TCMPortMapper` submodule |
| Delete `UntitledProjects` stub | Done — directory empty/absent |
| Initial commit | Done — `7bca0d7 Scaffold Swift port of XBolo`, tree clean |

**Step 0.2 — Prove the oracle builds — VERIFIED (RESOLVED):**

- **Status:** Verified. Running `xcodebuild -project Reference/c/XBolo.xcodeproj -target "Mac OS X" -configuration Development build` succeeds cleanly (`** BUILD SUCCEEDED **`) on Darwin 27. The oracle is ready.

**Step 0.3 — Housekeeping — not started.**

- Draft the upstream courtesy issue (D11) for your review before anything is posted.

**Verify:** `swift build` and `swift test` succeed on the package; C XBolo launches and a
game starts; `git log` shows a clean history.

### Phase 1 — Differential test harness

- Bridge the C code into a Swift test target; helpers to invoke both implementations on one input.
- State digest over terrain grid + entity table.
- **Verify:** a trivial module (`vector`) passes C-vs-Swift equality on fuzzed input.

### Phase 2 — Glyph art (unblocks distribution, first real Swift target)

- `BoloGlyphs` CLI: parse `images.h` → manifest → render 256×256 RGBA sheets of 16×16 cells.
- Layout stays byte-identical to the originals so no game-code changes are needed. Verified: index decodes as `row = idx >> 4`, `col = idx & 0xF`.
- Geometric/box-drawing glyphs from an **OFL font** (Noto Sans Symbols, DejaVu) — colour emoji are illegible at 16 px, and Apple Color Emoji is proprietary and non-redistributable.
- 16 tank headings have no 16 legible glyph equivalents: render **one** directional glyph rotated to 16 angles.
- Delete the © Cheshire art and sounds; synthesize simple replacement sounds.
- **Verify:** sheet validator passes; game renders and is playable with glyph art; no original assets remain in git history (needs history rewrite, not just deletion).

### Phase 3 — Incremental port

Waves 1–6 above. Each module: Swift implementation → differential test green → switch callers.
Behaviour-preserving only; log suspected fidelity bugs rather than fixing them here.

### Phase 4 — Measurement rig + fidelity spec

- Stand up [Infinite Mac](https://infinitemac.org) (browser/WASM, bundles Bolo, has a shared folder for file transfer). Confirm Bolo runs and **can write log files we can extract** — unverified, and the main risk here. Fallback: screen recording with frame counting.
- Run controlled experiments (hold accelerate across swamp; time a wall build; sit under a pillbox), extract logs, parse with [rooklift/ancient_bolo_parser](https://github.com/rooklift/ancient_bolo_parser) as an *external instrument only* — it has no licence, so we run it, never copy from it.
- Write `docs/FIDELITY.md`: terrain × speeds/buildability/destruction, LGM action costs and timings, pillbox fire-rate escalation curve, base refuel rates, tank handling. Every row cites its evidence; unknowns marked.
- **Verify:** one parsed original log prints a sane 50 Hz event stream.

### Phase 5 — Close the fidelity gaps

Now, and only now, change behaviour. Each gap: failing test → fix in `BoloCore` → green. One
place to fix, not two — the payoff from unifying client/server.

### Phase 6 — Networking

Self-contained (D4). Port the transport, LAN then internet via UPnP/NAT-PMP.

### Phase 7 — Ship

Universal binary, signing, notarization, DMG.

**Candidate later phase:** Bolo's AI "brains" via FoundationModels — directly serves the Apple
Intelligence learning goal, and `GSRobot.m` shows the reference had a bot API to model on.

---

## Verification

- **Build:** `BuildProject` MCP tool against `XBolo.xcodeproj`; `XcodeRefreshCodeIssuesInFile` for fast Swift diagnostics during the port.
- **Port correctness:** differential C-vs-Swift tests, per module, in CI.
- **Determinism:** identical inputs ⇒ identical state digest across runs.
- **Fidelity:** measured values from parsed emulator logs vs the Swift build's telemetry.
- **Visual:** `device-interaction` skill screenshots; contact sheets for glyph review.
- **Multiplayer:** two local clients + dedicated host; then macOS **Network Link Conditioner** at 100 ms / 2% loss baseline, 300 ms / 5% degraded.

## Risks, honestly

1. **SDK breakage on macOS 27** — blocks Phase 0 entirely. Most likely near-term failure. Still unresolved as of this writing (Step 0.2 not attempted).
2. **Scope.** A 500 KB behaviour-preserving port is the largest thing in this plan by an order of magnitude. Phases 0–2 deliver a legally clean, playable game *before* the port begins, so there's standing value even if the port stalls.
3. **Emulator may not yield extractable logs** (Phase 4). Fallback is frame counting — cruder, slower.
4. **Differential testing only proves agreement with xbolo, not with Bolo.** xbolo was itself written without the original source, so it has its own inaccuracies. That's exactly what Phase 4/5 exist to catch — don't mistake green tests for fidelity.

---

## Wave implementation status (updated 2026-09-04 by PLANNER — Wave 7 v1 vertical slice CLOSED, D89 re-audit PASS `a2f1779`; see AGENT_NOTES.md)

| Wave | Content | Status |
|---|---|---|
| Wave 1 | Vector, Rect, List, Buf, ErrChk | ✅ Complete — b729781 |
| Wave 2 | Terrain enum, Tiles enum, TileGrid, 8 predicates | ✅ Complete — 9695275 |
| Wave 3 | Image constants, mapimage autotiling | ✅ Complete — db747b2 |
| Wave 3.1 | Physics constants, TerrainGrid, terrain speed functions | ✅ Complete — 24d7ae0 (PARITY Findings 1+2 resolved: 6580e2a, 20e156d) |
| Wave 4 | terrainToTile, defaultTerrain/Tile, BMAP format structs | ✅ Complete — 8044fb0 (35 tests green) |
| Wave 4.1 | BMAP RLE codec — readRun/writeRun, nibble helpers | ✅ Complete — 7298d2c |
| Wave 5.0 | Physics constants additions, roundDir, maxSpeed/maxTurnSpeed w/ pill/base overrides | ✅ Complete — e2636fb (PARITY PASS, F4 deferred) |
| Wave 5.1 | GameState model (tanks, pills, bases, shells, builders) | ✅ Complete — a3126c6 (76 tests: 26 diff + 50 unit) |
| Wave 5.2a | tankMoveTick — tank physics tick | ✅ Complete — a752a77 (101 tests: 27 diff + 74 unit) |
| Wave 5.2b | tanklocallogic/enter() — local player input, mine/boat/refuel/fire | ✅ Complete — see AGENT_NOTES (scope note: mine-chain/flood propagation and pill-scatter placement deferred, injection points added) |
| Wave 5.3a | shellTick — shelllogic, shellcollisiontest, recvcldamage, recvcltouch, `killTank` (pulled forward from 5.6 — hidden dependency, armour-zero on shell hit calls it) | ✅ Complete — PARITY PASS (Q14 open, non-blocking) — 151+28 tests |
| Wave 5.3b | builderTick — builderlogic + server-side build/repair/mine/grab-trees handlers merged into the unified tick; absorbs former Wave 5.4's buildercollision | ✅ Complete — PARITY PASS, 27a76d3 (D24 applied; D26 fp-contract fix, cross-cutting) — 199+31 tests |
| Wave 5.3c | pillTick — pilllogic + forestvis (moved from 5.5b, needed by pillTick's firing condition) | ✅ Complete — PARITY PASS after FAIL/fix cycle, 03d56b3 (shared-counter ordering bug, see D27) — 227+33 tests, no coverage lost (D28) |
| ~~Wave 5.4~~ | ~~tankcollision, buildercollision, testAlliance, findPill/findBase~~ | ⏹ Retired (D23) — absorbed into 5.1/5.2a/5.3b |
| Wave 5.5a | explosionAt/superboomAt/chain/flood (mine-detonation cascade), droppills (pill-scatter placement) | ✅ Complete — PARITY PASS, d99815e (D27 held throughout, no repeat of 5.3c FAIL pattern) — 257+33 tests, no coverage lost (D28) |
| Wave 5.5b | explosionTick (drains the explosion *particle* lists — visual-effect duration counters — not the chain-reaction ring buffers, which 5.5a already drains) | ✅ Complete — PARITY PASS, 08c6e85 (D22 phrasing corrected above) — 267+33 tests, no coverage lost (D28) |
| Wave 5.6 | spawn() — two-pass weighted selection; drown | ✅ Complete — PARITY PASS, a3f9540 (D29 applied) — 274 tests total, one LOW doc-only finding (dangling arc4random_uniform comment xref) |
| Wave 5.7 | growtrees (C bug replicated), pill cooldown, base replenish | ✅ Complete — PARITY PASS, 221ba97 (Pill.counter/coolCounter split confirmed genuine, no D27 hazard) — 296 tests total |

| Wave 5.8 | Docs/archive pass — PLAN.md, AGENT_NOTES.md → archive.md, project memory, project-instructions reconciliation (Q13) | ✅ Complete — AGENT_NOTES.md compressed to docs/notes/archive.md, project memory refreshed; project-instructions reconciliation pending Jerod pasting updated text (Claude cannot edit that setting directly) |
| Wave 5.9 | **New 2026-09-02 (Wave 6.1 finding):** wire the mine-cascade injection points (`onMineExplosion`/`onSuperboomTerrain`/`onDropPills`) into their actual trigger sites — `enterTile`, `grabTile`, `tankMoveTick`'s dead-tumble, `smallboom`/`superboom` — with correct causer-player attribution (`state.localPlayer`, uniformly). Also closed the periodic corpse-explosion `killPointBuilder` gap (in-scope, same literal C function) and `MineChain.swift`'s recursive-depth closure-threading gap. | ✅ **Complete — PARITY PASS, merged to `main`.** `d9116a9`+`d3d654e`+`7cb409f` (453 tests, +6, on `wave-5.9-mine-cascade`) → `be2e71d` (audit — independently re-derived D41's ordering fix, causer attribution, and the corpse-explosion scope inclusion, PASS) → `9c042e7` (merge). Report folded into `docs/AGENT_NOTES.md` (`e365d2a`); branch/worktree cleaned up. |

| Wave 6 | Networking — summary row; see 6.0–6.6 below for the D32 sub-wave split plus D38's 6.6 addition (UI split out to its own phase per IMPLEMENTER's 2026-09-02 scope survey — see AGENT_NOTES.md) | ✅ **Complete — Wave 6 (6.0-6.6, all sub-waves) closed 2026-09-04, all PARITY PASS.** D35/D37/D39/D45/D46/D48/D50/D53/D57 all resolved along the way; D54-D56 govern the tracker/NAT design. 572→597 tests across 6.5 alone; full networking phase went from 0 to a working byte-exact wire protocol, session/transport layer, and tracker/NAT-PMP client with no open findings. Wave 5.9 (independent of this sequence) also complete — PARITY PASS, merged to `main`. **Wave 7 (UI/app phase) pre-brief GO'd 2026-09-04 — see below.** |
| Wave 6.0 | Wire codec — `CL*`/`SR*` structs, `CLUpdate`, all three encodings (raw BE float, 1/256 fixed-point, 8-bit brads), derived byte-exact from the C oracle. **Correction 2026-09-02 (Wave 6.3 pre-brief finding):** preambles were never built despite this row originally claiming them — no `Preamble` type exists in `Sources/BoloNet/`; reassigned to Wave 6.3 below, not reopening 6.0. | ✅ Complete — PARITY PASS, `96704cd`+`5c5e47a` (FWIDTH-double finding independently re-derived by PARITY) — 345 tests total (+16) |
| Wave 6.1 | Tick orchestrator — `runclient()`/`runserver()`; new pause/timelimit/domination-basecontrol state machines | ✅ Complete — PARITY PASS, `d0a9834`+`b028bf8` (orig) then `1e0cbe6`+`91c4a8d` (D35 fix) then `3d4563a` (PARITY re-audit PASS) — 363 tests total (+18 net) |
| Wave 6.2 | **30** (not 33 — `sendmesg`/`timelimit`/`basecontrol` have no `GameState` mutation, confirmed on read) `recvsr*` client broadcast handlers (apply-given-value state application, a distinct category from Wave 5's authoritative tick functions — not a thin wiring layer). New callback surfaces for later waves: `onRequestGrabTile` (6.4, network send), `onShouldLeaveAlliance` (6.3, real `leavealliance()`). | ✅ Complete — PARITY PASS. Full chain: `0d44f30`+`a5e84b0` (orig, 408 tests) → `1bc2b90` (audit — 2 findings + Q21) → `682173a`+`3c180c7` (D37 fix, 413 tests) → `d2c8fb0` (re-audit PASS) |
| Wave 6.3 | Server session logic — join/kick/ban/alliance; **now also the three preamble structs** (`JOIN_Preamble`/`BOLO_Preamble`/`TRACKER_Preamble`) reassigned from Wave 6.0's corrected row above | ✅ Complete — PARITY PASS, no findings. `388a8c1` (orig, 445 tests) → `f75e1f2` (audit — clean; 2 secondary notes -> D38/D39) |
| Wave 6.4a | **Split from Wave 6.4 per D43.** Client-side transport: `joinClient(...)`-shaped async join handshake against `NWConnection` (byte-exact per D31/D33); `dgramclient()`'s post-decode CLUpdate-application logic (`PlayerState` field application, shell/explosion list rebuild, bounded dead-reckoning per D44); fog-of-war/sound callbacks simplified per this port's established no-op precedent. **Per D45, extended to own `joinclient()`'s back half (`applyBoloPreamble`, `client.c:690-750`), `clientloadmap()` (`decodeBMap`, discovered missing entirely mid-extension), and the persistent `UDPSession`/`TCPSession` receive loops.** **Per D46, `applyBoloPreamble` also owes `state.baseControlThreshold`.** | ✅ **Complete — PARITY PASS end to end.** Full chain: `e4ca245`+`970a1d8` (original, 502 tests) → `60d5059` (audit — clean code, incomplete scope) → `8296346`+`d8f464e` (D45 extension, 521 tests) → `515429f` (audit — one finding, D46) → `810d9b2`+`5575a00` (D46 fix, 521 tests) → `0e6d714` (re-audit, PASS). |
| Wave 6.4b | **Split from Wave 6.4 per D43.** Host-side transport: `NWListener`-based async accept loop wired to Wave 6.3's join-decision functions; `dgramserver()`'s dedup/relay pure decision logic (Finding 2, confirmed in-scope by D43) — packet sanity/player-range check, `dgramaddr` port update, `isNewerSeq`-based dedup (Wave 6.0), tank-position apply, cross-player relay; the tracker reachability echo at `dgramserver()`'s occurrence only (`registerserver()`'s occurrence deferred to Wave 6.5 per the pre-brief's proposed split, confirmed). **Per D45, its pre-brief must explicitly verify (not assume) that Wave 6.3's `evaluateJoinRequest`/`applyJoin` fully cover `joinplayerserver()`'s server-side "new player" state init — 6.4a's mirror-image gap was a mis-citation, not a proven-safe pattern — plus explicitly own the persistent TCP accept/receive loop dispatching decoded `CL*` opcodes to the already-shipped `recvCl*` handlers (Wave 6.6).** | ✅ **Complete — PARITY PASS, no fixes required.** `b26ee69`+`c0cf490` (Implementer: full implementation + completion report, 560 tests, +39, no coverage lost) → `11d792c` (Parity post-commit audit — PASS, every claim re-derived including the six extended `RecvCL.swift` callback signatures, the `CLSendMesg` masked-relay special case, and the `HostListener` `AsyncStream` exclusivity fix; one cosmetic citation drift noted, non-blocking). Three disclosed open issues (dropPills broadcast gap, unwired UDP listener, zeroed `dgramaddr`) ruled by D50 into new Wave 6.4c — not silently implied closed. |
| Wave 6.4c | **New 2026-09-03 (D50 — Wave 6.4b completion report's three disclosed open issues, PARITY-confirmed real).** Wire `NWListener(using: .udp)` to drive `DgramServerRelay.swift`'s existing decision core against real datagrams; derive `dgramaddr` from the joining TCP connection's real address at join (`server.c:844`) instead of a zeroed placeholder; add `onShouldBroadcastDropPill` to `MineChain.swift`'s `droppills()`/`dr()` (Wave 5.5a) and wire `SRDropPill`, mirroring 6.4b's G-2 pattern. | ✅ **Complete — PARITY PASS.** Pre-brief `091c364` (D51/D52 ruled) → `5fdb1bc`+`b3d6c8a` (Implementer, 571 tests) → `7ead213` (Planner review, PARITY activated) → `5bab7c1` (PARITY audit — one real finding, D53) → `534aa57`+`172a1d0` (Implementer: D53 fix — `handlePlayerDisconnect`'s `.normal` case now uses `sendToAll`, matching `sendsrplayerexit()`'s combined effect; `.abnormal` untouched; 572 tests) → `999dbde` (PARITY re-audit — PASS, fix confirmed correct, both regression tests independently assessed sound including the negative-assertion pattern). Closed by Planner. |

| Wave 6.5 | Tracker/NAT-PMP — **split into 6.5a/6.5b per D55, run simultaneously per Jerod's directive.** Trap flagged 2026-09-03: `TrackerHost`/`TrackerHostList` (`tracker.h:36-56`) are the one wire struct family that is NOT `__attribute__((__packed__))` — reproduced the offset-51 pad byte deliberately; two further trap bugs found at pre-brief, ruled by D56 (heartbeat's missing `htonl` on `timelimit`, replicated bug-for-bug; the un-`bzero`'d struct's indeterminate bytes, zero-filled as a disclosed safety deviation), both landed and tested; one post-audit fix (D57) landed and re-confirmed in 6.5a. | ✅ **Complete — both sub-waves PARITY PASS, closed 2026-09-04.** 572 → 597 tests, all green, no coverage lost (D28). See 6.5a/6.5b rows below for full commit chains. |
| Wave 6.5a | Tracker protocol — `registerserver()`'s nine-step handshake, the 60-second heartbeat (`sendtrackerupdate()`), `listtracker()`'s browse decode. Oracle-testable against `Reference/c/` throughout. | ✅ **Complete — PARITY PASS.** `a23d49d` (591 tests) → `150e822` (Planner review, PARITY activated) → `c9e37ef` (PARITY audit — layout/D56/T-3/T-8 all independently re-derived and confirmed sound; one real finding, D57) → `6cbec85`+`f7b0528` (Implementer: D57 fix — `truncatingIfNeeded`, matching the C's actual conversion semantics; 597 tests) → `7f1a9ee` (PARITY re-audit — PASS, fix semantically verified not just trap-avoidance, regression test's bit pattern hand-checked). Closed by Planner. |
| Wave 6.5b | NAT-PMP/UPnP port mapping via `DNSServiceNATPortMappingCreate` (D54) — no C oracle possible, verification is a live router round-trip; carries the `README.md:61-64` licensing constraint directly. | ✅ **Complete — PARITY PASS, no findings (closes independently of 6.5a per D57).** `a250c57` (5 tests) → `c68d250` (Planner review) → `cdff28d` (mechanical build fix) → `150e822` (PARITY activated) → `c9e37ef` (PARITY audit — decision logic, byte-swap arithmetic, `Unmanaged` boxing all confirmed correct; live NAT round-trip correctly disclosed as D55's non-goal, not a gap). |
| Wave 6.6 | **New 2026-09-03 (D38, Wave 6.3 PARITY note):** the ~19 unassigned `recvcl*` server TCP-receive handlers (`recvcldamage`, `recvclbuildroad`/`buildwall`/`buildboat`/`buildpill`, `recvclrepairpill`, `recvclplacemine`, `recvclsmallboom`/`superboom`, `recvclrefuel`, `recvclhittank`, `recvclsendmesg`, `recvcldropboat`/`droppills`/`dropmine`, `recvcltouch`, `recvclgrabtile`/`grabtrees`) — same shape as Wave 6.2/6.3's session logic (pure decision/mutation core, split from the receive mechanism per the 6.3 precedent), covering every `CL*` opcode except `CLSetAlliance` (already done, Wave 6.3). | ✅ Complete — PARITY PASS, no findings. `ebb8fe4`+`0bc2e17` (487 tests, +40) → `f31413a` (audit — independently re-derived the `detonated`/`firesDamageBroadcast` case lists by hand, confirmed the Swift-exclusivity constraint is real, PASS). One non-blocking observation recorded (`testAlliance`'s `used` guard, provably redundant, not a new issue). |
| Wave 7 | **Revised 2026-09-04 (D60) — the UI/app phase, scoped down to a playable vertical slice as its v1 cut-line.** Sized against the reference Cocoa app before scoping (`Reference/c/Mac OS X/`: a 4,037-line monolithic `GSXBoloController.m`, ~5,700 lines total across host/join panels, HUD, key remap, alliance/chat panels, tracker browser) and found genuinely large with no clear ship path as a single wave — split per D60 into a v1 vertical slice (this wave, 7.0-7.3 below) plus three deferred, not-yet-GO'd future milestones (see the note below the sub-wave rows). **Folds in the never-started Phase 2 (glyph art) work** as 7.0 below — `Sources/BoloGlyphs/` is currently a one-line stub. `images.h` sized at 290 unique names across **two** independent index spaces/sheets (tiles 177/256, sprites 113/256) — corrected 2026-09-04 (D62) from an initial miscount that assumed one 256-cell sheet. **Note carried forward:** Phase 2's own verify step calls for a git-history rewrite to strip the original copyrighted assets (Q18) — real, destructive, cost grows with every commit made before it happens; must be raised to Jerod explicitly before execution, not done silently mid-wave. **Correction 2026-09-05 (D92):** this premise doesn't hold — `Reference/c` is a plain git submodule with no copyrighted bytes in this project's own history; Q18 is resolved and closed, no destructive rewrite is needed. | ✅ **v1 vertical slice CLOSED 2026-09-04 (7.0-7.3 all PARITY PASS) — D60's cut-line fully delivered.** A window opens, renders a real map from real generated assets, and lets you drive a tank via the actual physics engine, keyboard-controlled, tick-driven, single-process — exactly what D60 scoped this wave down to. Six real regressions/gaps were found and fixed along the way, all via the same fix→re-audit→close discipline this project has used since Wave 5: D70 (tank-heading convention, 7.0), D77 (PNG premultiply defect, 7.0-era, closed at 7.1), D84/D86 (corner-fill defect + its own follow-on road regression, 7.2), D88 §3/§4 (LMINE immediate-plant + unwired respawn, found at 7.3's pre-brief, not defects), D89 (mine-waste on unminable terrain, 7.3). Milestone B (Host/Join UI), Milestone C (HUD/prefs/alliance), and Milestone D (zoom/scroll polish, signing/notarization, Q18's git-history rewrite) remain deferred, not yet GO'd — see the note below the sub-wave rows. See sub-wave rows below for full detail. |
| Wave 7.0 | Asset pipeline (Phase 2) — `BoloGlyphs` CLI renders **two** tile/sprite sheets (corrected 2026-09-04, D62/D63: tiles 177/256 cells, sprites 113/256 cells, not one combined sheet) from the 290 image constants and `mapimage()` already ported to `Sources/BoloKit/Images.swift` — no re-parse of `images.h` needed. Cell indexing `row = idx >> 4`, `col = idx & 0xF` confirmed correct; sheet row-0 origin is **top-left** (D66, ratifying Implementer's pre-brief), must match 7.2. Plus: 16 tank headings as one directional glyph rotated per-angle, geometric glyphs drawn procedurally (D67, approved deviation — no vendored OFL font; a `GlyphSource` seam keeps one addable later; shipped as raw RGBA pixel manipulation rather than literal `CoreGraphics` calls, a stricter/more-testable reading of D67's substance, noted not ruled on — no font, still procedural, seam intact). Implementation split into `BoloGlyphsCore` (library, testable) + `BoloGlyphs` (thin executable) per D68, approved. Sound synthesis explicitly deferred past v1 (not needed to prove the render/input/tick loop). Original © Cheshire art stays out of `Sources/`/`Reference/c/`'s working tree is fine as-is (submodule, not distributed) — the git-history purge itself is Milestone D's concern, not this sub-wave's, since nothing here re-adds copyrighted bytes to history. | ✅ **Closed 2026-09-04 — PARITY PASS (`79840b1`), every claimed number independently re-derived from `Reference/c/images.c`/`images.h` directly, no citation drift found.** Self-check clean (47/16/10/16/31/8/9=137 tile variants + 8 flat + 32 pill = 177, zero gaps/overlaps), sprite count 113 independently recomputed and matched exactly, 605 tests (+8, D28 baseline 597→605, confirmed by PARITY's own count). One follow-up item spun off, not a 7.0 defect — see D70. |
| Wave 7.1 | Xcode app target — create the actual macOS app target/bundle (none exists yet; the project has been SPM-only since Phase 0 by design). Minimal window, placeholder app icon, entitlements skeleton. Unblocks every other sub-wave — nothing in 7.2/7.3 can run as an app without this existing first. | ✅ **Closed 2026-09-04 — PARITY PASS (`a03aa07`), audited by execution (build/test/decode), not hand-trace.** `Bolo 2026.xcodeproj` at the repo root, exactly one Cocoa App target/scheme, `BoloKit`-only dependency (D73), D72's build-time sheet generation verified from a full clean build (DerivedData deleted) with `BoloGlyphs` as a real `PBXTargetDependency` and declared input/output paths, generated-not-copied placeholder icon (md5-verified free of `Reference/c` asset bytes, and byte-identical to a fresh regeneration), app launches and reads `BoloKit`'s real `ticksPerSec` with both sheets in bundle. Tests 608 → 612, no coverage lost (D28). Four flagged deviations accepted at **D74**; both required follow-ups — **D75** (deployment target 26.0) and **D76** (shared `.xcscheme`) — landed `6e060e9`, independently re-verified by PARITY on the shipped binary. Bundle identifier confirmed by Jerod — see **D90**. **PARITY's audit found one confirmed defect pre-existing from Wave 7.0, not a 7.1 regression — see D77** (fix gated on Wave 7.2's coding GO, not this close) — plus two record corrections (**D78**, **D79**) and a `docs/PARITY.md` amendment (**D80**, adopted). |
| Wave 7.2 | Game rendering — the actual draw loop (`GSBoloView.m`'s 600-line role, MIT-licensed and directly portable per D1/D13, D25's clean-room restriction applies only to WinBolo), consuming 7.0's sheets to render terrain + tanks from a live `GameState`. **Rendering approach (SwiftUI Canvas/TimelineView vs. an AppKit `NSView`/`CALayer`-backed view via `NSViewRepresentable`) is an engineering call for Implementer to prototype and report on, not a call requiring Jerod's ruling up front** — PLANNER will review whichever approach the pre-brief proposes against this wave's performance needs (matching D41's tick-timing discipline) before the coding GO stands. **Fog-of-war/seen-tiles display, ruled 2026-09-04 (D65): out of scope for v1** — the reference's `client.images[y][x]`/`seentiles`/`fog` display arrays were never modeled in this port (`BMap.swift`'s own comments); v1 treats every tile as visible and reads the sheet index straight from `mapimage()`, deferring real fog-of-war display to a later wave. | ✅ **Closed 2026-09-04 — PARITY PASS end to end (`3dfabff` original audit, `07974bd` D86 re-audit).** `GameRenderView`/`GameRenderRepresentable` built exactly per D81/D82. `tileFor`/`displayTileGrid`: verified line-by-line against `client.c:6106-6141`. No-y-flip claim: independently reproduced three separate ways (two by Implementer, one by PARITY's own headless `CGBitmapContext` harness) — tank draws as an east-pointing triangle at heading 0, matching D70. All four smaller judgment calls re-derived against actual field-population/call sites and hold up. One regression found and fixed within the wave (D86 — `.road` missing the same diagonal-data exemption `.wall` got, collapsing 26 of 31 autotile images into 9), fixed (`3e03137`+`e5fccee`) and independently re-audited clean (`07974bd`) — every claim re-derived from a fresh harness, not re-checked against the completion report. Tests 614 → 627 total across the wave (D28-compliant throughout; first parameterized test in the suite, so declared-count ≠ executed-case-count going forward — informational only). |
| Wave 7.3 | Input + tick loop — keyboard input wired to tank movement, driving `BoloKit`'s real tick loop (Wave 5/6.1), rendered live via 7.2. **Single-process, no networking wired in** — proving the core render/input/simulate loop is the whole point of the v1 slice; Host/Join panels and any multiplayer wiring are explicitly Milestone B, not this wave. Closes the loop: a window opens, shows a real map with a controllable tank, driven by the actual physics engine. | ✅ **Closed 2026-09-04 — PARITY PASS end to end (`4eb483f` original audit, `a2f1779` D89 re-audit).** `GameSession.swift` (new, `@MainActor`, `DispatchSourceTimer` at 50Hz) drives `runTick`+`GameRenderView.render` per D82 — measured jitter (D41): ~20.05-20.08ms avg interval, 3.4-5.1ms steady-state jitter, comfortably inside the 20ms budget. Keyboard capture extends `GameRenderView` itself (`keyDown`/`keyUp`/`flagsChanged`, `isARepeat`-guarded, mirroring `GSBoloView.m`'s first-responder architecture), routed through a new pure `BoloKit` function `inputFlagsChange(forKeyCode:isDown:)` (literal default keymap from `DefaultPreferences.plist`). **D88 §3** (`layMineOnKeyDown`, `TankLocalTick.swift`) delegates straight to the existing `plantMine` with no duplicated terrain guard — the exact concern flagged for PARITY, directly answered in the diff. **D88 §4** (`tankMoveTick` now calls `spawn(state:)` via its own `inout` binding, `TankTick.swift`) avoids the exclusivity trap entirely — the second flagged concern, also directly answered. Verified via a real `xcodebuild BUILD SUCCEEDED` (no toolchain hang this run) plus an end-to-end integration harness driving a live `DispatchSourceTimer` session and synthesized real `NSEvent`s through the actual entry points — confirmed across 3 runs: dead-player revival through the real timer, mine-planting while stationary via the real modifier-key path, and real physics movement through the full input pipeline. Tests 627 → 639 (+12, 0 removed, D28-compliant). Four judgment calls disclosed (InputKeymap's BoloKit placement, Shift-only `flagsChanged` tracking, unconditional `makeFirstResponder`, a debug-only tick-interval buffer for the jitter measurement). **This is the last sub-wave of Wave 7's v1 vertical slice (D60) — closing this with a PARITY PASS closes the whole slice.** **PARITY audited (`4eb483f`) — NOT a clean PASS.** Priorities 2/3/5 (spawn exclusivity, keyboard-capture architecture incl. byte-for-byte keymap verification, D28 test count) all independently re-derived and PASS. Priority 4 (jitter) magnitude independently corroborated via a separate bare-timer harness (partial check, disclosed). **One confirmed regression, D89, fixed and re-audited clean (`2a53299`+`28b00ee`, PARITY PASS `a2f1779`).** |
| Milestone B | **Coding GO'd 2026-09-05 (D93) — first of the three, per Jerod's alphabetical-order ruling.** ✅ **B.0 (package/entitlement plumbing) closed 2026-09-05 — PARITY PASS (`63b58c0` auditing `27c200c`+`89fadd4`).** `BoloNet` exported as a real SPM library product, `ENABLE_OUTGOING_NETWORK_CONNECTIONS`/`ENABLE_INCOMING_NETWORK_CONNECTIONS` added (D78, finally landed), `Bolo 2026` target wired to depend on `BoloNet` — no Swift source touched, no `import BoloNet` call site yet (that's B.1). PARITY independently re-derived all four checks (Package.swift diff, `.pbxproj` object-count deltas, entitlement keys, real `xcodebuild`+`codesign`+`nm` — 639 tests, 5 entitlements, 5,283 `BoloNet` symbols, exact match). One non-blocking number-accuracy note (pre-brief's "126 public declarations" undercounted small wire-format structs; actual 638 — file count of 18 was exact), nothing actionable. ✅ **B.1 (navigation shell) closed 2026-09-05 — PARITY PASS (`8b78e84` auditing `0948f26`+`b116ac3`).** `AppRootView`/`NewGameView`/`GameView` (renamed from `ContentView`) landed exactly as ruled (single-window mechanism, temporary "Play Demo" scaffolding, both confirmed verbatim in the shipped code). 639 tests unaffected. ✅ **B.2 (host panel, local-only) closed 2026-09-05 — PARITY PASS (`54f87f7` auditing `ea089d9`+`4c9e4ba`).** `HostGameView` (map picker + settings form, D94-narrowed) landed clean — empty-starts guard confirmed load-bearing (traced `Spawn.swift:41`'s unconditional indexing), decode/merge/spawn path re-verified via an independent from-scratch harness with different values than Implementer's own, all 5 form fields confirmed 1:1 against the reference's outlets and shipped defaults. 639 tests unaffected, entitlements unchanged. Two non-substantive citation-drift notes, no rework needed. **B.3 (join panel) closed 2026-09-05 — PARITY PASS (`bcdc9ca` re-audit).** `JoinClient`/`JoinGameView` clean; D97 (empirical-claim comment correction) fixed and re-confirmed, no functional change. ✅ **B.5a (accept/join wiring) closed 2026-09-05 — PARITY PASS (`3e18775` auditing `01a2d89`+`a00ad0a`).** `runHostAcceptLoop`'s crux concurrency claim (never more than one `processJoinAttempt` in flight) independently proven with a built, negative-control-validated test (a bug reintroduced on purpose made the test fail correctly, then reverted) — not accepted on "AsyncStream is single-consumer" reasoning alone. 641 tests confirmed (639→641, +2). One self-caught test-synchronization bug (not a production defect) fixed before commit. B.5b split further per D96 into **B.5b** (tick timer + dgram relay + host's own outbound `CLUpdate` — pre-brief reviewed, coding GO pending one clarification) ✅ **B.5b closed 2026-09-05 — PARITY PASS (`9e72569` re-audit).** `HostGameEngine`'s single-mutator design proven with a negative-control test; D98 (pause/time-limit cadence gate) and D99 (time-limit boundary off-by-one) both fixed and re-confirmed. Base-control-reached's latch semantics deferred as a tracked gap (confirmed genuinely necessary, not a shortcut). 650 tests. ✅ **B.5c (TCP `CL*` dispatch, dynamic-producer extension) closed 2026-09-05 — PARITY PASS (`b4866f6` auditing `8ca6567`+`82068fc`+`47e9c09`+`25bce0f`).** Items 1-5 + D101 (join slot-leak fix) both independently re-derived (negative control on D101, diff-level behavior check on the dispatch split) — not just re-run tests. One real new gap found by scratch-test execution, tracked as **D102** rather than blocking close: `HostGameEngine.stop()` doesn't tear down already-joined connections/producer `Task`s, latent since `stop()` has no production caller yet — revisit at Wave 7.3. 655 tests confirmed (650→655, +5: +4 from items 1-5, +1 from D101). Item 6 (mine-chain broadcast wiring) split out to new ✅ **B.5d — closed 2026-09-05 (`35e2320`+`f3e79af`) — PARITY PASS (`aaf2229`).** Scope self-corrected down during implementation (flagged live, no ruling needed both times): `RecvCL.swift`'s call sites were already correctly wired since Wave 6.6 (PARITY independently confirmed all 16 `explosionAt`/`superboomAt` call sites, not just the 3 spot-checked); the only real gap was `chain()`/`flood()` (`MineChain.swift`), fixed with a single `onShouldBroadcastSmallBoom` parameter, no causer-threading refactor needed anywhere. `onDropPills` direct-call refactor also landed and PARITY-confirmed with an independent negative control — a real behavior fix, not just wiring (`dropPills`'s spiral-search placement had never actually run in production at any of its 5 fire sites), plus a dead-end `CLDispatchCallbacks.onDropPills` no-op fixed mid-refactor, also negative-control-confirmed. One non-blocking precision note: D103's ruling text said the fix sites would live "inside `explosionAt`/`superboomAt`"; they shipped in the callers (`chainAt`/`floodAt`) instead — already live-flagged and acknowledged at the time, PARITY just stated it precisely. 660 tests confirmed (655→660, +5), stable across 3 runs. `killSquareBuilder`/`killPointBuilder`'s local-only multiplayer-scoping gap split into new ✅ **B.5e — closed 2026-09-05 (`b0d2791`+`ece24c4`) — PARITY PASS (`55513aa`).** All 7 steps landed exactly per the approved ordering, no scope surprises (the ~112-site estimate held). 6 fields (`mines`/`trees`/`builderTask`/`builderMines`/`builderTrees`/`builderPill`) moved `LocalPlayerState`→`PlayerState`; `killPointBuilder`/`killSquareBuilder` now loop every connected player — PARITY confirmed against `client.c:6999-7045` directly that this is the correct generalization for a unified process, not a deviation; `killBuilder` takes an explicit `player: Int`. The bonus clobbering bug fixed for free by the same migration — including two dead `if player == state.localPlayer` gates in `returnTick`, confirmed genuinely obsolete (not a reference invariant) against `client.c:4934-5000` and independently negative-controlled. Migration exhaustiveness confirmed by grep (one harmless pre-existing doc-comment hit, not live code). 662 tests confirmed, full suite. **B.4** split out per D105 into new ✅ **B.6 (tracker/UPnP UI wiring) — closed 2026-09-05 (`a7c9392`+`d4a6ebf`) — PARITY PASS (`9365c7f`).** Host-side toggles confirmed genuinely inert (plain `@State`, no live call anywhere, disclosure text character-for-character matching `portText`'s D94 precedent); join browse list confirmed genuinely wired to the real `listTrackerGames`; default tracker hostname verified against `Reference/c/en.lproj/DefaultPreferences.plist`'s own `GSTrackerString`; build + both `#Preview`s execution-verified. Which surfaced **D107**: no UI path anywhere in the app actually starts a real `HostGameEngine`, all of B.5a-B.5d's engine work has no caller yet. Split into new ✅ **B.7 — closed 2026-09-05 (`f4b8efc`+`de31477`) — PARITY PASS (`6b31ba1`).** `HostGameView.startHosting()` now builds a real `HostListener`/`HostDgramListener`/`HostGameEngine`, hands the **engine** (not a bare `GameState`) up through a new `AppScreen.hosting(HostGameEngine)` case; `GameSession`'s host path creates no timer of its own at all, delegating `start()`/`stop()` straight to the engine and rendering off a new `HostGameEngine.onTickRendered` callback firing once per tick with a value-type snapshot. D102's `stop()`→`shutdown()` fix lands with its first real caller. **Disclosed addition beyond D108's literal wording, not folded in silently:** local keyboard input can't safely mutate `HostGameEngine.state` directly (the consumer `Task` has no actor isolation, not guaranteed main-thread) — two new `HostEngineEvent` cases + `submitLocalInputChange`/`submitLocalLayMineKeyDown`, reusing the existing "queue into the merged stream" mechanism every other producer already uses, not a new one. 665 tests (662→665, +3, negative-controlled). PARITY confirmed tick-conflict/local-input exclusivity by exhaustive grep of every mutating `state` access (stronger than a runtime negative control for a race claim), the `onTickRendered` snapshot as a genuine value-type copy, and independently negative-controlled D102's `shutdown()` fix plus its real app-side caller (`GameView.swift`'s Quit-to-Menu/`.onDisappear`). **Surfaced D108's third finding: a symmetric, equally real gap on the join side** — `GameSession`/`JoinGameView` have zero live network loop post-handshake (confirmed against `GameSession.swift`'s own D73 header); a second real player joining a real host gets the map, then runs a fully disconnected local sandbox forever after. Split into new ✅ **B.8 — closed 2026-09-06 (`106946c`+`4b309e6`+`058d23f`) — PARITY PASS (`53ff764`).** Transport restructuring (handshake moves from `joinClient`'s scoped `withNetworkConnection` into `TCPSession` itself, one type owning the connection's lifecycle end to end) + a third `GameSession` mode that never calls `runTick` (applies `SR*` broadcasts via `recvSr*`, sends outbound `CLUpdate`s via `UDPSession`), broadcast-cadence movement only, PLUS running `tankMoveTick`-only local physics for the join player's own tank (self-authoritative, matching the host's own trust-and-relay model per D114) — narrowed further by D116: `tankLocalTick`'s shared-object branches (touching pills/bases/mines) need real outbound `CL*` sends the join client's protocol doesn't have yet, not the existing direct-mutation calls, so shared-object interaction is out of B.8's own scope. `UDPSession` gained an internal per-player seq/lastUpdate table (D115) to unblock the relay receive loop. ✅ **B.9 closed 2026-09-06 (`8da3454`+`2aa96c6`) — PARITY PASS with findings (`8242443` auditing `8da3454`+`2aa96c6`+`33dca74`, D126).** Re-scoped mid-flight to its real, bigger-than-assumed shape: remote players' tanks/builders/shells were completely invisible on screen (a stale D73-era rendering assumption from before B.7/B.8 made real multiplayer possible), not just jerky — fixed first (`GameRenderView.drawSprites()` generalized to every connected player, reusing C.0's `testAlliance` check), then `RemotePositionSmoother` (pure `BoloKit`, no reference counterpart — port-original UI polish) layered on top, negative-control-verified. Builder positions for remote players smoothed 2026-09-07 (`46e6f60`, PARITY PASS `c4f4cc7`, path-to-v1.0 Phase 2), plus other players' name labels added (matching `GSBoloView.m:328-330`'s `vis > 0.90` case). Shell positions remain raw by design — `PlayerState.shells` array indices aren't stable identities across ticks, confirmed not smoother-safe. 690 tests. ✅ **B.10 — closed 2026-09-06 (`e37d0a9`+`5a001f4`) — PARITY PASS (`3d8c2d5`).** `detectJoinTileEntry`/`detectJoinLMineKeyDown` (`TankLocalTick.swift`, read-only, verified by tests that `state` stays unmutated) ported branch-for-branch against `enter()`/`layMineOnKeyDown`; wired into `GameSession.swift`'s join tick handler, sends via the existing `TCPSession.send`. Zero new `BoloNet` surface, no `GameSession` concurrency changes — pre-brief's sizing held exactly. 511 tests (501→511, +10, wider than the estimated 4-6). **Disclosed incidental fix:** the join-path initializer had never wired `view.onLayMineKeyDown` at all (a pre-existing B.8-era gap, not introduced here) — fixed since otherwise B.10's own LMINE-key send would be unreachable, confirmed genuine by PARITY diffing against the pre-commit state directly. PARITY traced both functions branch-for-branch against `enter()`/`keyevent()`'s LMINE case and confirmed read-only via both direct reading and the 10 tests' own field-level assertions. Builder-task/shell-impact sends explicitly out of scope (join path doesn't run those ticks yet) — required (alongside B.7/B.8) before Milestone B delivers what D58 actually promised. Host/Join panels wired to Wave 6's already-built, fully-tested `BoloNet` (per D58). Confirmed: `BoloNet` has no SPM product export yet (Milestone B's own mechanical first step, D74), no `ENABLE_OUTGOING_NETWORK_CONNECTIONS`/`ENABLE_INCOMING_NETWORK_CONNECTIONS` entitlement keys exist yet (D78), and the app currently has **no navigation shell at all** — `ContentView.swift` is a bare render view over a hardcoded demo `GameState`, no "New Game"/Host/Join window exists, and no `.map` file exists anywhere in-repo to feed a host flow's map picker. Sized closer to Wave 7's UI-wiring character than Wave 6's protocol-design character — the hard networking work is already done and tested; the one genuinely new axis is this project's first `async`/`await` call spanning a SwiftUI/AppKit UI boundary (`GameSession.swift`'s tick loop deliberately avoided this via a synchronous timer). **Open risk for B.3's pre-brief:** the reference's `joinprogress()` dispatches 19 distinct `kJoin*` status codes (6 live progress states, 8 distinct network-error cases) through one callback — **corrected 2026-09-05: no percentage exists anywhere in the reference's join path**, `RECVMAP`'s progress case is a bare state transition with no partial-bytes reporting; B.3's own pre-brief traced this directly and caught the earlier miscount (`bolo.h:240-272`, `GSXBoloController.m:3780-3872`); `Sources/BoloNet/JoinClient.swift`'s `JoinClientError` only models the 6 protocol-rejection cases plus two framing catch-alls, and its `async throws` call has no progress-callback mechanism at all — replicating the reference's live progress sheet and per-failure-mode messaging needs new `JoinClient` surface area, not just UI wiring. Proposed split (Implementer-lens research, subject to a real pre-brief at coding-GO time): **B.0** package/entitlement plumbing (export `BoloNet`, add the two entitlement keys, wire the target dependency); **B.1** navigation shell (Host/Join tab window, currently nonexistent); **B.2** host panel, local-only path (map picker, host settings form bound to already-modeled `DominationType`, wired to `HostSession`); **B.3** join panel (address/port/password fields, progress UI — blocked on the `JoinClient` gap above); **B.4** tracker + UPnP wiring (optional/stretch — `TrackerBrowser`/`PortMapping` are already built and tested, this is pure UI binding, could fold into B.2/B.3 instead of standing alone). **B.5 (new, D94)** — the
real host-network engine: draining `HostListener`/`HostDgramListener`'s accept-loop streams, wiring
the already-built join-handshake functions (`processJoinAttempt`/`runJoinHandshake`), and running the
50Hz tick loop concurrently with inbound-message dispatch and `CLUpdate` broadcast to every connected
`HostSessionTable` slot — genuine, previously-undesigned engineering (real concurrent async I/O
against `state: inout GameState`'s single-writer discipline), not UI wiring. Corrected out of B.2
(D94) after IMPLEMENTER traced the actual unwired-primitives gap; not yet pre-briefed, sequencing
relative to B.3/B.4 to be decided when IMPLEMENTER gets there (tracker registration in B.4 likely
wants B.5 to land first, since registering a host with no real engine behind it is meaningless — not
a hard rule yet, just a dependency worth noting). **Split further per D95** into **B.5a** (accept/
join wiring — a host can be joined and see players register, no gameplay relay yet; low-risk,
`JoinAcceptSerializer` already serializes it) and **B.5b** (the tick+relay+dispatch engine — actual
playable multiplayer, built on a single-serialized-consumer-over-a-merged-event-stream architecture,
approved same footing as D81). Neither pre-briefed yet. | 🟡 **Coding GO'd (D93) — pre-brief pending, first sub-wave (B.0) assigned to IMPLEMENTER.** |
| Milestone C | **Pre-planned, not GO'd, 2026-09-05 (D92 pass).** Full HUD/stat views, key remap, alliance/messages panels, sound, preferences (per D60). The largest of the three remaining milestones by sub-wave count, but most of it is Wave-7-character bind-existing-model-to-UI: HUD status icons, alliance panel, builder-tool matrix, and player kick/ban all have complete, already-tested `BoloKit`/`BoloNet` models behind them (`PlayerState.alliance`/`testAlliance`, `BuilderStatus`, `HostSession.hostKickPlayer`/`hostBanPlayer`) waiting only for a UI. **Two corrections to this pre-plan's original framing, both caught by subagent verification rather than assumed:** (1) key remap is **not** "expose already-shipped defaults for editing" — `Sources/BoloKit/InputKeymap.swift` is a hardcoded 7-case `switch` over literal keycodes, only 6 of the reference's 14 remappable bindings are wired at all (no `InputFlags` equivalent for scroll/view-centering exists yet), so C.1 must build the remappable keymap model itself, not just a settings UI over it. (2) sound is **not** procedural synthesis (an earlier, unverified claim of mine) — independently confirmed sample-based: 24 named `.aiff` effects (`bubbles`/`build`/`builderdeath`/`explosion`/`fbuild`/`fbuilderdeath`/`fexplosion`/`fhittank`/`fhitterrain`/`fhittree`/`fshot`/`fsink`/`fsuperboom`/`ftree`/`hittank`/`hitterrain`/`hittree`/`mine`/`msgreceived`/`pillshot`/`sink`/`superboom`/`tankshot`/`tree`) played via small round-robin `NSSound` pools (`GSXBoloController.m:357-490,3620-3737`) — no DSP/oscillator code anywhere in the reference. This makes C.3's *code* small, but it needs licensed replacement audio assets (Q28) — the same asset-sourcing problem Wave 7.0 solved for art — plus new "simulation event → sound trigger" hook points through the tick loop that nothing in Wave 5/6/7 needed. **Real fidelity risk found, inherited from D65, not new:** the alliance system and fog-of-war are not independently scopable — `requestalliance()`/`leavealliance()` (`client.c:6314-6454`) call `increasevis()`/`decreasevis()` to merge/unmerge shared vision, which this port has never modeled anywhere (already disclosed in `SessionLogic.swift`'s own header). Building C.2's alliance UI before real fog-of-war exists means alliance accept/leave will functionally diverge from the reference (no vision reveal) — an accepted, D65-consistent v1-shape gap, not a defect, but one the C.2 pre-brief should state explicitly rather than silently ship. HUD status icons, by contrast, were traced and confirmed independent of fog/seen-tiles state (`setPlayerStatus`/`setPillStatus`/`setBaseStatus`, `GSXBoloController.m:2103-2419`, never touch `client.images`/`seentiles`/`fog`) — no equivalent risk there. Proposed split (Implementer-lens research, subject to a real pre-brief at coding-GO time): **C.0** status/HUD panel + player kick/ban (lowest risk, bind-only); **C.1** key-remap model + UI (biggest risk in C — touches `BoloKit`'s already-shipped `InputKeymap` contract, not purely additive); **C.2** alliance panel (bind-only, but ships the fog-of-war divergence disclosed above); **C.3** sound (the one genuinely new engineering axis in B/C — licensed asset sourcing per Q28, plus new tick-loop hook plumbing); **C.4** messages panel (small net-new `GameState` field — no message-history model exists yet); **C.5** preferences shell (thin, persistence-only); **C.6** builder-tool UI (bind-only, `BuilderStatus` already fully modeled). | **Started ahead of schedule 2026-09-06 per D118 (Jerod's direct override of D93's sequencing)** — B.9/B.10 (Milestone B's last two loose ends) left open, not blocking. ✅ **C.0 closed — PARITY PASS (`8759f8d` auditing `318e244`)** — `submitKickPlayer`/`submitBanPlayer` on `HostGameEngine`, `PlayerStatusView.swift`, narrow `GameSession` passthrough; self-caught test bug independently re-traced and confirmed genuinely fixed. ✅ **C.5 closed — PARITY PASS (`01a2b0e` auditing `3db8d6c`)** — 4-scalar-field `@AppStorage` preferences shell, keys verified against the reference plist directly. ✅ **C.3 closed — PARITY PASS (`75dbd63` auditing `2662d5e`)** — procedural sound synthesis (D121/D122); PARITY built and ran the `BoloSounds` executable directly, confirmed valid playable output via `afinfo`; one bookkeeping correction, no functional defect. Xcode Run Script wiring in progress (D124). 684 tests total across all three tracks, confirmed everywhere checked. **Real process finding (D123):** all three parallel sessions independently hit and self-caught the same git-index race running in one shared working tree — no data lost, but future parallel IMPLEMENTER work uses separate worktrees going forward. **C.1**/**C.2**/**C.4** not yet started. |
| Milestone D | **Pre-planned, not GO'd, 2026-09-05 (D92 pass).** Zoom/scroll polish, signing/notarization, and the `Reference/c` removal question (D61/Q10). **Meaningfully smaller and lower-risk than assumed at D60's original scoping** — D92 (above) confirmed the git-history-rewrite premise doesn't hold: `Reference/c` removal is a plain `git submodule deinit`, not a destructive rewrite. Zoom/scroll already has real measured data from Wave 7.2's D81 pre-brief (AppKit `NSView` wins at v1's viewport sizes; SwiftUI `Canvas` only wins at near-full-map sizes, which is exactly zoom's territory — the crossover point is already known, not a fresh unknown). Signing/notarization is the one piece of D that isn't primarily an engineering question: `project.pbxproj` has no `DEVELOPMENT_TEAM`/`CODE_SIGN_IDENTITY` keys at all today (confirmed), and needs an actual Apple Developer Team ID from Jerod before that sub-wave can start (Q29) — an account/credential dependency, not a code-complexity one. Proposed split (not yet detailed at sub-wave granularity — D's scope is narrow enough that a single pre-brief per item is likely sufficient rather than needing Wave-7-style splitting): **D.0** zoom/scroll rendering (informed directly by D81's existing crossover measurements); **D.1** signing & notarization (blocked on Q29); **D.2** `Reference/c` submodule removal + README pointer update (per D61/Q10, whenever the port stops actively referencing it — confirmed trivial by D92, no longer a cost that grows with time). | Queued — 3rd of 3 per D93's alphabetical-order ruling, not GO'd until Milestone C closes clean. |


## Wave 5 behavioral benchmarks (PARITY audit, 2026-08-31)

Critical behaviors where XBolo must match original Bolo 0.99.7 — NOT WinBolo:

- **Wall friction:** Original applies substantial friction halting momentum. WinBolo is "like ice." Port the C collision response in `client.c` exactly — do not simplify.
- **Tank deceleration:** Original brakes precisely. WinBolo overshoots. Float-precision tick accumulation (D18) is the correct safeguard.
- **Boat-to-land transition:** Original applies resistance forces at the water/land boundary. WinBolo treats it as a plain speed-zone change. This is NOT captured by `terrainMaxSpeed` — must be ported separately in Wave 5.
- **Mine self-damage:** Original does NOT damage the laying tank on detonation. WinBolo does. Wave 5 mine handler must explicitly skip the owner.
- **Mine self-damage asymmetry (confirmed Wave 5.5a, PARITY):** `smallboom`'s tank-damage check is independent of the self-caused gate — a smallboom CAN damage the tank that caused it. `superboom`'s damage check is nested inside the self-caused gate — a superboom you caused yourself never damages you. Not symmetric; do not assume one implies the other in any future wave touching these paths.
- **Builder retrieval:** Original retrieves stranded builders by proximity. WinBolo requires killing them first. Wave 5 builder-retrieval logic must match the original proximity-only check.
- **Pillbox range:** WinBolo fires ~0.5 squares too far. Use only the C oracle constant — never a WinBolo source.
- **Tick rate:** 50 Hz confirmed in both original and WinBolo. ✅ Consistent with `ticksPerSec` in Physics.swift.

## Three-bot team structure (effective 2026-08-31)

- **IMPLEMENTER** (Xcode Claude agent) — writes Swift, owns DifferentialTests, commits
- **PLANNER** (Claude.app Cowork) — owns this file + AGENT_NOTES.md, issues wave assignments
- **PARITY AUDITOR** (Claude.app, adversarial) — independent behavioral parity review, updates this file         

Communication: all handoffs via AGENT_NOTES.md with explicit [TO: X] tags.

## Wave 5.0 — Physics constants reference (pre-read complete 2026-08-31)

All constants to be added to `Physics.swift` in Wave 5.0. Values from `bolo.h`.

| Swift name | Value | C macro |
|---|---|---|
| tankRadius | 0.375 | TANKRADIUS |
| builderRadius | 0.125 | BUILDERRADIUS |
| shellVelocity | 7.0 | SHELLVEL |
| maxShellRange | 7.0 | MAXRANGE |
| kickForce | 3.125 | KICKFORCE |
| explosionTicks | 24 | EXPLOSIONTICKS *(particle display)* |
| explodeTicks | 45 | EXPLODETICKS *(death animation)* |
| respawnTicks | 150 | RESPAWN_TICKS |
| maxShells | 40 | MAXSHELLS |
| maxMines | 40 | MAXMINES |
| maxArmour | 40 | MAXARMOUR |
| maxTrees | 40 | MAXTREES |
| roadTrees | 2 | ROADTREES |
| wallTrees | 2 | WALLTREES |
| boatTrees | 20 | BOATTREES |
| pillTrees | 4 | PILLTREES |
| maxPlayers | 16 | MAXPLAYERS |
| maxStarts | 16 | MAX_STARTS |
| pillOnboard | UInt8(0xff) | ONBOARD |
| playerNeutral | UInt8(0xff) | NEUTRAL |
| noPill | UInt8(0xff) | NOPILL |
| minBaseArmour | 5 | MINBASEARMOUR |

**Distinguishing `explosionTicks` vs `explodeTicks`:**
- `explosionTicks (24)` — how long an `Explosion` particle effect renders before removal from the list
- `explodeTicks (45)` — how long the dead-tank explosion animation plays before `spawn()` is eligible (`respawncounter > EXPLODETICKS`)
